# 1. 09:30 ~ 10:20
#### \# JSP 웹 프로그래밍과 스프링 프레임워크 책 공부 한번해보기
1. MVC가 뭔가? (8장)
2. Web에 대해서 (HTTP 등)
3. JDBC에 대해서 (9장)
바쁘니까 책에 있는 내용들 코딩해보지는 말지



#### \# 서비스(ProductService) (05.상품.txt)
get 방식에 대한 요청
```java
public Product getProductById(Long id) 메소드
```
상품 id를 넘겨주면 해당하는 상품을 찾아야 함
(ProductService)

- 코드 추가
```java
// 상품 수정 버튼 클릭시 컨트롤러로 갔다가 여기로 옴
public Product getProductById(Long id){
	Optional<Product> product = this.productRepository.findById(id);

	return product.orElse(null);
}
```



#### \# useEffect Hook React 교안 PDF (P.87)
```typescript
useEffect(<function>, <dependency>)
```
1번째 매개 변수는 필수 사항, 2번째 매개 변수는 optional
function : 구현하고자 하는 동작을 명시
dependency : props 또는 state 값을 명시
(props 또는 state 값이 변할때마다 화면 갱신 - useEffect 호출)



#### \# 컨트롤러(ProductController) (05.상품.txt)
get 방식에 대한 요청
```java
public ResponseEntity<Product> getUpdate(@PathVariable Long id)
```
(ProductController)

- 코드 추가
```java
// ProductUpdateForm.tsx에 customAxios.get()때문에 GetMapping해야함
@GetMapping("/update/{id}")
public ResponseEntity<Product> getUpdate(@PathVariable Long id){
	System.out.println("수정할 상품 번호 : " + id);

	Product product = this.productService.getProductById(id);

	if(product == null){
		return ResponseEntity.status(HttpStatus.NOT_FOUND).body(null) ;
	}else{ // 200번대여서 ok
		return ResponseEntity.ok(product) ;
	}
}
```



# 2. 10:30 ~ 11:20
#### \# 서비스(ProductService) (05.상품.txt)
(ProductService)
put 방식에 대한 요청
    public Optional<Product> findById(Long id) 메소드
    private void deleteOldImage(String oldImageFileName) 메소드
    public Product updateProduct(Product savedProduct, Product updatedProduct) 메소드

- 코드 추가
```java
public Optional<Product> findById(Long id) {
	return this.productRepository.findById(id);
}

private void deleteOldImage(String oldImageFileName) {
	// oldImageFileName가 무의미하면 return
	if (oldImageFileName == null || oldImageFileName.isBlank()) {
		return;
	}

	// 기존의 이미지 파일 경로 변수를 이용해서 해당 파일의 경로 설정해서 객체 생성
	File oldImageFile = new File(productImageLocation + oldImageFileName);

	// oldImageFile이 존재하면
	if (oldImageFile.exists()) {
		// 삭제하기 (삭제시 deleted는 true)
		boolean deleted = oldImageFile.delete();
		if (!deleted) { // 삭제 실패 (삭제 실패시 deleted는 false) - (!deleted는 true)
			System.out.println("기존 이미지 삭제 실패 : " + oldImageFileName);
		}
	}
}

// Product 수정 (읽고와서 쓰기 한 것)
public Product updateProduct(Product savedProduct, Product updatedProduct) {
	savedProduct.setName(updatedProduct.getName());
	savedProduct.setPrice(updatedProduct.getPrice());
	savedProduct.setCategory(updatedProduct.getCategory());
	savedProduct.setStock(updatedProduct.getStock());
	savedProduct.setDescription(updatedProduct.getDescription());

	if (updatedProduct.getImage() != null && updatedProduct.getImage().startsWith("data:image")) {
		deleteOldImage(savedProduct.getImage());
		String imageFileName = saveProductImage(updatedProduct.getImage());
		savedProduct.setImage(imageFileName);
	}

	return productRepository.save(savedProduct);
}
```



#### \# 컨트롤러(put 요청) (05.상품.txt)
(ProductController)

- 코드 추가
```java
// 상품 수정
@PutMapping("/update/{id}")
// @PathVariable Long id는 유일한 id가 있음을 표현 - 상품 등록과 수정의 차이점
public ResponseEntity<?> putUpdate(@PathVariable Long id,
								   @Valid @RequestBody Product updatedProduct,
								   BindingResult bindingResult) {
	// 유효성 검사
	if (bindingResult.hasErrors()) {
		Map<String, String> errors = new HashMap<>();
		for (FieldError xx : bindingResult.getFieldErrors()) {
			errors.put(xx.getField(), xx.getDefaultMessage());
		}
		return new ResponseEntity<>(
				Map.of(
						"message", "상품 수정 유효성 검사에 문제가 있습니다.",
						"errors", errors
				),
				HttpStatus.BAD_REQUEST
		);
	}

	// 상품 정보 수정
	Optional<Product> findProduct = productService.findById(id);

	if (findProduct.isEmpty()) {
		return ResponseEntity.notFound().build();
	}

	try {
		Product saveProduct = findProduct.get();
		this.productService.updateProduct(saveProduct, updatedProduct);

		return ResponseEntity.ok(Map.of("message", "상품 수정 성공"));

	} catch (Exception err) {
		return ResponseEntity
			.status(HttpStatus.INTERNAL_SERVER_ERROR)
			.body(
				Map.of(
						"message", err.getMessage(),
						"error", "상품 수정 실패"
				)
			);
	}


}
```



# 3. 11:30 ~ 12:20
#### \# 테스트 시나리오(get 요청) (05.상품.txt)
    이때, 브라우저는 상품 수정 페이지로 이동해야 하며, 기존에 등록되어 있던 상품의 세부 정보가 모두 입력란에 자동으로 표시되어야 합니다.
    예를 들어, 상품명, 가격, 재고 수량, 카테고리, 설명, 이미지 파일명 등의 항목이 기존 데이터와 동일하게 표시되는지를 확인합니다.
    그러나 이미지 \<input type="file">에는 보안상의 이유로 이전에 업로드한 파일 경로/내용을 기본값으로 넣을 수 없습니다.
    이건 브라우저가 파일 입력란에 value를 설정할 수 없도록 막아 놓았기 때문입니다.



#### \# 테스트 시나리오(put 요청) (05.상품.txt)
테스트 검증 항목
    이미지 폴더에 "product_년월일시분초" 형식으로 시작하는 새로운 이미지가 업로드 되어야 합니다.
    데이터 베이스에 이전 행의 정보가 수정이 되어야 합니다.
    상품 목록 페이지에 수정된 정보가 보여야 합니다.
    과거에 업로드했던 이전 이미지를 삭제되어야 합니다.



#### \# 제05장-07. 상세 보기 (05.상품.txt)



#### \# 상품 목록 페이지 (05.상품.txt)
(ProductList.tsx)
    ProductList.tsx 파일
```html
        <Card className="h-100" onClick={() => navigate(`/product/detail/${item.id}`)}>
```

- 코드 추가
<Card className="h-100"
onClick={() => navigate(`/product/detail/${item.id}`)}
style={{ cursor: 'pointer' }}>
여기에서
onClick={() => navigate(`/product/detail/${item.id}`)}
추가하기



#### \# 상품 목록 페이지 (05.상품.txt)
(AppRoutes.tsx)
        ProductDetail 라우터 추가
- 코드 추가
```typescript
import ProductDetail from './../pages/ProductDetail';

<Route path='/product/detail/:id' element={<ProductDetail user={user} />} />
```



#### \# 상품 상세 페이지 작성 (05.상품.txt)
(pages 폴더에 ProductDetail.tsx 파일생성)
    ProductDetail.tsx 신규 작성
- 코드 작성 : ProductDetail01.txt 파일 참조
(약간 수정함)
/*
상품 상세 보기
전체 화면을 좌우측을 1대2로 분리합니다.
왼쪽은 상품의 이미지 정보, 오른쪽은 상품의 정보 및 `장바구니`와 `주문하기` 버튼을 만듭니다.
\*/

```typescript
import customAxios from "./../api/axiosInstance";
import { useEffect, useState } from "react";
import { Button, Card, Col, Container, Form, Row, Table } from "react-bootstrap";
import { useNavigate, useParams } from "react-router-dom";
import { API_BASE_URL } from "../config/config";
import type { Product } from "../types/Product";
import type { User } from "../types/User";

interface AppProps {
    user: User | null
}

function App({ user }: AppProps) {
    const { id } = useParams(); // id 파라미터 챙기기
    const [product, setProduct] = useState<Product>(null); // 백엔드에서 넘어온 상품 정보

    // 로딩 상태를 의미하는 state로, 값이 true이면 현재 로딩 중입니다.
    const [loading, setLoading] = useState(true);

    const navigate = useNavigate();

    // 파라미터 id가 갱신이 되면 화면을 다시 rendering 시킵니다.
    useEffect(() => {
        if (!user) {
            alert('로그인이 필요한 서비스입니다.');
            navigate('/member/login');
            return;
        }

        const url = `${API_BASE_URL}/product/detail/${id}`;

        customAxios
            .get(url)
            .then((response) => {
                setProduct(response.data);
                setLoading(false); // 상품 정보를 읽어 왔습니다.
            })
            .catch((error) => {
                console.log(error);

                if (error.response && error.response.status === 401) { // 401(UnAuthrized)
                    alert('로그인이 필요한 서비스입니다.');
                    navigate('/member/login'); // 로그인 페이지로 리다이렉트

                } else {
                    alert('상품 정보를 불러 오는 중에 오류가 발생하였습니다.');
                    navigate(-1); // 이전 페이지로 이동하기
                }
            });
    }, [id, user, navigate]);

    // 아직 backend에서 읽어 오지 못한 경우를 대비한 코딩입니다.
    if (loading === true) {
        return (
            <Container className="my-4 text-center">
                <h3>
                    상품 정보를 읽어 오는 중입니다.
                </h3>
            </Container>
        );
    }

    // 상품에 대한 정보가 없는 경우를 대비한 코딩입니다.
    if (!product) {
        return (
            <Container className="my-4 text-center">
                <h3>
                    상품 정보를 찾을 수 없습니다.
                </h3>
            </Container>
        );
    }

    return (
        <Container className="my-4">
            <Card>
                <Row className="g-0">
                    {/* 좌측 상품 이미지 */}
                    <Col md={4}>
                        <Card.Img
                            variant="top"
                            src={`${API_BASE_URL}/images/${product.image}`}
                            alt={`${product.name}`}
                            style={{ width: '100%', height: '400px' }}
                        />
                    </Col>
                    {/* 우측 상품 정보 및 구매 관련 버튼 */}
                    <Col md={8}>
                        <Card.Body>
                            <Card.Title className="fd-3">
                                <h3>{product.name}</h3>
                            </Card.Title>
                            <Table striped>
                                <tbody>
                                    <tr>
                                        <td className="text-center">가격</td>
                                        <td>{product.price.toLocaleString()}원</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">카테고리</td>
                                        <td>{product.category}</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">재고</td>
                                        <td>{product.stock.toLocaleString()}개</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">설명</td>
                                        <td>{product.description}</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">등록일자</td>
                                        <td>{product.inputdate}</td>
                                    </tr>
                                </tbody>
                            </Table>

                            {/* 구매 수량 입력란 */}
                            {/* as={Row}는 렌더링시 기본 값인 <div> 말고 Row로 렌더링하도록 해줍니다. */}
                            <Form.Group as={Row} className="mb-3 align-items-center">
                                <Col xs={3} className="text-center">
                                    <strong>구매 수량</strong>
                                </Col>
                                <Col xs={5}>
                                    {/* 구매 수량은 최소 1이상으로 설정했고, user 모드에 따라서 분기 코딩했습니다. */}
                                    <Form.Control
                                        type="number"
                                        min="1"
                                        disabled={!user}
                                    />
                                </Col>
                            </Form.Group>

                            {/* 버튼(이전 목록, 장바구니, 주문하기) */}
                            <div className="d-flex justify-content-center mt-3">
                                <Button variant="primary" className="me-3 px-4" href="/product/list">
                                    이전 목록
                                </Button>
                                <Button variant="success" className="me-3 px-4"
                                    onClick={`일단오류무시`}
                                >
                                    장바구니
                                </Button>
                                <Button variant="danger" className="me-3 px-4"
                                    onClick={`일단오류무시`}
                                >
                                    주문하기
                                </Button>
                            </div>
                        </Card.Body>
                    </Col>
                </Row>
            </Card>
        </Container>
    );
}

export default App;
```



#### \# 컨트롤러 작성 (05.상품.txt)
```html
    public ResponseEntity<Product> detail(@PathVariable Long id) 메소드
```
- 코드 추가
```java
@GetMapping("/detail/{id}")
public ResponseEntity<Product> detail(@PathVariable Long id){
	Product product = productService.getProductById(id) ;

	if (product == null){
		return ResponseEntity.status(HttpStatus.NOT_FOUND).build();
	}else{
		return ResponseEntity.ok(product);
	}
}
```



#### \# 데이터 베이스 확인 (05.상품.txt)
```sql
    select * from products where product_id = 상품번호 ;

select * from products where product_id = 502 ;
```



#### \# 테스트 시나리오 (05.상품.txt)
    상품을 클릭하여 상품 상세 페이지로 이동합니다.
    상품 정보들이 화면에 정확하게 반영되어 있는지, 확인합니다.

502	CAKE	너무 달아요	product_20260413113938.jpg	2026-04-10	호호호	5000	40



#### \# 장바구니.zip 오픈채팅에서 다운로드



# 4. 12:30 ~ 13:20
#### \# 연관 관계 매핑 (SpringBoot 교안 PDF (P.155))
Entity 간의 연관 관계를 서술하는 기법



#### \# 쇼핑 카트 데이터 구조 다이어그램.png
(연관 관계들)
- 카트와 고객은 1대1 관계
(어노테이션 : @OnetoOne)

- 카트와 카트상품 일대다 관계
하나의 카트에는 모든 제품이 담길 수 있음
하나의 카트에는 여러 종류의 상품(항목)이 담긴다.
(어노테이션 : @OnetoMany)

- 카트상품과 상품은 다대일 관계
상품은 모든 사람이 구매할 수 있음
여러 명의 카트에 담긴 '진라면' 항목들은 결국 하나의 '진라면' 상품 정보를 참조한다.
(특정 상품이라는 고유의 특징은 동일하다는 느낌)
(어노테이션 : @ManytoOne)

\* 연관 관계 매핑 적는 순서
해당entity to 어노테이션적는클래스
ex) 카트와 카트상품은 @OnetoMany
카트 클래스에 카트상품 변수를 적고 거기에 어노테이션@OnetoMany를 적으면
One은 카트클래스
Many는 카트상품변수



#### \# 제06장-01. 장바구니 공통 사항 (스프링) (06.장바구니.txt)



#### \# 카트(Cart) (스프링) (06.장바구니.txt)
(entity 폴더에 Cart 클래스 생성)
    Cart.java 파일을 신규 작성합니다.

- 코드 작성 : Cart01.txt 파일 참조
```java
package com.coffee.entity;

import jakarta.persistence.*;
import lombok.Getter;
import lombok.Setter;
import lombok.ToString;

import java.util.List;

@Getter @Setter @ToString
@Entity
@Table(name = "carts")
public class Cart {
    @Id // 기본키(pk) 설정
    @GeneratedValue(strategy = GenerationType.AUTO) // 숫자 자동 생성
    @Column(name = "cart_id") // 실제 데이터 베이스 컬럼명은 cart_id
    private Long id ;

    // 연관 관계 매핑 (Entity간의 연관 관계 서술)
    // fetch : 데이터 베이스에서 데이터를 가져온다는 뜻
    // (우리는 axios를 사용하지만 자바의 기본 기능이 fetch)
    // FetchType.LAZY : 즉시로딩 말고 지연로딩을 의미함 (진짜 필요할때 가져오기)
    // 부모자식 참조관계에서 일대일, 일대다, 다대일은 중요하지 않음
    // 어떤 관계든 자식이 부모의 PK를 FK로 가진다
    @OneToOne(fetch = FetchType.LAZY)
    // @JoinColumn은 자식 테이블에 적어야 하는 어노테이션임 (조인 어노테이션)
    @JoinColumn(name = "member_id") // members테이블의 pk 컬럼명과 동일하게 적어야 함 (관례임)
    private Member member ; // carts테이블에 member_id라는 이름의 members테이블의 fk인 컬럼이 생성됨

    // 여기에는 @JoinColumn을 적지 않음. 왜?
    // 카트안에는 카트상품이 여러개 담길 수 있어서 컬렉션(중에서도 List)로 변수 생성해야 함
    // 카트에는 여러 개의 '카트 상품'들이 담겨야 하므로 List가 좋습니다
    @OneToMany(mappedBy = "cart", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<CartProduct> cartProducts ;
}
```



#### \# 카트 상품(CartProduct) (스프링) (06.장바구니.txt)
(entity 폴더에 CartProduct 클래스 생성)
    CartProduct.java 파일을 신규 작성합니다.

- 코드 작성 : CartProduct01.txt 파일 참조
```java
package com.coffee.entity;

import jakarta.persistence.*;
import lombok.Getter;
import lombok.Setter;
import lombok.ToString;

@Getter
@Setter
@ToString
@Entity
@Table(name = "cart_products")
public class CartProduct {
    @Id // 기본키(pk) 설정
    @GeneratedValue(strategy = GenerationType.AUTO) // 숫자 자동 생성
    @Column(name = "cart_product_id") // 실제 데이터 베이스 컬럼명은 cart_product_id
    private Long id;

    // 부모자식 참조관계에서 일대일, 일대다, 다대일은 중요하지 않음
    // 어떤 관계든 자식이 부모의 PK를 FK로 가진다
    @ManyToOne(fetch = FetchType.LAZY)
    // @JoinColumn은 자식 테이블에 적어야 하는 어노테이션임 (조인 어노테이션)
    @JoinColumn(name = "cart_id") // carts테이블의 pk 컬럼명과 동일하게 적어야 함 (관례임)
    private Cart cart; // cart_products테이블에 cart_id라는 이름의 carts테이블의 fk인 컬럼이 생성됨

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "product_id", nullable = false)
    private Product product ;

    // 카트에 담길 상품들의 갯수
    @Column(nullable = false)
    private int quantity ;
}
```



# 5. 14:20 ~ 15:10
#### \# 연관 관계 매핑 (다대다 관계) (중요!)
다대다 관계(카트 vs 상품)는 그 둘이서 연관관계를 만들 수 없어서
이를 해결하기 위해 중간에 교차 엔터티(카트상품)를 생성해서 해결함



#### \# 카트(Cart) 내용 추가 (스프링) (06.장바구니.txt)
(entity 폴더에 Cart 클래스에 코드 추가)
- 코드 추가
```java
// 여기에는 @JoinColumn을 적지 않음. 왜?
// 카트안에는 카트상품이 여러개 담길 수 있어서 컬렉션(중에서도 List)로 변수 생성해야 함
// 카트에는 여러 개의 '카트 상품'들이 담겨야 하므로 List가 좋습니다
@OneToMany(mappedBy = "cart", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
private List<CartProduct> cartProducts ;
```



#### \# 연관 관계 매핑 - 영속성 전이 (SpringBoot 교안 PDF (P.159))
- CASCADE 종류
ALL 부모 Entity의 영속성 상태 변화를 자식 Entity에게 모두 전이 시킵니다.
등 - 나머지 한 번 보기



# 6. 15:20 ~ 16:10
#### \# 상품 상세 보기 페이지(ProductDetail.tsx) (리액트) (06.장바구니.txt)
(ProductDetail.tsx)
    임포트 구문 수정
```typescript
    import axios from "axios";
    import customAxios from "../api/axiosInstance";
```

    1) quantity 스테이트 신규 선언(주의 : useEffect() 코딩 이전에 작성)
    2) QuantityChange 이벤트 핸들러 함수(주의 : useEffect() 코딩 이전에 작성)
    3) 구매 수량 관련 \<Form.Control>의 onChange={QuantityChange} 속성과 연동시킵니다.
    4)  addToCart 함수를 작성합니다.
    5) \<Button> '장바구니'의 onClick 속성에 addToCart 함수와 연동하는 코드를 작성합니다.

- 코드 작성 : ProductDetail02.txt 파일 참조
```java
/*
```
상품 상세 보기
전체 화면을 좌우측을 1대2로 분리합니다.
왼쪽은 상품의 이미지 정보, 오른쪽은 상품의 정보 및 `장바구니`와 `주문하기` 버튼을 만듭니다.
\*/
```typescript
import axios from "axios";
import customAxios from "./../api/axiosInstance";
import { useEffect, useState } from "react";
import { Button, Card, Col, Container, Form, Row, Table } from "react-bootstrap";
import { useNavigate, useParams } from "react-router-dom";
import { API_BASE_URL } from "../config/config";
import type { Product } from "../types/Product";
import type { User } from "../types/User";

interface AppProps {
    user: User | null
}

function App({ user }: AppProps) {
    const { id } = useParams(); // id 파라미터 챙기기
    const [product, setProduct] = useState<Product | null>(null); // 백엔드에서 넘어온 상품 정보

    // 로딩 상태를 의미하는 state로, 값이 true이면 현재 로딩 중입니다.
    const [loading, setLoading] = useState(true);

    const navigate = useNavigate();

    // 1) quantity 스테이트 신규 선언(주의 : useEffect() 코딩 이전에 작성)
    const [quantity, setQuantity] = useState(0);

    // 2) QuantityChange 이벤트 핸들러 함수(주의 : useEffect() 코딩 이전에 작성)
    const QuantityChange = (
        event: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>
    ) => {
        const newValue = parseInt(event.target.value);
        setQuantity(newValue);
    };



    // 파라미터 id가 갱신이 되면 화면을 다시 rendering 시킵니다.
    useEffect(() => {
        if (!user) {
            alert('로그인이 필요한 서비스입니다.');
            navigate('/member/login');
            return;
        }

        const url = `${API_BASE_URL}/product/detail/${id}`;

        customAxios
            .get(url)
            .then((response) => {
                setProduct(response.data);
                setLoading(false); // 상품 정보를 읽어 왔습니다.
            })
            .catch((error) => {
                console.log(error);

                if (error.response && error.response.status === 401) { // 401(UnAuthrized)
                    alert('로그인이 필요한 서비스입니다.');
                    navigate('/member/login'); // 로그인 페이지로 리다이렉트

                } else {
                    alert('상품 정보를 불러 오는 중에 오류가 발생하였습니다.');
                    navigate(-1); // 이전 페이지로 이동하기
                }
            });
    }, [id, user, navigate]);

    // 아직 backend에서 읽어 오지 못한 경우를 대비한 코딩입니다.
    if (loading === true) {
        return (
            <Container className="my-4 text-center">
                <h3>
                    상품 정보를 읽어 오는 중입니다.
                </h3>
            </Container>
        );
    }

    // 상품에 대한 정보가 없는 경우를 대비한 코딩입니다.
    if (!product) {
        return (
            <Container className="my-4 text-center">
                <h3>
                    상품 정보를 찾을 수 없습니다.
                </h3>
            </Container>
        );
    }

    // 4) addToCart 함수를 작성합니다.
    const addToCart = async () => {
        if (!user) {
            alert('로그인이 필요합니다.');
            navigate('/member/login');
            return;
        }

        if (!product) return;

        if (quantity < 1) {
            alert(`구매 수량은 1개 이상이어야 합니다.`);
            return;
        }
        //alert(`${product.name} ${quantity} 개를 장바구니에 담기`);

        // memberId: user.id,
        try {
            const parameters = {
                productId: product.id,
                quantity: quantity
            };

            const url = `/cart/insert`;
            const response = await customAxios.post(url, parameters);


            alert(response.data);
            navigate('/product/list'); // 상품 목록 페이지로 이동

        } catch (error) {
            console.log('오류 발생 : ' + error);

            if (axios.isAxiosError(error)) {
                console.log(error.response?.data);
                alert('장바구니 추가 실패');
            } else {
                console.log('예상치 못한 오류', error);
            }
        }
    }

    return (
        <Container className="my-4">
            <Card>
                <Row className="g-0">
                    {/* 좌측 상품 이미지 */}
                    <Col md={4}>
                        <Card.Img
                            variant="top"
                            src={`${API_BASE_URL}/images/${product.image}`}
                            alt={`${product.name}`}
                            style={{ width: '100%', height: '400px' }}
                        />
                    </Col>
                    {/* 우측 상품 정보 및 구매 관련 버튼 */}
                    <Col md={8}>
                        <Card.Body>
                            <Card.Title className="fd-3">
                                <h3>{product.name}</h3>
                            </Card.Title>
                            <Table striped>
                                <tbody>
                                    <tr>
                                        <td className="text-center">가격</td>
                                        <td>{product.price.toLocaleString()}원</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">카테고리</td>
                                        <td>{product.category}</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">재고</td>
                                        <td>{product.stock.toLocaleString()}개</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">설명</td>
                                        <td>{product.description}</td>
                                    </tr>
                                    <tr>
                                        <td className="text-center">등록일자</td>
                                        <td>{product.inputdate}</td>
                                    </tr>
                                </tbody>
                            </Table>

                            {/* 구매 수량 입력란 */}
                            {/* as={Row}는 렌더링시 기본 값인 <div> 말고 Row로 렌더링하도록 해줍니다. */}
                            <Form.Group as={Row} className="mb-3 align-items-center">
                                <Col xs={3} className="text-center">
                                    <strong>구매 수량</strong>
                                </Col>
                                <Col xs={5}>
                                    {/* 구매 수량은 최소 1이상으로 설정했고, user 모드에 따라서 분기 코딩했습니다. */}
                                    {/* 3) 구매 수량 관련 <Form.Control>의 onChange={QuantityChange} 속성과 연동시킵니다. */}
                                    <Form.Control
                                        type="number"
                                        min="1"
                                        disabled={!user}
                                        value={quantity}
                                        onChange={QuantityChange}
                                    />
                                </Col>
                            </Form.Group>

                            {/* 버튼(이전 목록, 장바구니, 주문하기) */}
                            <div className="d-flex justify-content-center mt-3">
                                <Button variant="primary" className="me-3 px-4" href="/product/list">
                                    이전 목록
                                </Button>
                                {/*  5) <Button> '장바구니'의 onClick 속성에 addToCart 함수와 연동하는 코드를 작성합니다. */}
                                <Button variant="success" className="me-3 px-4"
                                    onClick={() => {
                                        if (!user) {
                                            alert('로그인이 필요한 서비스입니다');
                                            return navigate('/member/login');
                                        } else {
                                            addToCart();
                                        }
                                    }}
                                >
                                    장바구니
                                </Button>
                                <Button variant="danger" className="me-3 px-4"
                                    onClick={`일단오류무시`}
                                >
                                    주문하기
                                </Button>
                            </div>
                        </Card.Body>
                    </Col>
                </Row>
            </Card>
        </Container>
    );
}

export default App;
```



#### \# 카트 상품 DTO (스프링) (06.장바구니.txt)
(dto 폴더에 CartProductDto 클래스 생성)
    CartProductDto 신규 작성

- 코드 작성
```java
package com.coffee.dto;

import lombok.Getter;
import lombok.Setter;

// 장바구니에 담기 위하여 리액트가 넘겨 주는 파라미터의 값을 저장하기 위한 클래스
@Getter @Setter
public class CartProductDto {
    private Long memberId ;
    private Long productId ;
    private int quantity ;
}
```



#### \# 서비스(MemberService) (스프링) (06.장바구니.txt)
    // import java.util.Optional;
```html
    public Optional<Member> findMemberById(Long memberId) 메소드
```

- 코드 작성
```java
import java.util.Optional;

public Optional<Member> findMemberById(Long memberId){
	return this.memberRepository.findById(memberId);
}
```



#### \# 서비스(ProductService) (스프링) (06.장바구니.txt)
```html
    public Optional<Product> findProductById(Long productId) 메소드
```

- 코드 작성
```java
public Optional<Product> findProductById(Long productId){
	return productRepository.findById(productId);
}
```



#### \# 리포지터리(CartRepository) (스프링) (06.장바구니.txt)
(repository 폴더에 CartRepository 인터페이스 생성)
   신규 작성
   Optional\<Cart> findByMember(Member member) 메소드

- 코드 작성
```java
package com.coffee.repository;

import com.coffee.entity.Cart;
import com.coffee.entity.Member;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface CartRepository extends JpaRepository<Cart, Long> {
    // 이 member가 가진 카트 정보가 있는지 조회하는 메소드 (카트를 가지고 있는지 없는지)
    Optional<Cart> findByMember(Member member);
}
```



#### \# 리포지터리(CartProductRepository) : 신규 작성 (스프링) (06.장바구니.txt)
(repository 폴더에 CartProductRepository 인터페이스 생성)

- 코드 작성
```java
package com.coffee.repository;

import com.coffee.entity.CartProduct;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CartProductRepository extends JpaRepository<CartProduct, Long> {
}
```



# 7. 16:20 ~ 17:10
#### \# 시험



#### \# 시험 문제
﻿웹 개발 기초 및 동작 원리와 클라이언트-서버 모델 이해
\[문항1]  `useState` 훅을 사용하는 주된 이유는 무엇인가요?
 1) 컴포넌트 내부에서 상태 변수를 만들고 그 값을 갱신하실 수 있도록 도와드립니다.
 2) 컴포넌트의 스타일(CSS)을 설정하는 역할을 합니다.
 3) DOM 요소를 직접 조작하는 기능을 제공합니다.
 4) 서버에서 데이터를 자동으로 가져옵니다.

﻿웹 개발 기초 및 동작 원리와 클라이언트-서버 모델 이해
\[문항2]  코드 `errors.general && {errors.general}`의 의미는 무엇인가요?
 1) 항상 Alert를 화면에 보여 드립니다.
 2) errors.general 값이 없을 때만 Alert를 보여 드립니다.
 3) errors.general 값을 초기화하는 코드입니다.
 4) errors.general 값이 존재할 때만 Alert를 화면에 보여 드립니다.

﻿HTML5+CSS3 활용 웹 페이지 구조화를 위한 태크와 요소와 스타일링과 레이아웃 디자인 구현
\[문항3]  `axios.post(url, parameters)`는 주로 어떤 상황에서 사용하시나요?
 1) 기존 리소스를 수정하실 때 사용합니다.
 2) 새로운 리소스를 생성하실 때 서버로 데이터를 전송하실 때 사용합니다.
 3) 서버에서 리소스를 삭제하실 때 사용합니다.
 4) 서버에서 리소스를 조회하실 때 사용합니다.

﻿HTML5+CSS3 활용 웹 페이지 구조화를 위한 태크와 요소와 스타일링과 레이아웃 디자인 구현
\[문항4]  `event.preventDefault();`는 어떤 역할을 하나요?
 1) 브라우저 캐시를 초기화합니다.
 2) 서버와의 네트워크 연결을 차단합니다.
 3) 기본적으로 브라우저가 실행하는 새로고침 동작을 막아 드립니다.
 4) 이벤트 전파를 중지하지 않고 그대로 실행합니다.

﻿JavaScript의 역할과 기본 문법 소개 및 함수 개념 이해
\[문항5]  `useNavigate()` 훅은 어떤 기능을 제공하나요?
 1) URL 파라미터를 읽어 옵니다.
 2) 페이지를 새로고침합니다.
 3) 프로그래밍적으로 특정 경로로 이동할 수 있도록 도와드립니다.
 4) 브라우저의 뒤로가기 기능을 비활성화합니다.

﻿JavaScript의 역할과 기본 문법 소개 및 함수 개념 이해
\[문항6]  아래 코드에서 `isInvalid={!!errors.email}`은 어떤 의미인가요?
 1) errors.email 값이 존재하면 true로 변환하여 에러 표시를 보여 드립니다.
 2) errors.email 값이 없을 때만 true로 설정합니다.
 3) errors.email을 문자열로 변환합니다.
 4) errors.email 값을 초기화합니다.

﻿HTML5, CSS3, JavaScript를 활용한 실제 웹 페이지 개발
\[문항7]  `useEffect(() => {...}, []);`는 어떤 시점에 실행되나요?
 1) state가 변경될 때마다 항상 실행됩니다.
 2) 버튼 클릭 시마다 실행됩니다.
 3) axios 요청이 실패했을 때만 실행됩니다.
 4) 컴포넌트가 처음 화면에 마운트될 때 한 번만 실행됩니다.

﻿HTML5, CSS3, JavaScript를 활용한 실제 웹 페이지 개발
\[문항8]  상품 목록 페이지에서 `products.map((item) => ...)`는 어떤 역할을 하나요?
 1) 상품 배열의 길이를 반환합니다.
 2) 상품 배열을 순회하여 각각의 상품 컴포넌트를 생성합니다.
 3) 상품 배열을 비웁니다.
 4) 상품 배열을 정렬합니다.

﻿주어진 디자인에 따라 레이아웃과 스타일링 작업
\[문항9]  아래 조건문에서 관리자만 버튼을 보이게 하는 조건은 무엇인가요?
 1) `if (user.role == 'ADMIN') return null;`
 2) `if (user?.role !== 'ADMIN') return null;`
 3) `if (user.role = 'ADMIN') return true;`
 4) `if (user = 'ADMIN') return null;`

﻿주어진 디자인에 따라 레이아웃과 스타일링 작업
\[문항10]  아래 코드의 의미는 무엇인가요?
await axios.delete(`${API_BASE_URL}/product/delete/${item.id}`);
 1) 해당 id의 상품을 서버에 추가합니다.
 2) 해당 id의 상품을 서버에서 조회합니다.
 3) 해당 id의 상품을 서버에서 삭제합니다.
 4) 해당 id의 상품 가격만 변경합니다.

﻿주어진 디자인에 따라 레이아웃과 스타일링 작업
\[문항11]  `window.confirm()` 함수는 어떤 값을 반환하나요?
 1) 확인 버튼을 누르면 true, 취소 버튼을 누르면 false를 반환합니다.
 2) 항상 true를 반환합니다.
 3) 항상 false를 반환합니다.

﻿JavaScript를 이용하여 사용자 상호작용 구현
\[문항12]  `useParams()` 훅은 어떤 역할을 하나요?
 1) 브라우저 쿠키 값을 가져옵니다.
 2) 로컬 스토리지 값을 읽어 옵니다.
 3) 서버에서 JSON 데이터를 자동으로 가져옵니다.
 4) URL 경로에 포함된 동적 파라미터 값을 읽어 옵니다.

﻿JavaScript를 이용하여 사용자 상호작용 구현
\[문항13]  상품 수정 페이지에서 `useEffect` 훅을 사용하는 이유는 무엇인가요?
 1) id 값이 변경될 때마다 기존 상품 정보를 서버에서 다시 가져오도록 하기 위함입니다.
 2) state를 초기화하기 위해 사용합니다.
 3) axios 요청을 취소하기 위해 사용합니다.
 4) 상품 이미지를 자동으로 업로드하기 위해 사용합니다.

﻿JavaScript를 이용하여 사용자 상호작용 구현
\[문항14]  아래 코드에서 `const { name, value } = event.target;`의 의미는 무엇인가요?
 1) event 객체를 삭제합니다.
 2) event.target에서 name과 value 속성을 추출하여 지역 변수로 만듭니다.
 3) event.target 값을 문자열로 변환합니다.
 4) event.target 값을 초기화합니다.

﻿Ajax와 비동기 통신 학습
\[문항15]  FileReader 객체의 `readAsDataURL(file)` 메서드는 어떤 역할을 하나요?
 1) 파일을 서버로 전송합니다.
 2) 파일을 삭제합니다.
 3) 파일 이름만 읽어 옵니다.
 4) 파일을 Base64 인코딩 문자열로 변환합니다.

﻿Ajax의 개념과 웹 애플리케이션에서의 활용 방법
\[문항16]  상품 수정 시 axios 요청에서 PUT 메서드를 사용하는 이유는 무엇인가요?
 1) 새로운 데이터를 추가하기 위해 사용합니다.
 2) 데이터를 삭제하기 위해 사용합니다.
 3) 기존 데이터를 수정하기 위해 사용합니다.
 4) 데이터를 읽기 위해 사용합니다.

﻿Ajax의 개념과 웹 애플리케이션에서의 활용 방법
\[문항17]  아래 코드의 역할은 무엇인가요?
setProduct(initial_value);
 1) product 상태를 초기 값으로 되돌립니다.
 2) product 상태를 서버로 전송합니다.
 3) product 상태를 삭제합니다.
 4) product 상태를 문자열로 변환합니다.

﻿서버 데이터를 비동기적으로 가져와 웹 페이지 업데이트
\[문항18]  아래 구문에서 `\[id]`를 의존성 배열로 넣은 이유는 무엇인가요?
```typescript
useEffect(() => { ... }, [id]);
```
 1) id 값을 변경하지 못하게 막기 위해서입니다.
 2) id 값을 초기화하기 위해서입니다.
 3) id 값이 변경될 때마다 useEffect가 다시 실행되도록 하기 위함입니다.
 4) id 값을 로컬 스토리지에 저장하기 위해서입니다.

﻿서버 데이터를 비동기적으로 가져와 웹 페이지 업데이트
\[문항19]  `const parameters = {...product};` 구문은 무엇을 의미하나요?
 1) product 객체를 완전히 비웁니다.
 2) product 객체의 얕은 복사를 만들어 parameters에 저장합니다.
 3) product 객체를 JSON 문자열로 변환합니다.
 4) product 객체를 깊은 복사합니다.

﻿Ajax와 비동기 통신 학습
\[문항20]  아래 코드는 무엇을 의미하나요?
```typescript
const config = { headers: { 'Content-Type': 'application/json' } };
```
 1) 서버 응답이 HTML 형식임을 알려 줍니다.
 2) 서버 응답을 무시합니다.
 3) 데이터를 암호화합니다.
 4) 서버로 전송할 데이터가 JSON 형식임을 알려 줍니다.

﻿컴포넌트의 상태(state)관리와 이벤트 처리
\[문항21]  상품 등록 페이지와 상품 수정 페이지의 가장 큰 차이점은 무엇인가요?
 1) 등록 페이지에서는 카테고리를 선택할 수 없습니다.
 2) 수정 페이지에서는 이미지를 업로드할 수 없습니다.
 3) 수정 페이지에서는 기존 상품 정보를 미리 불러와 보여 줍니다.
 4) 등록 페이지에서는 axios를 사용할 수 없습니다.

﻿Axios 라이브러리를 활용한 서버와의 HTTP 통신 실습
\[문항22]  `onClick={(event) => { event.stopPropagation(); ... }}`의 역할은 무엇인가요?
 1) 클릭 이벤트를 항상 실행합니다.
 2) 클릭 이벤트가 상위 요소로 전파되지 않도록 막습니다.
 3) 클릭 이벤트를 취소하고 페이지를 새로고침합니다.
 4) 클릭 이벤트를 브라우저 콘솔에 출력합니다.

﻿Axios 라이브러리를 활용한 서버와의 HTTP 통신 실습
\[문항23]  상품 가격을 보여 줄 때 `item.price.toLocaleString()`을 사용하는 이유는 무엇인가요?
 1) 가격을 무조건 0으로 초기화하기 위해 사용합니다.
 2) 가격을 서버로 전송하기 위해 사용합니다.
 3) 가격을 JSON으로 변환하기 위해 사용합니다.
 4) 숫자를 지역화된 문자열(3자리 콤마 포함)로 변환하기 위해 사용합니다.

﻿Node.js를 활용한 백엔드 서버 개발
\[문항24]  아래 코드의 역할은 무엇인가요?
```html
<Link to={`/product/insert`}></Link>
```
 1) 클릭 시 상품 등록 페이지로 이동합니다.
 2) 클릭 시 상품 목록을 삭제합니다.
 3) 클릭 시 상품을 서버에 저장합니다.
 4) 클릭 시 브라우저를 새로고침합니다.

React 라우팅과 상태관리
\[문항25]  상품 삭제 성공 후 `setProducts(prev => prev.filter(p => p.id !== item.id));`는 어떤 동작을 하나요?
 1) 삭제된 상품을 제외한 나머지 상품들로 목록을 다시 갱신합니다.
 2) 상품 배열을 모두 삭제합니다.
 3) 상품 배열의 길이를 반환합니다.
 4) 상품 배열을 정렬합니다.



# 8. 17:20 ~ 18:10
#### \# 시험
