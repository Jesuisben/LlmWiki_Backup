# 1. 09:30 ~ 10:20
#### \# 오늘 실습에 필요한 파일 및 과정
4.Development Environment.txt - 파일에 적힌 IP 등을 초기화시키기

Passwordless 강의자료_Docker_ICT학원교육_20260514.pdf

PX1280_ova_server.ova

1.RockyLinux_PasswordlessServer_Docker_Setting.txt



#### \# 실습하기 (로그인 및 서비스 등록) (Passwordless 강의자료_Docker_ICT학원교육_20260514.pdf(P.8~27))
(1.RockyLinux_PasswordlessServer_Docker_Setting.txt)
1\. 기존 VMware의 가상 머신 삭제 후 새로운 ova로 가상머신 생성
(VMware Workstation - File - Open)
PX1280_ova_server.ova 선택 후 열기
경로 지정 (D:\VirtualBox VMs\PX1280) 후 Import

2\. 가상머신 로그인
1) 아이디, 비밀번호 : root / 1qaz2wsx3e#

3\. IP 주소 찾기
가상머신(가상머신 IP) : ifconfig - {가상머신 IP}
cmd (로컬PC IP) : ipconfig - {로컬PC IP}
Public IP(퍼블릭 IP) : https://www.whatismyip.com - {퍼블릭 IP}
-\> 4.Development Environment.txt 파일에 작성하기

4\. Passwordless Members 사이트 접속 및 로그인
https://members.passwordlessalliance.org/

5\. 서비스 추가
서비스 모드 : 테스트 모드
서비스 이름 : ICT 실습1
서비스 도메인 : (가상머신 IP) {가상머신 IP}
서비스의 사설 IP : (가상머신 IP,로컬PC IP,Public IP) : {가상머신 IP},{로컬PC IP},{퍼블릭 IP}
유저 커넥션 서버의 도메인 : {가상머신 IP}

등록하기

6\. MobaXterm으로 세션 생성 및 접속
(MobaXterm)
생성 : Sesstion - Remote host ({가상머신 IP}) - ok
아이디 : root
비밀번호 : 1qaz2wsx3e#
알림창은 cancel

7\. 설정 및 네트워크 설정
(MobaXterm)
```shell
dnf config-manager --add-repo=https://download.docker.com/linux/centos/docker-ce.repo
dnf -y install docker-ce docker-ce-cli containerd.io
```

```shell
systemctl start docker
systemctl enable docker
systemctl status docker
```

```shell
docker network create some-network
docker network ls
```

8\. PasswordlessX1280 Docker 컨테이너 실행
(MobaXterm)
(1.RockyLinux_PasswordlessServer_Docker_Setting.txt 파일에서 코드(IP주소) 수정 후 가져오기)
```shell
docker run -d --name server \
--network some-network \
--restart always \
--cpus=1 \
--cpuset-cpus="0" \
--cpu-shares=1024 \
--memory=1g \
--memory-swap=2g \
-e DOMAIN={가상머신 IP} \
-e USE_SSL=false \
-v auth-settings:/opt/x1280/tomcat/conf/passwordless \
-v auth-logs:/opt/x1280/tomcat/logs \
-v user-connection-logs:/opt/x1280/connector/logs \
-v push-request-logs:/opt/x1280/pushConnector/logs \
-v config:/etc/opt/x1280 \
-v database:/var/lib/mysql \
-p 8006:8005 \
-p 8080:8080 \
-p 8180:8180 \
-p 8143:8143 \
-p 8443:8443 \
-p 11040:11040 \
-p 12010:12010 \
-p 15010:15010 \
dualauth/passwordless-x1280-single:latest
```

\* MobaXterm 붙여넣기 설정
Settings - Configuration - Terminal - Paste using right-click 체크 - OK



# 2. 10:30 ~ 11:20
#### \# 실습하기 이어서 하기 (로그인 및 서비스 등록) (Passwordless 강의자료_Docker_ICT학원교육_20260514.pdf(P.28~38))
(1.RockyLinux_PasswordlessServer_Docker_Setting.txt)
9\. 방화벽 오픈
(Docker 컨테이너의 방화벽이 아니고 Docker가 설치된 Host 서버의 방화벽을 오픈)
(Docker 컨테이너의 방화벽은 Docker 컨테이너 설치 시 자동으로 오픈됨)
1) 설치
```shell
dnf install -y firewalld
```

2) 적용 및 확인
```shell
systemctl start firewalld
systemctl enable firewalld
systemctl status firewalld
```

```shell
firewall-cmd --list-all
```

```shell
firewall-cmd --zone=public --permanent --add-port=8005/tcp
firewall-cmd --zone=public --permanent --add-port=8080/tcp
firewall-cmd --zone=public --permanent --add-port=8180/tcp
firewall-cmd --zone=public --permanent --add-port=8143/tcp
firewall-cmd --zone=public --permanent --add-port=8443/tcp
firewall-cmd --zone=public --permanent --add-port=11040/tcp
firewall-cmd --zone=public --permanent --add-port=12010/tcp
firewall-cmd --zone=public --permanent --add-port=15010/tcp
```

```shell
firewall-cmd --reload
firewall-cmd --list-all
```

```shell
systemctl restart docker.service # (리스타트는 안해도 될 때가 있지만 이왕이면 리스타트를 생활화하는게 좋다)
```


10\. Docker 컨테이너 확인
```shell
docker image ls
docker network ls
docker volume ls
```


11\. Docker Guest 서버 접속
```shell
docker exec -it server /bin/bash
```

12\. 이동 및 설치
```shell
cd /opt/x1280/tomcat
```

```shell
dnf -y install procps # (ps 명령어 사용을 위한 설치)
```

13\. 서버 확인
1) Docker Auth Server 확인
```shell
ps -ef | grep tomcat
```

2) Docker User Connection Server 확인
```shell
cd /opt/x1280/connector
```

```shell
ps -ef | grep connect
ps -ef | grep nginx
```

3) Docker Push Request Server 확인
```shell
cd /opt/x1280/pushconnector
```

```shell
ps -ef | grep push
```

13-1. 설치된 Docker 리셋하는 법
```shell
docker stop server
docker rm server
docker rmi dualauth/passwordless-x1280-single:latest
```

```shell
docker volume rm auth-logs auth-settings config database push-request-logs user-connection-logs
docker network rm some-network
```

```shell
Restart the Docker PasswordlessX1280 installation from the beginning.
```

삭제 후 1번부터 다시 실행하면 됨


14\. Passwordless Members 사이트 접속 및 setting.ap 파일 다운
https://members.passwordlessalliance.org/

15\. 접속하기 및 setting,.ap 업로드 및 로그인 (admin / admin)
https://{가상머신 IP}:8143/

16\. 서버 아이디 및 서버키 발급 / 서버 API 사용 종류 설정
(https://{가상머신 IP}:8143/) - (서비스 서버)
1) 정보 가져오기 (해당 데이터를 4.Development Environment.txt 파일에 저장)
서버 아이디 : {서버 아이디}
서버키 : {서버 키}

2) API 사용 종류 설정
서버 이름 클릭 후 REST로 설정


#### \# 실습하기 이어서 하기
(2.Local_Dev_windows.txt)
17\. MySQL 정지 (Port가 마리아 DB와 겹쳐서 정지시킴)
검색창에 서비스 검색
MySQL 찾은 후 서비스 중지
MySQL 우클릭 - 속성 - 시작유형 수동으로 변경


18\. 마리아 DB 설치 및 설정 (2.Local_Dev_windows.txt)
1) 설치
(패스워드리스 자료 - mariadb-10.6.5-winx64.msi 실행)
New root password : {MASKED}
Confirm : {MASKED}
Use UTF8 as default server's character set 체크
설치

2) 설정 (환경변수)
시스템 환경 변수 편집 검색 - 환경 변수 - 시스템 변수 (Path 편집) - 새로 만들기 (C:\Program Files\MariaDB 10.6\bin) - 확인

19\. cmd로 마리아 DB 접속 (마리아DB 설치 및 환경변수 설정 확인용)
(cmd)
```shell
mysql -uroot -p
패스워드 입력 ({MASKED})
```

20\. 마리아 DB 데이터 넣기
1) 데이터 넣기전 잔여 데이터 삭제
```sql
drop database passwordless;

USE mysql;

delete from db where user = 'pwless';
delete from user where user = 'pwless';

flush privileges;
```

2) 데이터 넣고 나가기
```sql
create database passwordless;
CREATE USER 'pwless'@'%' IDENTIFIED BY 'qwer!@#$';
CREATE USER 'pwless'@'localhost' IDENTIFIED BY 'qwer!@#$';

GRANT ALL PRIVILEGES ON passwordless.* TO 'pwless'@'%';
GRANT ALL PRIVILEGES ON passwordless.* TO 'pwless'@'localhost';

flush privileges;

USE passwordless;

CREATE TABLE userinfo (
	`id` varchar(1000) NOT NULL,
	`pw` VARCHAR(1000) NOT NULL,
	`regdate` datetime NULL default NOW(),
	PRIMARY KEY (`id` ASC)
);

desc userinfo;

exit
```



# 3. 11:30 ~ 12:20
#### \# 실습하기 이어서 하기
(2.Local_Dev_windows.txt)

21\. Java 설치
(패스워드리스 자료 - jdk-21_windows-x64_bin.msi 파일 실행)

22\. sts-5.0.1.x86_64.RELEASE.zip 압축해제
(D: - project 폴더 생성 후 sts-5.0.1.x86_64.RELEASE.zip 파일 복사 후 압축해제)
sts-5.0.1.x86_64.RELEASE 폴더에 lombok.jar 확인

23\. SpringToolsForEclipse 설치
(D드라이브 - project - sts-5.0.1.x86_64.RELEASE - SpringToolsForEclipse.exe 실행)
경로는 (D드라이브 - project - workspace_edu 폴더 생성 후 선택)


24\. SpringToolsForEclipse에 깃허브 리파이토리 버튼 설정
(SpringToolsForEclipse - Window - Show View - Other... 클릭 후 Git - Git Repositories.. Open하기)
Open된 View를 중앙 하단으로 옮기기


25\. 깃허브에서 repository 복사
https://github.com/PasswordlessAlliance
(https://github.com/PasswordlessAlliance/PasswordlessX1280-with-Java-RestAPI-webservice.git)
Repository에서 PasswordlessX1280-with-Java-RestAPI-webservice의 코드 복사해서
SpringToolsForEclipse에서 Git Repositories에서 Clone repository눌러서 (D드라이브 - project - workspace_edu 폴더 생성 후 선택) 해당 경로에 클론

26\. 클론한 repository를 메이븐 프로젝트로 생성
(SpringToolsForEclipse)
File - Import... - Maven - Existing Maven Projects 선택 후 Next
Root Directory를 D:\project\workspace_edu\PasswordlessX1280-with-Java-RestAPI-webservice로 설정 후 (pom.xml이 뜬거 확인후) next
Maven으로 프로젝트가 생성됨 (시간이 걸림)



# 4. 12:30 ~ 13:20
#### \# 실습하기 이어서 하기
(2.Local_Dev_windows.txt)

27\. 생성된 프로젝트에 JWT 라이브러리 추가
생성된 프로젝트 우클릭 Properties - Java Build Path - Libraries - Classpath(add JARs 클릭) - src - main - webapp - WEB-INF - lib - java-jwt-3.18.3.jar
apply and close

28\. Maven 업데이트
프로젝트 우클릭 - Maven - Update Project.. - OK

29\. Java 설정파일 수정
\* Ctrl + Shift + R : 파일 검색하기 (config.properties 검색 및 오픈)
```properties
passwordless.corpId = {서버 아이디}
passwordless.serverId = {서버 아이디}

passwordless.serverKey = {서버 키}

passwordless.simpleAutopasswordUrl = http://{가상머신 IP}:8080

passwordless.restCheckUrl = http://{가상머신 IP}:11040

passwordless.pushConnectorUrl = ws:{가상머신 IP}:15010
```

30\. Java 실행하기
프로젝트 우클릭 - Run As - Spring Boot App

31\. 웹페이지 확인 및 실습해보기
http://localhost/

1) 계정 생성 : test01 / test01

2) 패스워드리스 등록

3) 패스워드리스로 로그인

\* 이 방식은 패스워드리스로 로그인하면 비밀번호가 자동으로 임의의 값으로 변경이 됨

로그아웃하고 그냥 test01 / test01로 로그인하면 비밀번호가 다르다고 나옴
또한 그냥 비밀번호로 로그인을 하기위해서는 패스워드리스 등록을 해제하라고 나옴

4) 패스워드리스 해제

5) 비밀번호로 로그인해보기



# 5. 14:20 ~ 15:10
#### \# 실습하기 이어서 하기
(3.RockyLinux_ApplicationServer_Setting.txt)
(5.vi 기본명령어 모음.txt 파일내의 내용 확인하기)

32\. 마리아DB 서버 설치
(MobaXterm)
```shell
yum install -y mariadb-server  > /dev/null
```

```shell
systemctl start mariadb
systemctl enable mariadb
```

32\. openjdk21 설치
```shell
yum install -y java-21-openjdk java-21-openjdk-devel
```

```shell
vi /etc/profile
```
-\> 마지막에 export JAVA_HOME="/usr/lib/jvm/java" 추가

```shell
source /etc/profile
```

33\. tomcat 설치
```shell
dnf install -y wget
wget http://archive.apache.org/dist/tomcat/tomcat-9/v9.0.30/bin/apache-tomcat-9.0.30.tar.gz

tar -zxvf apache-tomcat-9.0.30.tar.gz
mv apache-tomcat-9.0.30 /usr/local/tomcat
```

34\. 유저 생성
```shell
useradd -d /usr/local/tomcat -M -s /sbin/nologin tomcat
```

35\. tomcat 유저 디렉토리 설정 및 권한 설정
```shell
chown -R tomcat:tomcat /usr/local/tomcat
vi /usr/local/tomcat/conf/server.xml
```
-\> \<Connector port="8080" protocol="HTTP/1.1".... 이부분의 8080을 80으로 수정하고 저장

```shell
/usr/local/tomcat/bin/shutdown.sh
/usr/local/tomcat/bin/startup.sh
```

36\. 방화벽 설정
```shell
dnf install -y firewalld
```

```shell
systemctl enable firewalld
systemctl start firewalld

firewall-cmd --zone=public --list-all
firewall-cmd --zone=public --permanent --add-port=80/tcp
firewall-cmd --reload
```

37\. tomcat 웹페이지 확인
http://{가상머신 IP}/

38\. 마리아 DB 실행 및 입력
```shell
mysql -uroot
```

```sql
create database passwordless;
CREATE USER 'pwless'@'%' IDENTIFIED BY 'qwer!@#$';
CREATE USER 'pwless'@'localhost' IDENTIFIED BY 'qwer!@#$';

GRANT ALL PRIVILEGES ON passwordless.* TO 'pwless'@'%';
GRANT ALL PRIVILEGES ON passwordless.* TO 'pwless'@'localhost';

flush privileges;

USE passwordless;

CREATE TABLE userinfo (
	`id` varchar(1000) NOT NULL,
	`pw` VARCHAR(1000) NOT NULL,
	`regdate` datetime NULL default NOW(),
	PRIMARY KEY (`id` ASC)
);

desc userinfo;

exit;
```


39\. 로컬에 있는 데이터를 서버에서 보여주기
(SpringToolsForEclipse 중지)

1) 프로젝트 우클릭 - Run As - Maven Install
(처음에는 실패되고 2번째는 성공함)

2) Maven 파일 실행
D:\project\workspace_edu\PasswordlessX1280-with-Java-RestAPI-webservice\targe 경로에 ROOT_rest.war 생성됨

3) ROOT_rest.war 파일을 드래그 앤 드롭으로 MobaXterm 세션에 복사

4) 명령어 실행
```shell
/usr/local/tomcat/bin/shutdown.sh
rm -rf /usr/local/tomcat/webapps/ROOT
rm -f /usr/local/tomcat/webapps/ROOT.war
cp ./ROOT_rest.war /usr/local/tomcat/webapps/ROOT.war
/usr/local/tomcat/bin/startup.sh
```

5) 웹페이지 열고 여러가지 확인해보기
http://{가상머신 IP}

5-1) 설정파일만 수정하고 다시 웹페이지 켜기
(설정파일의 내용 수정 후 끄고 다시 키기)
```shell
vi /usr/local/tomcat/webapps/ROOT/WEB-INF/classes/properties/config.properties
```

```properties
passwordless.corpId = {MASKED}
passwordless.serverId = {MASKED}

passwordless.serverKey = {MASKED}
```

```shell
/usr/local/tomcat/bin/shutdown.sh
/usr/local/tomcat/bin/startup.sh
```



# 6. 15:20 ~ 16:10
이어서 학습

# 7. 16:20 ~ 17:10
이어서 학습

# 8. 17:20 ~ 18:10
이어서 학습
