# 1. 09:30 ~ 10:20
#### \# API 사용법 (0.Passwordless_X1280_인증서버_REST_API_20260511.pdf(P.1~))
- API를 사용할때 필요한 Postman 설치
https://www.postman.com/downloads/



# 2. 10:30 ~ 11:20
#### \# 가상머신 클론
1) (VMware Workstation)
기존 Default Rocky 8.9의 스냅샷을 이용해 Default 상태로 되돌리고 가상머신을 끄고
manage에서 클로닝해서 PX1280이라는 가상머신을 만듬 (full cloning)

PX1280 가상머신을 실행하고 ifconfig로 ip확인하기
-\> {MASKED}

2) (MobaXterm 세션 생성)
로그인 ({MASKED} / {MASKED})

3) 4.Development Environment.txt 파일에 IP 주소 작성
MyPC IP (Local PC): {MASKED}

AuthServer IP (VM) : {MASKED}

Public IP (https://www.whatismyip.com) : {MASKED}


#### \# 실습하기 - API 사용법 (0.Passwordless_X1280_인증서버_REST_API_20260511.pdf)
(1.RockyLinux_PasswordlessServer_Docker_Setting.txt)
1\. 세션에서 명령어 입력
(MobaXterm)
```shell
dnf config-manager --add-repo=https://download.docker.com/linux/centos/docker-ce.repo
dnf -y install docker-ce docker-ce-cli containerd.io
```

```shell
systemctl start docker
systemctl enable docker
systemctl status docker
```

```shell
docker network create some-network
docker network ls
```

```shell
docker run -d --name server \
--network some-network \
--restart always \
--cpus=1 \
--cpuset-cpus="0" \
--cpu-shares=1024 \
--memory=1g \
--memory-swap=2g \
-e DOMAIN={MASKED} \
-e USE_SSL=false \
-v auth-settings:/opt/x1280/tomcat/conf/passwordless \
-v auth-logs:/opt/x1280/tomcat/logs \
-v user-connection-logs:/opt/x1280/connector/logs \
-v push-request-logs:/opt/x1280/pushConnector/logs \
-v config:/etc/opt/x1280 \
-v database:/var/lib/mysql \
-p 8006:8005 \
-p 8080:8080 \
-p 8180:8180 \
-p 8143:8143 \
-p 8443:8443 \
-p 11040:11040 \
-p 12010:12010 \
-p 15010:15010 \
dualauth/passwordless-x1280-single:latest
```

```shell
dnf install -y firewalld
```

```shell
systemctl start firewalld
systemctl enable firewalld
systemctl status firewalld
```

```shell
firewall-cmd --list-all
```

```shell
firewall-cmd --zone=public --permanent --add-port=8005/tcp
firewall-cmd --zone=public --permanent --add-port=8080/tcp
firewall-cmd --zone=public --permanent --add-port=8180/tcp
firewall-cmd --zone=public --permanent --add-port=8143/tcp
firewall-cmd --zone=public --permanent --add-port=8443/tcp
firewall-cmd --zone=public --permanent --add-port=11040/tcp
firewall-cmd --zone=public --permanent --add-port=12010/tcp
firewall-cmd --zone=public --permanent --add-port=15010/tcp
```

```shell
firewall-cmd --reload
firewall-cmd --list-all
```

```shell
systemctl restart docker.service
```

```shell
docker image ls
docker network ls
docker volume ls
```

2\. 웹페이지 들어가기
1) 패스워드리스 맴버스 (Setting.ap 파일을 다운받기 위해서)
https://members.passwordlessalliance.org/aod/web/login/p/login

2) MobaXterm으로 만든 서버의 포트로 들어가서 Setting.ap 파일 업로드
https://{MASKED}:8143/

3) 관리자로 접속해서 서비스 서버에서 서버 아이디와 서버키 발급
서버 아이디 : {MASKED}
서버키 : {MASKED}
4.Development Environment.txt  파일에 입력

3\. [KR][교육용 빈칸] Passwordless X1280 API v1.postman_collection.json 파일 다운 후 Postman에 import하기

4\. Postman 사용해보기
1) 
import한 JSON 파일 제목 선택 후 Variables (변수 관리) 에  Value값 넣기
serverKey : {MASKED}
Ctrl + S 로 저장하기

2) 
사용자 등록여부 확인 (isAP)
{{AuthServerIP}}라는 변수의 값을 {MASKED}로 넣기

3) 사용자 등록여부 확인 (isAP) - {{userId}}
{{userId}}라는 변수의 값을 testuser01로 넣기

4) Send하고 그 결과값을 Raw에서 JSON으로 표기해서 보기
```json
{
    "result": true,
    "msg": "OK.",
    "code": "000.0",
    "data": {
        "exist": false
    }
}
```

5\. 나머지도 해보기


# 3. 11:30 ~ 12:20
5\. 나머지도 해보기


# 4. 12:30 ~ 13:20
5\. 나머지도 해보기


# 5. 14:20 ~ 15:10



# 6. 15:20 ~ 16:10


# 7. 16:20 ~ 17:10


# 8. 17:20 ~ 18:10
