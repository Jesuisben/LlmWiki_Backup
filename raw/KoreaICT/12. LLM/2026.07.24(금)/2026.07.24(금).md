# 1. 09:30 ~ 10:20
#### \# 웹 UI와의 연동 - streamlit (LLM(이론).pdf (P.193))
파이썬 코드만으로 웹 UI(대시 보드, 챗봇 화면 등)를 만들 수 있는 프레임워크

- 사용 예시
![[Pasted image 20260724094020.png]]



#### \# 웹 UI와의 연동 - streamlit (LLM(이론).pdf (P.190~192))

- 실행 및 접속
```text
# 기본 실행 방법 : app.py은 사용자가 생성한 파이썬 파일 이름입니다.
streamlit run app.py

--server.port 8501` : 포트 지정
--server.address 0.0.0.0` : 외부 접속 허용
→ 예시:
streamlit run app.py --server.port 8501 --server.address 0.0.0.0

접속: `http://localhost:8501`
외부 네트워크: `http://<서버IP>:8501`
```

- 사용 예시 01
```text
# 설치해야 할 패키지
pip install streamlit

역할
LLM 실습, 챗봇 수업용으로 매우 많이 사용됨

실행
import streamlit as st
```

- 사용 예시 02
```text
터미널 또는 명령 프롬프트(CMD)에서 다음과 같이 실행합니다.
# app.py 파일을 streamlit 서버로 실행하는 역할을 합니다.
# 웹 브라우저(http://localhost:8501)가 자동으로 열립니다.
streamlit run app.py

다음과 같은 메시지가 보이는 데, 그냥 엔터키를 누릅니다.
Welcome to streamlit!

	If you'd like to receive helpful onboarding emails,
news, offers, promotions,
	and the occasional swag, please enter your email
address below. Otherwise,
	leave this field blank.

Email: [0m

You can find our privacy policy at https://streamlit.io/privacy-policy

Summary:
- This open source library collects usage statistics.
- We cannot see and do not store information contained inside
streamlit apps,
… 중략

You can now view your streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.0.4:8501
```



#### \# 웹 UI와의 연동 - 입력 위젯(Input Widgets) (LLM(이론).pdf (P.194~197))
사용자 입력을 받는 UI 요소들을 의미하며, streamlit 내부적으로 자동 상태 저장(stateful) 구조를 가짐

- 입력 위젯

| 위젯 | 설명 | 예시 코드 |
|---|---|---|
| st.text_input() | 문자열 입력 | name = st.text_input("이름 입력") |
| st.number_input() | 숫자 입력 | age = st.number_input("나이", 0, 120) |
| st.slider() | 범위 선택 | level = st.slider("난이도", 1, 10, 5) |
| st.selectbox() | 드롭다운 선택 | lang = st.selectbox("언어", ["Python", "R", "C++"]) |
| st.checkbox() | 체크박스 | agree = st.checkbox("동의합니다") |
| st.file_uploader() | 파일 업로드 | file = st.file_uploader("CSV 업로드") |


- 출력 요소

| 출력 함수 | 용도 | 예시 |
|---|---|---|
| st.write(‘text’) | 텍스트, 변수, 데이터 프레임 자동 처리 | st.write(df) |
| st.dataframe() | 스크롤 가능한 표 | st.dataframe(df) |
| st.table() | 정적 표 | st.table(df.head()) |
| st.json() | JSON 포맷 출력 | st.json({\"name\": \"AI\", \"ver\": 1.0}) |
| st.line_chart() | 선형 차트 | st.line_chart(df) |
| st.bar_chart() | 막대 차트 | st.bar_chart(df) |


- 사용 예시 01

| 항목 | 설명 |
|---|---|
| st.sidebar | side bar 영역이라는 의미로 사용됩니다.<br>사이드 바는 일반적으로 "설정 패널" 역할을 합니다. |
| st.title("💬 Chatbot") | 웹페이지의 큰 제목(H1)을 만들어 줍니다. |
| st.session_state | 사용자별 데이터를 기억하는 저장소(streamlit의 메모리)<br>dict를 요소로 하는 list 자료 구조(대화는 순서가 있음)입니다. |
| st.session_state.messages | 대화 기록이 저장된 리스트(list)입니다.(대화 Database) |
| st.chat_message(somerole) | 채팅 말풍선 UI를 생성합니다.<br>somerole에 따라서 아이콘 모양이 달라집니다.<br>write() 메소드를 사용하여 글자를 입력합니다. |
| st.chat_input('text') | streamlit의 채팅 입력창으로 사용자가 Enter 키를 누르면 문자열을 반환합니다.<br>사용 예시) st.chat_input("메뉴를 물어보세요") |
| st.info('text') | 파란색 안내 박스 내에 text라는 문자열을 출력해 줍니다. |
| st.stop() | streamlit 실행을 즉시 중단합니다. |
| st.markdown('text') | Markdown(서식이 적용된 글) 문법을 사용해 텍스트를 화면에 출력하는 함수입니다. |
![[Pasted image 20260724095242.png]]


- 사용 예시 02

| 항목 | 설명 |
|---|---|
| @st.cache_resource | 한 번만 생성해 계속 재사용해야 하는 객체(Resource)를 캐시하는 데 사용하는 데코레이터입니다.<br>비용이 많이 드는 객체를 한 번만 만들고, 이후에는 동일한 객체를 재사용합니다. |
| st.set_page_config() | 웹 브라우저에서 표시되는 페이지의 기본 설정을 지정합니다.<br>이 함수는 반드시 Streamlit 코드에서 가장 먼저 호출해야 합니다. |
| st.subheader('text') | HTML의 \<h3> 정도의 크기로, 중간 크기의 제목을 출력하는 데 사용합니다. |
| st.expander('text') | 접었다 펼칠 수 있는 영역을 만듭니다. |
```python
st.set_page_config(
	page_title="RAG 챗봇",
	page_icon=" ",
	layout="wide"
)
```

```text
page_title은 브라우저 탭에 표시되는 제목입니다.
page_icon은 브라우저 탭의 아이콘입니다.
layout은 화면 폭을 결정합니다.
```



#### \# 웹 UI와의 연동 - streamlit 위젯 실습 (LLM(실습).pdf (P.21))
(chapter02_prompt_engineering - streamlit_widget.py)
(실습용 파일은 streamlit_widget_new.py)

- 실행 결과
![[Pasted image 20260724094942.png]]


- 코드 내용
```python
import streamlit as st

st.set_page_config(
    page_title="Streamlit 첫 예제",
    page_icon="🎈",
    layout="centered"
)

# LLM(실습).pdf (P.21)
# 페이지 상단에 큰 제목을 출력
st.title("Hello Streamlit~")

# 제목 아래에 일반 텍스트 출력
st.write("Streamlit 앱이 정상적으로 동작하고 있습니다.")

# 가로 구분선 생성
st.divider()

# 중간 크기의 제목을 출력
st.header("입력 위젯 데모")

# 사용자에게 문자열을 입력받는 텍스트 입력 위젯
# React의 <input type="text">와 비슷한 역할
name = st.text_input("이름")

# number_input('나이', 최소값, 최대값, 보여지는값)
age = st.number_input("나이", 0, 120, 25)

# 여러 선택지 중 하나를 선택하는 드롭다운 위젯
# HTML의 <select> 또는 콤보박스와 비슷한 역할
lang = st.selectbox("주 사용 언어", ["Python", "R", "Java", "C++"])

# falsy, truthy 개념
# st.button()은 버튼을 클릭한 실행에서 True를 반환
if st.button("확인"):
    # 성공 메시지를 초록색 알림 상자 형태로 화면에 출력하는 함수
    st.success("입력이 완료되었습니다.")
    # 일반 텍스트 출력 (마크다운 문법이 적용되어 ###을 이용하면 크기가 커짐)
    st.write("### 입력 결과")
    # 마크다운 문법이 적용되어 **을 사용하면 굵은 글씨가 됨
    st.write(f"**이름** : {name}")
    st.write(f"**나이** : {age}")
    st.write(f"**언어** : {lang}")
    st.success(f"{name}님은 {age}세이며, {lang}을(를) 사용합니다.")
```


- 실습 화면
![[Pasted image 20260724103327.png]]
![[Pasted image 20260724103016.png]]



# 2. 10:30 ~ 11:20
#### \# 웹 UI와의 연동 - 디자인 템플릿 및 UI 활용 (LLM(이론).pdf (P.198))
디자인은 처음부터 새로 만들기보다, 기존 템플릿을 참고하고 커스터마이징하는 것이 훨씬 효율적

- 디자인 원칙
```text
1. 처음에는 완성된 구조를 복제하라.
2. 필요할 때만 커스터마이징하라.
3. 시각보다는 기능과 명료성을 우선하라.
```

- 참고 자료

| 항목 | 설명 |
|---|---|
| streamlit 공식 갤러리 | https://streamlit.io/gallery |
| GitHub | `awesome-streamlit`, `streamlit-extras` 등 |
| 커뮤니티 오픈 소스 | HuggingFace Spaces, streamlit Community Cloud |

- 추후 배포 방법
```text
1. 도메인 구매(가비아, GoDaddy, AWS Route53 등)
2. 클라우드 서버(AWS, GCP, Naver Cloud 등)에 배포
3. Nginx/Apache Reverse Proxy 설정
 - `http://chat.my-domain.com` 으로 접근 가능하게 구성
```



#### \# 프롬프트 엔지니어링 - 출력 요소 (LLM(실습).pdf (P. 22))
(chapter02_prompt_engineering - streamlit_output_components.py)
(실습용 파일 : streamlit_output_components_new.py)

- 코드 내용
```python
import streamlit as st
import pandas as pd

st.header("가전 제품 판매량 예제")

data = pd.DataFrame({
    '가전제품': ['냉장고', '세탁기', 'TV', '전자레인지', '에어컨'],
    '판매량': [120, 95, 150, 80, 110]
})

st.dataframe(data) # 표 출력

chart_data = data.set_index("가전제품")

st.subheader("막대 그래프")
st.bar_chart(chart_data)

st.divider()

st.subheader("꺾은선 그래프")
st.line_chart(chart_data)
```


- 실습 화면
![[Pasted image 20260724111519.png]]



#### \# DbBrowserForSqlite
구글에 "DbBrowserForSqlite" 검색
https://sqlitebrowser.org/blog/version-3-13-1-released/
"- [DB.Browser.for.SQLite-v3.13.1-win64.msi](https://github.com/sqlitebrowser/sqlitebrowser/releases/download/v3.13.1/DB.Browser.for.SQLite-v3.13.1-win64.msi) - Standard (MSI) installer for Win64" 다운로드하기



# 3. 11:30 ~ 12:20
#### \# Function Calling - Function Calling이란? (LLM(이론).pdf (P.69))
LLM이 사용자의 질문을 이해한 뒤, 미리 정의된 함수 중 어떤 것을 어떤 인자로 호출해야 하는지 결정해 주는 기능
자연어로 요청하면 LLM이 함수를 사용해야겠다라고 생각해서 알아서 함수를 호출해서 값을 반환함
![[Pasted image 20260724113934.png]]



#### \# Function Calling - 테이블 생성 (LLM(실습).pdf (P.36))
(chapter03_function_calling - db_init.py)
(실습용 파일 : db_init_new.py)
Function Calling을 사용하여 데이터 베이스에 대한 CRUD 작업을 수행하는 코드를 작성

- 요구 사항
다음과 같은 코드를 사용하여 데이터 베이스에 샘플용 데이터를 추가
```sql
CREATE TABLE IF NOT EXISTS products(
	product_id INTEGER PRIMARY KEY,
	name TEXT NOT NULL,
	price INTEGER NOT NULL,
	stock INTEGER NOT NULL
)
products = [
	(1, 'Keyboard', 30000, 15),
	(2, 'Mouse', 15000, 40),
	(3, 'Monitor', 200000, 7),
	(4, 'USB Cable', 5000, 100),
]
```
![[Pasted image 20260724114151.png]]


- 출력 결과
```text
사용자 : 상품 1의 이름과 단가를 알려줘
DEBUG: …중략
function=Function(arguments='{"product_id": 1, "field": "name"}', name='get_product_info'), type='function'),
ChatCompletionMessageFunctionToolCall(id='call_9STwewIegK1Hb5hnuZh6qV9g', function=Function(arguments='{"product_id": 1, "field":
"price"}', name='get_product_info'), type='function')])
AI : 상품 1의 이름은 "Keyboard"이며, 단가는 30,000원입니다.

사용자 : 10만원짜리 하드 디스크 HDD를 10개 추가하는 코드 작성
DEBUG: …중략
function=Function(arguments='{"name":"Hard Disk Drive(HDD)","price":100000,"stock":10}', name='add_product'), type='function')])
AI : 새로운 상품 "Hard Disk Drive(HDD)"가 단가 100,000원으로 재고 10개가 추가되었습니다.

사용자 : exit
```
![[Pasted image 20260724114230.png]]


- 코드 내용
```python
# python 설치시 기본으로 설치됨
import sqlite3

# 사용할 SQLite 데이터베이스 파일 이름
# 해당 파일이 없으면 연결할 때 새로 생성됨
DB_NAME = "database.db"

# 데이터베이스를 초기화하는 함수
def init_database():
    # SQLite 데이터베이스 파일에 연결
    # database.db가 없으면 현재 실행 위치에 새로 생성됨
    conn = sqlite3.connect(DB_NAME)

    # SQL문을 실행하기 위한 Cursor 객체 생성
    cursor = conn.cursor()

    # SQL문 실행
    # 상품(products) 테이블 생성
    cursor.execute("""
    CREATE TABLE IF NOT EXISTS products(
        product_id INTEGER PRIMARY KEY,
        name TEXT NOT NULL,
        price INTEGER NOT NULL,
        stock INTEGER NOT NULL
    )
    """)

    # 재실습을 위하여 기존 데이터 삭제
    cursor.execute("delete from products")

    # 샘플 데이터 추가
    items = [
        (1, 'Keyboard', 30000, 15),
        (2, 'Mouse', 15000, 40),
        (3, 'Monitor', 200000, 7),
        (4, 'USB Cable', 5000, 100),
    ]

    # executemany(sql, parameters) : 같은 SQL문을 여러 데이터에 반복 실행하는 함수
    # sql : 반복해서 실행할 하나의 SQL문
    # parameters : SQL문에 넣을 여러 데이터 묶음
    cursor.executemany(
        # ?를 placeholder라고 부름 (원래 html에서 입력상자 기본값으로도 사용)
        # ?가 무언가로 치환될 것이라고 말하는 것 (치환 대상 items의 내용들)
        #INSERT INTO products VALUES (1, 'Keyboard', 30000, 15);
        # INSERT INTO products VALUES (2, 'Mouse', 15000, 40);
        # INSERT INTO products VALUES (3, 'Monitor', 200000, 7);
        # INSERT INTO products VALUES (4, 'USB Cable', 5000, 100);
        "insert into products values(?, ?, ?, ?)",
        items
    )

    # 지금까지 실행한 INSERT, UPDATE, DELETE,
    # CREATE TABLE 등의 변경 내용을 데이터베이스에 최종 반영
    conn.commit()

    # 데이터베이스 연결 종료
    conn.close()

    print(f"{DB_NAME} 데이터베이스 생성 완료")

if __name__ == "__main__":
    init_database()
```


- 설치한 SQLite로 database.db 파일 열기
![[Pasted image 20260724121200.png]]



# 4. 12:30 ~ 13:20
#### \# Function Calling - 테이블 생성 (LLM(실습).pdf (P.37))
(chapter03_function_calling - db_main.py, db_functions.py)
(실습용 파일 : db_main_new.py, db_functions_new.py)

- 출력 결과
```text
사용자 : 상품 1의 이름과 단가를 알려줘
DEBUG: …중략
function=Function(arguments='{"product_id": 1, "field": "name"}', name='get_product_info'), type='function'),
ChatCompletionMessageFunctionToolCall(id='call_9STwewIegK1Hb5hnuZh6qV9g', function=Function(arguments='{"product_id": 1, "field":
"price"}', name='get_product_info'), type='function')])
AI : 상품 1의 이름은 "Keyboard"이며, 단가는 30,000원입니다.

사용자 : 10만원짜리 하드 디스크 HDD를 10개 추가하는 코드 작성
DEBUG: …중략
function=Function(arguments='{"name":"Hard Disk Drive(HDD)","price":100000,"stock":10}', name='add_product'), type='function')])
AI : 새로운 상품 "Hard Disk Drive(HDD)"가 단가 100,000원으로 재고 10개가 추가되었습니다.

사용자 : exit
```
![[Pasted image 20260724121507.png]]


- 코드 내용 (db_main_new.py)
```python
import json
from openai import OpenAI

from utility.env_util import get_api_key
from db_functions_new import get_product_info, add_product, tool_list


find_api = "OPENAI_API_KEY"
api_key = get_api_key(find_api)

client = OpenAI(api_key=api_key)

'''
1. 함수 정의
2. Tool 정의
3. Tool 목록 작성
4. LLM에 Tool 목록 전달
    
'''

'''
상품 1의 이름과 단가를 알려줘.
10만원짜리 하드 디스크 HHD를 10개 추가하는 코드 작성
상품 번호 3번의 재고 수량을 50으로 변경해주세요.
'''

# ai_tools : LLM이 사용할 함수
def get_ai_response(message_list, ai_tools=None):
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=message_list,
        tools=ai_tools
    )
    return response

messages = [
    {
        "role": "system",
        "content": "너는 상품 정보를 안내하는 친절한 튜터야. 2~3문장 이내로 간결하게 답변해."
    }
]

while True:
    user_input = input("사용자 : ")

    if user_input.lower() == "exit":
        break

    messages.append({
        "role":"user",
        "content": user_input
    })

    response = get_ai_response(messages, ai_tools=tool_list)
    ai_message = response.choices[0].message
    print("AI Message : ", ai_message)

    messages.append(ai_message)

    # tool_calls 항목에 따른 분기 코딩
    if ai_message.tool_calls:
        for one_tool in ai_message.tool_calls:
            arguments =json.loads(one_tool.function.arguments)

            if one_tool.function.name == "get_product_info":
                result = get_product_info(
                    product_id=arguments["product_id"],
                    field=arguments["field"]
                )

            elif one_tool.function.name == "add_product":
                result = add_product(
                    name=arguments["name"],
                    price=arguments["price"],
                    stock=arguments["stock"]
                )
            # end if

            messages.append({
                "role":"tool",
                "tool_call_id":one_tool.id,
                "name":one_tool.function.name,
                "content":result
            })

        # end for

        response = get_ai_response(messages, ai_tools=tool_list)
        ai_message = response.choices[0].message
    # end if
# end while
'''
```


- 코드 내용 (db_functions_new.py)
```python
import sqlite3

DB_NAME = "database.db"

#############################################################
# LLM이 호출하여 실제 동작을 수행하는 함수
#############################################################

# 사용자 : 상품 1의 이름과 단가를 알려줘
def get_product_info(product_id: int, field: str):
    """
    상품 ID와 조회할 항목(name, price, stock)을 받아 결과 반환
    """
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()

    query = f"SELECT {field} FROM products WHERE product_id = ?"
    cursor.execute(query, (product_id,))
    result = cursor.fetchone()

    print("get_product_info 함수 결과")
    print(result)

    conn.close()

    if result:
        return f"상품 번호 {product_id}의 필드 {field}의 반환 값 {result}"
    else:
        return "상품을 찾을 수 없습니다."

# 사용자 : 10만원짜리 하드 디스크 HDD를 10개 추가하는 코드 작성
def add_product(name: str, price: int, stock: int):
    """
    새로운 상품을 DB에 추가
    """
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()

    cursor.execute(
        "INSERT INTO products (name, price, stock) VALUES (?, ?, ?)",
        (name, price, stock)
    )

    conn.commit()
    conn.close()

    return f"상품 추가 완료: {name} / {price}원 / 재고 {stock}개"
#############################################################
# 툴에 대한 명세표
#############################################################
get_product_info_tool = {
    "type": "function",
    "function": {
        "name": "get_product_info",
        "description": "상품 정보를 조회합니다.",
        "parameters": {
            "type": "object",
            "properties": {
                "product_id": {
                    "type": "integer",
                    "description": "상품 번호"
                },
                "field": {
                    "type": "string",
                    "description": "조회할 항목 (name, price, stock)"
                }
            },
            "required": ["product_id", "field"]
        }
    }
}
add_product_tool = {
    "type": "function",
    "function": {
        "name": "add_product",
        "description": "새로운 상품을 추가합니다.",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "상품명"
                },
                "price": {
                    "type": "integer",
                    "description": "단가"
                },
                "stock": {
                    "type": "integer",
                    "description": "재고 수량"
                }
            },
            "required": ["name", "price", "stock"]
        }
    }
}
#############################################################
# LLM에게 전달해줄 Tool List
#############################################################
tool_list = [
    get_product_info_tool,
    add_product_tool
]
```



# 5. 14:20 ~ 15:10
#### \# Function Calling - 테이블 생성 (LLM(실습).pdf (P.37)) 이어서 작성


# 6. 15:20 ~ 16:10
#### \# Function Calling - 테이블 생성 (LLM(실습).pdf (P.37)) 이어서 작성


# 7. 16:20 ~ 17:10



# 8. 17:20 ~ 18:10
