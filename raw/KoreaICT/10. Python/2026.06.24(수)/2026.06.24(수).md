# 1. 09:30 ~ 10:20
#### \# 내장 함수 - enumerate 함수 (Python(실습).pdf (P.80))
#### \# EnumerateTest.py (Python(실습).pdf (P.80))

- 코드 내용
```python
# EnumerateTest.py
fruits = ['사과', '감', '오렌지', '한라봉', '바나나']

# enumerate() : 매개변수의 요소의 색인 번호와 값을 나열(열거)하는 함수
# 자바의 enum(열거형 타입)과 비슷한 맥락임
for xx in enumerate(fruits):
    print(xx)
print("*"*30)

# enumerate 함수는 iterable 객체의 색인 번호와 값을 튜플 형식으로 반환해 줍니다.
# 일반적으로 현재 요소의 인덱스를 사용하고자 할 때 주로 사용합니다.
for idx, value in enumerate(fruits):
    message = '%d번째 품목은 \'%s\'입니다.' % (idx, value)
    print(message)
print('-'*30)

# start : 색인 번호를 나열할때의 기본값(시작점) 설정 (기본값은 0)
# 타입 - idx : int / value : string
for idx, value in enumerate(fruits, start=1):
    message = '품목 \'%s\'의 인덱스 : %d' % (value, idx)
    print(message)
print('-'*30)

# 타입 - item : tuple
for item in enumerate(fruits):
    message = '%d번째 품목은 \'%s\'입니다.' % (item[0], item[1])
    print(message)
print('-'*30)

print('*은 가변 매개 변수로 인식되며, tuple으로 취급됩니다.')
for item in enumerate(fruits):
    # string의 함수 format()을 이용해서 매개변수를 문자열 안에 넣기
    message = '{}번째 값 : {}'.format(*item)
    print(message)
print('-'*30)

# enumerate() 함수는 iterable이면 다 사용가능하지만
# 키와 값을 짝지어 모두 반환하는 items() 함수는 키와 값을 가진 사전만 사용 가능함
# 공통점 : tuple 타입으로 반환
# 차이점 : enumerate() - 인덱스, 값 반환 / items() - 키, 값 반환
# 사전의 items()라는 함수는 key, value를 tuple 형식으로 반환해 줍니다.
coffees = {'아메리카노':5000, '카페라떼':6000, '카푸치노':5500}

for name, price in coffees.items():
    message = '\'%s\'의 단가는 %d원입니다.' % (name, price)
    print(message)
print()

for item in coffees.items():
    message = '\'%s\'의 단가는 %d원입니다.' % (item[0], item[1])
    print(message)
print()

for item in coffees.items():
    message = '품목 \'{}\'의 단가 : {}원'.format(*item)
    print(message)
print()
```



#### \# 사용자 정의 함수 (Python(이론).pdf (P.113))
개발자가 함수를 직접 만들 수 있음

- 자바와 다른점
1) 반환 타입 없음
자바와는 다르게 파이썬에서 메소드를 정의할때 반환타입을 적을 필요없음
자동으로 반환값의 타입으로 반환됨

2) 매개변수 타입 없음

-\> 1), 2) 가 되는 이유 : 파이썬은 자바와 다르게 타입을 선언하지 않음
변수의 값에 따라 그 변수의 타입이 정해짐 (이런 방식을 동적 타이핑이라고 불림)

3) 예약어 사용
def라는 예약어 사용 def(define)

4) 기본값 설정 가능 (자바는 이걸 오버로딩으로 우회함)
\* 기본값 있는 매개변수 뒤에는 기본값 없는 매개변수가 올 수 없음
-\> 따라서 가장 앞에 있는 매개변수에 기본값을 넣으려면 나머지 뒤에 있는 매개변수들에게 기본값을 다 줘야함


- 함수 정의
문법 :
```python
def 메소드명(매개변수) :
	return 반환값
```

ex)
```python
def add(first, second) :
	return first + second
```

\* 매개변수에 기본값 넣기
매개변수=기본값
ex)
```python
def add(first, second=50) :
	return first + second
```
-\> 이렇게 하면 second의 기본값은 숫자인 50으로 설정하는 것임



#### \# AddFunction.py (Python(실습).pdf (P.85))

- 코드 내용
```python
# 사용자 정의 함수
def add(first, second=50) :
    return first + second

su01 = 14
su02 = 5
# positional argument : 위치 기반 매개 변수 (위치 기반 인수)
# -> 함수를 호출 할때 순서(위치)로 매개변수를 구분하는 방식
result = add(su01, su02)
print(result)
print(add(100, 200))

# positional argument + keyword argument
# 2개의 방식을 섞을때는 반드시 positional argument가 먼저 나와야 함
print(add(100, second=200))

# 다중 할당
su01, su02 = 111, 222
# keyword argument : 키워드 기반 매개 변수 (키워드 인수)
# 키워드 기반이기에 매개변수 순서가 바뀌어도 값이 같음
# 매개변수 이름을 모를때는 ()안에서 Ctrl + Space하면 보임
result = add(second=su01, first=su02)
print(result)

# TypeError: add() missing 1 required positional argument: 'second'
# 필수(required) 매개변수(argument)인 'second'가 없다는 경고문
result = add(10)
print(result)
```



# 2. 10:30 ~ 11:20
#### \# RandomSuGenerate.py (Python(실습).pdf (P.89))
1) 요구 사항
```text
a이상 b이하의 랜덤한 정수 n개를 list로 만들어 주는 함수 randomGen() 함수를 구현해보세요.

힌트
import random
random.randint(a, b)
```

2) 출력 결과
```text
[5, 5, 7] (랜덤)
```

3) 코드 내용
```python
import random

def randomGen(a, b, n) :
    mylist = []
    for idx in range(n):
        # randint(a, b) : a 이상, b 이하의 랜덤한 수 (a, b포함)
        # randrange(a, b) : a 이상, b 미만의 랜덤한 수 (a 포함, b 미포함)
        mylist.append(random.randint(a, b))
    return mylist

a, b, n = 5, 8, 3

result = randomGen(a, b, n)

print(result)
```



#### \# SungjukTest.py (Python(실습).pdf (P.94))
1) 요구 사항
```text
# 이름과 국영수 정보를 입력 받아서, 합격 여부를 출력해주는 함수 sungjukInfo()를 구현해 보세요.
# 합격은 평균이 70점 이상이어야 합니다.
# 영어/수학 각 과목의 미응시자에 대하여 기본 점수를 부여하도록 합니다.
# 기본 점수 : 영어(50), 수학(60)
```

2) 출력 결과
```text
김철수 학생의 정보
국어 : 50, 영어 : 60, 수학 : 70
총점 : 180, 평균 : 60.00, 합격 여부 : 불합격

박영희 학생의 정보
국어 : 80, 영어 : 50, 수학 : 60
총점 : 190, 평균 : 63.33, 합격 여부 : 불합격

심현철 학생의 정보
국어 : 100, 영어 : 90, 수학 : 30
총점 : 220, 평균 : 73.33, 합격 여부 : 합격

권유정 학생의 정보
국어 : 50, 영어 : 50, 수학 : 60
총점 : 160, 평균 : 53.33, 합격 여부 : 불합격
```

3) 코드 내용
```python
def sungjukInfo(name, kor, eng = 50, math = 60) :
    total = kor + eng + math
    average = total / 3.0

    if average >= 70.0 :
        passOrFail = "합격"
    else:
        passOrFail = "합격"

    print("%s 학생의 정보" % name)
    print("국어 : %d, 영어 : %d, 수학 : %d" % (kor, eng, math))
    print("총점 : %d, 평균 : %.2f, 합격 여부 : %s" % (total, average, passOrFail))
    print("="*40)

name, kor, eng, math = "김철수", 50, 60, 70
sungjukInfo(name, kor, eng, math)

sungjukInfo("박영희", 80)

sungjukInfo(math = 30, eng = 90, name = "심현철", kor=100)

sungjukInfo("권유정", 80, math = 90)

# 불가능
# sungjukInfo(math=30, "최철식", 80, 90)
```



# 3. 11:30 ~ 12:20
#### \# ArbitraryArgument01.py (Python(실습).pdf (P.98)) (중요함!!)

- 코드 내용
```python
# 매개변수 앞에 *적으면 튜플 타입이고 **적으면 사전 타입임

# 중요한 규칙!
# 1. 함수 생성시와 사용시 매개변수의 순서가 정해져있음
# (일반 -> *가변 위치 매개변수(args) -> **가변 키워드 매개변수(kwargs))
# 2. 하나의 함수의 매개변수에서 가변 위치 매개변수와 가변 키워드 매개변수는 하나씩만 넣을 수 있음
# 3. 가변 위치 매개변수나 가변 키워드 매개변수는 함수내에서 사용할때는 *나 **를 제거하고 사용함
def showData(category, *items, **keyword):
    print(f"category : {type(category)}")
    print(f"items : {type(items)}")
    print(f"keyword : {type(keyword)}")

    print('카테고리 이름 : %s' % category)
    print('가변 매개 변수 정보 출력')
    for element in items:
        print(element, end=' ')
    # end for
    print()

    print('키워드 매개 변수 정보 출력')
    for (key, value) in keyword.items():
        print('key=%s, value=%s' % (key, value))
    # end for

    print()

# showData 함수의 매개변수가 (일반, 가변 위치 변수, 가변 키워드 매개변수)로 구성이 되어있어서
# 1. 함수 사용시 맨 처음의 매개변수는 자동으로 일반으로 들어가게 됨
# 2. 나머지 매개변수 중에서 위치 기반 변수들은 모두 가변 위치 변수로 들어가고 튜플로 생성됨
# 3. 나머지 매개변수 중에서 키워드 기반 변수들은 가변 키워드 매개변수에 들어가게 되고 사전으로 생성됨
showData('과일', '바나나', '사과', '오렌지', 파인애플=100, 귤=30)
showData('커피', '아메리카노', '카페라떼', '바닐라라떼', '디카페인커피')
```



# 4. 12:30 ~ 13:20
#### \# lambda 함수 (Python(실습).pdf (P.99))
#### \# LambdaTest01.py (Python(실습).pdf (P.99))

- 코드 내용
```python
def hap(a, b) :
    return a + b

x, y = 14, 5
result = hap(x, y)
print("일반 함수 방식 : %d" % result)

# 람다 방식으로 함수 생성하는 법
# def 사용안함
# lambda m, n 이 부분의 m과 n이 매개변수
# : 뒤에 있는 m + n 은 return 내용임
# 이렇게 만들어진 람다 함수를 hap_lambda라는 변수에 할당함
# 람다 함수는 원래 이름 없는 익명 함수이지만
# 사실상 함수명이 hap_lambda가 되어서 사용할때는 hap_lambda()를 하면 됨
hap_lambda = lambda m, n : m + n

# 람다 방식으로 함수 사용하는 법
result = hap_lambda(10, 20)
print("람다 방식 01 : %d" % result)

# 람다 함수와 List comprehension 이용하기
somedata = [2, 5, 7]
pow_lambda = lambda a : a**2
# result = [su**2 for su in somedata]
result = [pow_lambda(su) for su in somedata]
print(result)

# 람다 함수와 List comprehension 이용해서 3의 배수의 비율 구하기
somedata = [idx for idx in range(1, 11)]
three_bae = lambda a : a % 3 == 0 # 논리값 반환
# result = [su % 3 == 0 for su in somedata]
result = [three_bae(su) for su in somedata]
print(result)
# True는 1로 False는 0으로 계산됨
print(sum(result) / len(result))

# 내장함수 filter()
# 문법 : filter(함수, iterable) - 함수는 True/False를 반환하는 함수
# iterable의 요소 중에 함수를 거쳐 True가 나오는 것들만 걸러서 주는 함수
result = list(filter(three_bae, somedata))
print(result)

# 내장함수 map()
# 문법 : map(함수, iterable)
# iterable의 요소 중에 함수를 동작시켜 새로운 데이터를 만들어 내는 함수
# 함수를 넣을때는 함수명()를 넣지 않고 ()를 제거한 함수명만 넣어서 매개변수화 시킴
print('리스트 요소 각각의 값에 3을 곱한 결과')
somedata = [2, 5, 7]
map_lambda = lambda num: 3*num

# somedata의 각 요소들을 map_lambda라는 람다 함수에 적용시켜 주세요.
result = list(map(map_lambda, somedata))
print(result)
```



# 5. 14:20 ~ 15:10
#### \# 문제풀이 (Python(실습).pdf (P.81/86/90/96/100))
#### \# BuiltExercise02.py (Python(실습).pdf (P.81))
1) 요구 사항
```text
다음 human 데이터를 이용하여 출력 결과와 같이 출력되도록 합니다.
human = ['김철수', '홍길동', '박영희', '심수련', '유재민']
enumerate() 함수를 사용하여, 인덱스 값이 짝수인 회원들만 따로 추출하여 mylist를 만들어 보세요.

listComprehension 기능을 사용하여, 다음과 같은 데이터를 만들어 보세요.
newhuman = ['김철수님', '박영희님', '유재민님']
```

2) 출력 결과
```text
['김철수', '박영희', '유재민']
['김철수님', '박영희님', '유재민님']
```

3) 코드 내용
```python
human = ['김철수', '홍길동', '박영희', '심수련', '유재민']
newhuman = [value + "님" for idx, value in enumerate(human) if idx % 2 == 0]
print(newhuman)
```



#### \# FuncTest01.py (Python(실습).pdf (P.86))
1) 요구 사항
다음 연산을 수행하는 함수 func01()을 구현해 보세요.
2\*su01+3\*su02
두 번째 매개 변수는 기본 값 5를 가질 수 있습니다

2) 출력 결과
```text
su01 : 3
su02 : 5
결과 : 21
```

3) 코드 내용
```python
def func01(a, b=5) :
    return 2*a+3*b

su01, su02 = 3, 5
result = func01(su01, su02)

print(f"su01 : {su01}")
print(f"su02 : {su02}")
print(f"결과 : {result}")
```



#### \# TernaryFunction01.py (Python(실습).pdf (P.90))
1) 요구 사항
3개의 정수형 매개 변수 x = 3, y = 8, z = 5의 최소 값을 구하는 함수 min 생성을 생성하세요.

1-1) 추가 문제
max 함수 구현(가장 큰 수 구하기)

2) 출력 결과
```text
최소값은 : 3
최대값은 : 8
```

3) 코드 내용
```python
mylist = []

def min01(x, y, z):
    mylist.append(x)
    mylist.append(y)
    mylist.append(z)
    print(f"최소값은 : {min(mylist)}")

def max01(x, y, z):
    mylist.append(x)
    mylist.append(y)
    mylist.append(z)
    print(f"최대값은 : {max(mylist)}")

min01(3, 8, 5)
max01(3, 8, 5)
```



#### \# PrintStar.py (Python(실습).pdf (P.96))
1) 요구 사항
카운터 수 n을 입력 받아서 별을 n번 출력하는 함수 starPrint()를 작성해 보세요.
한줄에 별을 5개씩 출력해야 합니다.
별의 개수가 입력이 되지 않으면, 기본 값으로 10번이 출력되어야 합니다.
starPrint(24)
starPrint()

2) 출력 결과
```text
* * * * *
* * * * *
* * * * *
* * * * *
* * * *

* * * * *
* * * * *
```

3) 코드 내용
```python
def starPrint(n=10) :
    for idx in range(1, n + 1):
        if idx % 5 == 0 :
            print("*")
        else:
            print("*", end="")

starPrint(24)
print("\n")
starPrint()
```



#### \# LambdaTest02.py (Python(실습).pdf (P.100))
1) 요구 사항
사용자 정의 함수와 람다(lambda) 함수를 사용하는 프로그램을 작성해 보세요.

2) 출력 결과
```text
두 수의 곱셈을 반환하는 람다 함수 mul_lambda 만들기
람다 방식 01 : 70
1부터 10까지의 정수 중 3의 배수가 아닌 수의 비율)
[True, True, False, True, True, False, True, True, False, True]
0.7
1부터 10까지의 정수 중 3의 배수가 아닌 수만 출력하기
[1, 2, 4, 5, 7, 8, 10]
리스트 요소 각각에 100을 더한 결과를 출력해 보세요
[120, 150, 170]
```

3) 코드 내용
```python
print('두 수의 곱셈을 반환하는 람다 함수 mul_lambda 만들기')
mul_lambda = lambda a, b: a*b
result = mul_lambda(7, 10)
print(f"람다 방식 01 : {result}")

print('1부터 10까지의 정수 중 3의 배수가 아닌 수의 비율')
mylist = [idx for idx in range(1, 11)]
three_bae = lambda a: a % 3 != 0
result = [three_bae(idx) for idx in mylist]
print(result)
print(sum(result) / len(result))

print('1부터 10까지의 정수 중 3의 배수가 아닌 수만 출력하기')
mylist = [idx for idx in range(1, 11)]
result = list(filter(three_bae, mylist))
print(result)

print('리스트 요소 각각에 100을 더한 결과를 출력해 보세요')
mylist = [20, 50, 70]
mul_lambda = lambda num: num+100
result = list(map(mul_lambda, mylist))
print(result)
```



# 6. 15:20 ~ 16:10
문제풀이 마무리



# 7. 16:20 ~ 17:10
#### \# 파일 생성 (ch06_package_module 폴더 안에)
exam.py
test.py

- exam.py 코드 내용
```python
# exam.py
def mysum(a, b) :
    return a + b

# __name__ 변수 : 파이썬에서 사용하는 해당 모듈의 이름을 의미하는 특수 형태의 내장 변수
# 프로그램(파이썬 파일)을 실행하는 주체에 따라서 __name__ 변수의 값이 달라짐
# 본인이 본인을 실행하면 (실행 주체가 본인이면) __name__ 변수의 값은 자동으로 "__main__"이 됨
# -> 여기서 실행하면 __name__의 값이 "__main__"이여서 그 조건에 맞는 동작이 실행됨
if __name__ == "__main__" :
    print(__name__)
    print("덧셈 결과 : %d" % mysum(2, 3))
else:
    print("다른 모듈이 %s 모듈을 호출함" % __name__)

print("a모듈 %s가 종료됨" % __name__)
```


- test.py 코드 내용
```python
# test.py
# exam.py의 __name__ 변수의 값이 ch06_package_module.exam 이걸로 바뀜
# exam.py를 불러오는 순간 exam.py 파일 전체가 실행됨(함수 생성 등)
from ch06_package_module.exam import mysum

su01, su02 = 3, 5

result = mysum(su01, su02)
print(result)

# 여기서의 __name__ 변수는 test.py의 변수라서 값이 __main__으로 나옴
print("모듈 %s가 종료되었습니다." % __name__)
```



#### \# __name__ 변수 (Python(실습).pdf (P.129))



#### \# 모듈 (Python(이론).pdf (P.126))
모듈(module)은 함수, 변수, 클래스 등을 포함하는 파이썬 코드 파일
파이썬에서 파일 하나를 모듈 하나라고 이해해도 됨

1\. 표준 모듈 (pip로 설치 여부 X)
파이썬에 기본적으로 포함 되어 있는 모듈
1) 표준 모듈 함수 (impot 필요)
ex) math.sqrt(), time.sleep(), random.randint()

2) 내장 함수 (impot 불필요)
ex) print(), len(), type()


2\. 사용자 생성 모듈 (pip로 설치 여부 X)
개발자(프로그래머)가 직접 만든 모듈

3\. 외부(Third Party) 모듈 (pip로 설치 여부 O)
협력 업체나 개인이 만들어서 제공해주는 모듈
ex) 판다스



#### \# 파이썬에서 제공하는 자주 쓰는 모듈들 (Python(이론).pdf (P.130~))
한번 보기


#### \# 패키지 (Python(이론).pdf (P.124))
폴더 -\> 패키지
-\> 패키지는 모듈들을 묶어놓은 폴더 개념

패키지(package)는 모듈(module)을 계층적으로 구조화하여 관리하는 방법

패키지는 유사한 기능들을 수행하는 모듈들을 하나의 폴더에 묶어 두기 위한 개념
패키지는 디렉터리로 구성되어 있으며, 각 디렉터리는 하나 이상의 모듈 파일을 포함할 수 있음
이를 통해 프로젝트를 여러 모듈로 나누고 관련 모듈을 그룹화하여 구성할 수 있음
패키지는 모듈을 구조화하고 네임스페이스를 관리하는 데 사용



#### \# 패키지, 모듈, 함수 정리
패키지 : 모듈(파이썬 파일)들이 모여있는 폴더(디렉토리)
모듈 : 함수나 여러 코드들이 들어있는 파이썬 파일
함수 : 모듈 안에 들어있는 동적 코드

ex)
상위 패키지 : somefolder
서브 패키지 : mymath
모듈 : MathModule.py
함수 : square_root
-\> 위를 전제로 import해서 함수를 사용하는 방법들

- 방법 1: 모듈까지 import
```python
import somefolder.mymath.MathModule
somefolder.mymath.MathModule.square_root(4)
```

- 방법 2: 모듈을 별칭으로
```python
import somefolder.mymath.MathModule as mm
mm.square_root(4)
```

- 방법 3: 서브패키지까지 import
```python
from somefolder.mymath import MathModule
MathModule.square_root(4)
```

- 방법 4: 서브패키지를 별칭으로
```python
from somefolder.mymath import MathModule as mm
mm.square_root(4)
```

- 방법 5: 함수 직접 import
```python
from somefolder.mymath.MathModule import square_root
square_root(4)
```

- 방법 6: 함수를 별칭으로
```python
from somefolder.mymath.MathModule import square_root as sr
sr(4)
```



# 8. 17:20 ~ 18:10
#### \# somefolder 다운로드 및 이동
오픈채팅에서 해당 .zip파일 다운 후 압축해제 후
python_project에 드래그 앤 드롭하기



#### \# 패키지 실습 (Python(실습).pdf (P. 104))
- 패키지 이름 : somefolder.mymath
-\> 상위 패키지 : somefolder / 서브 패키지 : mymath

- 모듈 : MathModule.py

- 함수 : square_root

- 설명 : 루트 값을 구해줍니다.



#### \# aaa.py
(somefolder -\> test -\> aaa.py)
(Python(이론).pdf (P.125) 내용을 코드로 적음)

- 코드 내용
```python
# 방법 1: 모듈까지 import
import somefolder.mymath.MathModule

result = somefolder.mymath.MathModule.square_root(5)
print(result)

result = somefolder.mymath.MathModule.jegob(2, 3)
print(result)


# 방법 2: 함수 직접 import
# # 1) 함수 하나하나 import
# from somefolder.mymath.MathModule import square_root
# from somefolder.mymath.MathModule import jegob

# 2) 함수 한번에 import
from somefolder.mymath.MathModule import square_root, jegob

# # 3) 함수 전체 import
# from somefolder.mymath.MathModule import *

result = square_root(3)
print(result)

result = jegob(4, 5)
print(result)


# 방법 3 : 모듈을 별칭으로 import
import somefolder.mymath.MathModule as asdf

result = asdf.square_root(9)
print(result)
```



#### \# MathTest01.py (Python(실습).pdf (P. 106))
한번 보기
Java의 Math 클래스도 한번 공부해보기
