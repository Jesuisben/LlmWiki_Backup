# 1. 09:30 ~ 10:20
#### \# pandas 관련 (https://pandas.pydata.org/docs/reference/index.html)
- Series
1) 개념
1차원 자료구조
오라클 테이블의 한 개의 행(row) 또는 한 개의 열(column) 과 비슷한 개념  
여러 개의 Series가 모여 DataFrame을 구성함

2) 생성자와 속성들 공부
Constructor : 생성자
Attributes : 속성
Conversion : 형변환
Indexing : 인덱싱, 슬라이싱
iteration : 반복 가능 한 집합
등등


- DataFrame
여러 개의 Series가 모인 2차원 자료구조
오라클의 테이블(Table) 과 비슷한 개념
행(row)과 열(column)으로 데이터를 관리함



#### \# 쥬피터 노트북
한번에 실행하기보다는 코드가 같은 곳에 있어도 코드 실행 순서를 내가 정해서 내가 원하는 코드만 실행할 수 있는 장점

1) 실행
cmd -> myju

2) 웹페이지 접속
ch13_pandas -> New -> Python 3 (ipykernel)

3) 작성
```text
Ctrl + Enter  : 왼쪽 [] 공간에 숫자가 늘어나고 실행이 됨
Shift + Enter : 왼쪽 [] 공간에 숫자가 늘어나고 실행이 되고 다음 시리즈로 옮겨짐
변수를 실행하면 출력이 됨 (print 안써도 됨) (but, 쥬피터를 파이썬에 넣을때 print가 안붙으니까 이왕이면 붙이기)
(붙여도 실행됨)
```

4) 저장
File -> Save Notebook As... -> pandas_test.ipynb 이름변경 후 저장

5) 시리즈 변환
```text
최상위에 ( <h1><font color="blue">일차원 데이터 Series</font></h1> )를 넣고
좌측 상단에 Code 부분을 Markdown으로 바꾸고 해당 코드 실행시키면  해당 코드가 실행되면서 바뀜
```



# 2. 10:30 ~ 11:20
#### \# 판다스 - 시리즈 실습 (쥬피터 노트북 이용) (Python(실습).pdf (P.181~182))
#### \# Chap01.Series.ipynb 파일 내용들
##### 일차원 데이터 Series
###### Series의 생성과 속성 다루기
1\. 다양한 방식으로 Series 만들기
- Series 생성 기본 문형
```python
# pandas 임포트하고 Alias(별칭) 설정
import pandas as pd
```


- Series 만들기
```python
# range(5, 8) : 숫자의 범위를 기억하는 range 객체 / 아직 리스트처럼 숫자를 저장한게 아님
myrange = range(5, 8)
print(myrange)

# pandas의 Series 객체 생성
# pd.Series(data=None, index=None, dtype=None, name=None, copy=False) -> pandas.Series
# data가 iterable 객체이면 요소를 하나씩 순회(iterate)하여 Series의 value로 사용함
# 사전은 key를 index로 value를 value로 사용함
# 내부적으로 반복문(for)으로 하나씩 꺼내는 것과 동일하게 이해하면 됨
# 자동으로 인덱스도 생성됨 (기본값 : 0부터 시작)
myseries01 = pd.Series(myrange)
myseries01
```
(출력)
range(5, 8)
0 5
1 6
2 7
dtype: int64
->
왼쪽이 index (0, 1, 2)
오른쪽이 value (5, 6, 7)
dtype(Data Type)은 value들의 자료형


- list를 이용한 시리즈 생성
```python

```










- 
1) 판다스 가져오기
```python
import pandas as pd
```

2) 코드
```python
myrange = range(5, 8)
myseries01 = pd.Series(myrange)
myseries01
```

3) 출력
0    5
1    6
2    7
dtype: int64

3-1) 0, 1, 2 : 인덱스 번호
3-2) 5, 6, 7 : 값
3-3) dtype : int64 : 정수 데이터이다라는 표현


- 리스트로 코딩하기 (인덱스 기본값 : 숫자)
1) 코드
```python
mylist01 = [10, 60, 50, 30]
myseries01 = pd.Series(mylist01)
myseries01
```

2) 출력
0    10
1    60
2    50
3    30
dtype: int64


- 리스트로 코딩하기 (인덱스 따로 설정)
1) 코드
```python
mylist01 = [10, 60, 50, 30]
myseries01 = pd.Series(mylist01, index = ["프랑스", "독일", "중국", "미국"])
myseries01
```

2) 출력
프랑스    10
독일       60
중국       50
미국       30
dtype: int64


- 사전으로 코딩하기 (인덱스 따로 설정)
1) 코드
```python
sdata = {"일본" : 3000, "영국" : 2000}
myseries01 = pd.Series(sdata)
myseries01
```

2) 출력
일본    3000
영국    2000
dtype: int64


- 인덱스 이름 바꾸기
1) 코드
```python
myseries01.index.name = "국가"

myseries01
```

2) 출력
국가
일본    3000
영국    2000
dtype: int64


- 생성자의 name 속성 설정하기
1) 코드
```python
myseries01.index.name = "국가"

myseries01.name = "국가별 신용도"

myseries01
```


2) 출력
국가
일본    3000
영국    2000
Name: 국가별 신용도, dtype: int64


- 생성자의 속성 출력 (str로 바꿔야 함)
1) 코드
```python
print("색인 이름 : " + str(myseries01.index.name))
print("시리즈 이름 : " + str(myseries01.name))
```

2) 출력
색인 이름 : 국가
시리즈 이름 : 국가별 신용도


- 생성자의 속성 출력 (str로 바꿔야 함)
1) 코드
```python
"시리즈의 값 확인 : " + str(myseries01.values)

"시리즈의 값 확인 : [3000 2000]"

for idx in myseries01.index:
    print("색인 : " + idx + ", 값 : " + str(myseries01[idx]))
```

2) 출력
색인 : 일본, 값 : 3000
색인 : 영국, 값 : 2000


- to_frame 속성 이용 + 인덱스를 문자열로 설정하기
1) 코드
```python
myindex02 = ["프랑스", "독일", "중국", "미국", "일본", "영국", "호주"]
mylist02 = [50, 30, 20, 60, 40, 80, 70]
myseries02 = pd.Series(data = mylist02, index = myindex02)
myseries02.to_frame()
```

2) 출력 (표 형식으로 나옴)

|     |  0  |
| :-: | :-: |
| 프랑스 | 50  |
| 독일  | 30  |
| 중국  | 20  |
| 미국  | 60  |
| 일본  | 40  |
| 영국  | 80  |
| 호주  | 70  |


- 시리즈 객체에서 정보 가져오기 (스칼라 데이터 (정적 데이터) vs 시리즈 데이터)
1) 스칼라 데이터 (정적 데이터)
```python
myseries02["미국"]
```
-> 60

2) 시리즈 데이터
```python
myseries02[["미국"]]
```
->
미국    60
dtype: int64


- 인덱스 슬라이싱 하기
1) 코드
```python
myseries02["미국":"영국"]
```

2) 출력
미국    60
일본    40
영국    80
dtype: int64


- 인덱스 여러개 한번에 가져오기
1) 코드 (대괄호 2개 사용)
쉽게 생각하기 : 밖에 있는 대괄호는 인덱싱이나 슬라이싱이고
안쪽에 대괄호는 리스트를 만드는 대괄호라고 생각하면 편함
```python
myseries02[["호주", "미국"]]
```

2) 출력
호주    70
미국    60
dtype: int64



- iloc (숫자가지고 어떤 일을 할때) (loc는 문자가지고 어떤 일을 할때)
1) 코드
```python
myseries02.iloc[2]
```

2) 출력
20


- 슬라이싱하는 법
1) 코드
```python
myseries02[0:7:3]
```

2) 출력
프랑스    50
미국       60
호주       70
dtype: int64



- 시리즈의 값 변경하기 01
1) 코드
```python
myseries02.iloc[2] = 22
myseries02[2:5] = 55
myseries02[["프랑스", "미국"]] = 33
myseries02
```

2) 출력
프랑스    33
독일       30
중국       55
미국       33
일본       55
영국       80
호주       70
dtype: int64



- 시리즈의 값 변경하기 02
1) 코드
```python
myseries02[0::2] = 77

myseries02.to_frame()
```

2) 출력 (표 모양으로 나옴)

|     |  0  |
| :-: | :-: |
| 프랑스 | 77  |
| 독일  | 30  |
| 중국  | 77  |
| 미국  | 33  |
| 일본  | 77  |
| 영국  | 80  |
| 호주  | 77  |


- boolean indexing (참 거짓으로 인덱싱 하기)
시리즈의 값이 50이하인 것들만 result에 저장하기
1) 코드
```python
result = myseries02[myseries02 < 50]
result
```

2) 출력
독일    30
미국    33
dtype: int64



- 시리즈 전체의 값을 변경하기 01
1) 코드
```python
myseries31.to_frame()
```
->

|     |  0  |
| :-: | :-: |
| 일본  | 30  |
| 영국  | 40  |
| 호주  | 50  |
| 독일  | 60  |


2) 코드
```python
myseries31 + 10
```
->
일본    40
영국    50
호주    60
독일    70
dtype: int64


- 시리즈 전체의 값을 변경하기 02
1) 코드
```python
myseries31.to_frame()
```
->

|     |  0  |
| :-: | :-: |
| 일본  | 30  |
| 영국  | 40  |
| 호주  | 50  |
| 독일  | 60  |


2) 코드
```python
myseries31.add(10)
```
->
일본    40
영국    50
호주    60
독일    70
dtype: int64



- gt() 함수
```python
myseries31.gt(50) # myseries31 > 50
```
->
일본    False
영국    False
호주    False
독일     True
dtype: bool


- 시리즈끼리 연산
(원래 myseries31와 31은 int인데 데이터에 NaN(not a number) 타입이 있으면 무조건 float)
```python
myseries31 + myseries32
```
->
독일       NaN
영국      80.0
일본      50.0
프랑스      NaN
호주     110.0
dtype: float64



- 시리즈끼리 연산 (fill_value 설정)
```python
# fill_value = 0 : 서로의 없는 값은 0으로 설정함
myseries31.add(myseries32, fill_value = 0)
```
->
독일      60.0
영국      80.0
일본      50.0
프랑스     70.0
호주     110.0
dtype: float64



- unique() 함수
DB에서 abstract같은것

```python
myunique04 = myseries04.unique() # abstract 중복되어도 한개만 나오게 하기
myunique04
```
->
```python
array(['미국', '영국', '중국', '일본'], dtype=object)
```


- value_counts() 함수
\# 값을 기반으로 중복되는 것을 카운트(빈도숫자) (정렬 기본값 : 숫자크기로 정렬)
\# 매개변수에 normalize=True를 넣으면 숫자말고 비율로 나옴 (그래프에 넣을 숫자로 이용함)
```python
myseries04.value_counts(normalize=True)
```
->
영국    0.4
미국    0.3
일본    0.2
중국    0.1
Name: proportion, dtype: float64



- isin() 함수
```python
# is -> boolean 타입 / in -> ~ 안에
# isin() -> 값이 ~면 / 복수의 값을 충족하는 매개변수를 넣으려면 []를 사용
mask04 = myseries04.isin(["중국", "미국"])
myseries04[mask04]
```
->
0    미국
3    중국
8    미국
9    미국
dtype: object



# 3. 11:30 ~ 12:20
판다스 시리즈 정리



# 4. 12:30 ~ 13:20
판다스 시리즈 정리



# 5. 14:20 ~ 15:10
파이썬 시험



# 6. 15:20 ~ 16:10
파이썬 시험



# 7. 16:20 ~ 17:10
#### \# 판다스 - 시리즈 실습 (쥬피터 노트북 이용) (Python(실습).pdf (P.181~182))
#### \# Chap01.Series.ipynb 파일 내용들

- matplotlib의 plot() 함수로 꺽은 선 그래프 만들기
```python
myindex05 = ['프랑스', '독일', '중국', '미국', '일본', '영국', '호주']
mylist05 = [50, 30, 20, 60, 40, 80, 70]
myseries05 = pd.Series(data=mylist05, index=myindex05)
myseries05.to_frame()

import matplotlib.pyplot as plt

plt.rc("font", family="Malgun Gothic")

myseries05.plot(kind="line", grid=True, ylim=[0, 100], rot=10, use_index=False)
plt.xlabel("국가명")
plt.ylabel("신용도")
plt.title("국가별 신용도 꺽은 선")

# (꺽은 선 그래프 나옴)
```


- matplotlib의 plot() 함수로 바 그래프 만들기
```python
myseries05.plot(kind="bar", grid=False, ylim=[0, 100], rot=10, use_index=True, color=["r", "g", "b", "y"])
plt.xlabel("국가명")
plt.ylabel("신용도")
plt.title("국가별 신용도 꺽은 선")

# (바 그래프 나옴)
```



# 8. 17:20 ~ 18:10
#### \# 데이터 프레임(Data Frame) (Python(실습).pdf (P.183))
Chap02.DataFrame.ipynb 파일 참고

- 행 색인, 열 색인
데이터 프레임은 2차원이기때문에
행을 행 색인이라고 부르고
열을 열 색인이라고 부름

- 예시 이용
```python
myframe01 = pd.DataFrame(columns=mydata01, index=myindex01)
```
행 색인 (index)
열 색인 (columns)

- 접근시 (암기하기)
1) 행 색인 : myframe01.index
2) 열 색인 : myframe01.columns

- 값 접근시
행 색인과 열 색인에 해당하는 데이터의 값
```python
myframe01.values
```
->
```python
array([['남', 30, '의사'],
       ['남', 35, '판사'],
       ['여', 40, '변호사'],
       ['여', 45, '노무사']], dtype=object)
```

- 타입 이름
파이썬에서 str을 판다스에서는 object라고 불림

- 행 색인과 열 색인을 반전시키는 함수
데이터 프레임 전치하기
myframe01.T

- 행 색인, 열 색인 수정
새로운 프레임을 만들어서 수정함
```python
mycolumns01 = ['직업', '성별', '나이']
newframe = pd.DataFrame(myframe01, index=myindex01, columns=mycolumns01)
newframe
```

- numpy 이용해서 행 색인과 열 색인에 해당하는 데이터 만들기
```python
myframe02 = pd.DataFrame(np.reshape(mylist02, (5, 5)), \
            index=myindex02, columns = mycolumns02 )
myframe02
```