# 1. 09:30 ~ 10:20
#### \# HTTPS 관련 (cloud.02.AWS 교안(실습).pdf(215))
요즘에는 보안관련때문에 http보다 https를 더 사용하려는 경향이 있음

DNS (Domain Name Server) 이용하기



#### \# 도메인 확인
- 가비아 접속
https://www.gabia.com/

- 도메인 구입한 상태에서 확인하기
My가비아 - (구입한 도메인) 관리 - 네임서버에 설정으로 나중에 서버 관리할 수 있음



#### \# 도메인 설정 (cloud.02.AWS 교안(실습).pdf(151~181))
- 순서
도메인 신청 -> 호스팅 영역 생성 -> 네임서버 취득 -> 가비아에 네임 서버 연동
-> 인증서 생성 -> 도메인에 대한 CNAME 레코드 추가 -> 호스팅 영역 (CNAME 레코드 확인)
-> 인증서 (상태 컬럼에 '발급됨')


도메인 신청된 상태
호스팅 영역 안의 메인 서버와 도메인 서버를 연결해줘야 함

호스팅 영역 생성시 레코드가 생기는데 그 레코드의 값/트래픽 라우팅 대상이라는 서버가 나옴
이 값을 가비아의 도메인 서버와 연결해주면 됨

- 호스팅 영역 생성
AWS - Route 53 - Route 53 대시보드 (DNS 관리 - 호스팅 영역 생성)
도메인 이름 : 내가 가비아에서 생성한 도메인의 이름 (jesuisben.store)
-> 호스팅 영역 생성

- 레코드
도메인 이름과 ip주소 사이의 연결정보를 저장하고 있는 단위
DB에서 row(행)과 비슷한 개념?

1) NS (Name Server)


네임서버의 4개 값/트래픽 라우팅 대상을 가비아의 도메인의 네임서버에 입력함
->


2) SOA


메타 데이터를 가지고 있는 것

- 가비아에 네임 서버 연동
가비아의 도메인 관리에 네임서버 설정에 AWS 호스팅 영역의 레코드의 NS인 4개의 네임서버 주소를 입력하기
1차 : 
2차 : 
3차 : 
4차 : 
-> 소유자 인증 후 적용


- 인증서 설정
(AWS -  AWS Certificate Manager)
1) 인증서 요청 (다음)


2) 도메인 이름 : jesuisben.store


3) 이 인증서에 다른 이름 추가 (도메인) : *.jesuisben.store (www같은거 도메인 앞에 붙어도 인증서로 보호하겠다는 의미)


-> 요청


- 도메인에 대한 CNAME 레코드 추가
(AWS -  AWS Certificate Manager)
Route 53에서 레코드 생성 - 레코드 생성

- 호스팅 영역 (CNAME 레코드 확인)
(AWS - Route 53)
인증성 레코드 하나 추가 된 것 확인하기

- 인증서 (상태 컬럼에 '발급됨')
(AWS -  AWS Certificate Manager)
인증서 나열 - 인증서가 상태가 '발급됨'인 것 확인



# 2. 10:30 ~ 11:20
#### \# 로드 밸런서 (cloud.02.AWS 교안(실습).pdf(1~80))
- 준비
2대 EC2 인스턴스 만들고 MobaXterm으로 접속하게 하기



#### \# 부하 분산 서비스 (cloud.03.AWS 교안(이론).pdf(P.185~188))
Waiting List
EC2를 상담자라고 생각하고 EC2를 많이 만들면 부하가 분산되고
혹시라도 EC2 하나가 문제가 생겨도 다른 EC2가 해결해줌



#### \# MobaXterm에서 nginx 설정
sudo apt install -y nginx

git clone https://github.com/seoljinuk/ec2_homepage.git

cd ec2_homepage/Public-2A/ # `Public-2C` 인스턴스에서는 `Public-2C`라고 입력합니다.

sudo cp -r ./* /var/www/html/

sudo systemctl start nginx

Public-2A : 15.164.36.134
Public-2C : 3.34.20.1



# 3. 11:30 ~ 12:20
#### \# 로드 밸런서 (cloud.02.AWS 교안(실습).pdf(P.183~215))
\* 인증서를 만들지 않으면 로드 밸런서 만드는 과정에서 다음! 으로 넘어가지지가 않음
대규모의 데이터나 작업을 여러 컴퓨터나 서버에 분산하여 처리하는 방식
(AWS - EC2 - 로드 밸런싱)
- 순서
대상 그룹 -> 보안 그룹 -> 로드 밸런서 생성

- 사용 항목
분류						이름					설명
Secutiry Group	EDU-CLB-SG	Classic Load Balancer Name(80)
Load Balancer		EDU-CLB			Classic Load Balancer
Target Group		EDU-ALB-TG	Target Group for Application Load Balancer
Secutiry Group	EDU-ALB-SG	Application Load Balancer Security Group Name(80, 443)
Load Balancer		EDU-ALB			Application Load Balancer Name


- 대상 그룹 (Target Group)
(AWS - EC2 - 로드 밸런싱 - 대상 그룹)
관리하고 싶은 목록 : 대상
그 목록들을 묶어놓은 것 : 대상 그룹

1) 대상 그룹 생성


대상 유형 : 인스턴스
대상 그룹 이름 : EDU-ALB-TG
VPC : EDU-VPC
-> 다음
사용 가능한 인스턴스 : EDU-PUBLIC-EC2-2C / EDU-PUBLIC-EC2-2A 체크 - 아래에 보류 중인 것으로 포함
-> 다음
-> 대상 그룹 생성


- 보안 그룹
(AWS - VPC - 보안 그룹)
이름 : EDU-ALB-SG
-> 생성

인바운드 규칙 추가 : http와 https만 추가하기

- 로드 밸런서 생성 (일종의 방화벽이 있어서 보안 그룹을 따로 설정하는 것)
(AWS - EC2 - 로드 밸런싱 - 로드밸런서)
로드 밸런서 생성 - Application Load Balancer - 생성
로드 밸런서 이름 : EDU-ALB
VPC : EDU-VPC
가용 영역 및 서브넷 : ap-northeast-2a (apne2-az1) / ap-northeast-2c (apne2-az3) 체크 + 서브넷도 설정
\* 하나의 가용 영역에는 서브넷이 여러 개 들어 갈 수 있음
보안 그룹 : EDU-ALB-SG / default 보안그룹은 삭제
대상 그룹 : EDU-ALB-TG

리스너 추가
프로토콜 : HTTPS
대상 그룹 : EDU-ALB-TG

기본 SSL/TLS 서버 인증서
인증서 소스 : ACM에서 (ACM은 AWS Certificate Manager을 의미)
인증서(ACM에서) : jesuisben.store (내가 만든 인증서)
-> 로드 밸런서 생성


- A 레코드 생성 (2개 생성)
(AWS - Route 53)
호스팅 영역 - 내가 생성한 (도메인) 호스팅 영역 선택 - 레코드 생성

1) 기본 A 레코드 생성


레코드 이름 : 비워두기
별칭 체크
트래픽 라우팅 대상
-> 엔드포인트 선택 : Application/Classic Load Balancer에 대한 별칭
-> 리전 선택 : 아시아 태평양(서울)
-> 로드 밸런서 선택 : EDU-ALB
레코드 생성

2) www. A 레코드 생성


레코드 이름 : www
별칭 체크
트래픽 라우팅 대상
-> 엔드포인트 선택 : Application/Classic Load Balancer에 대한 별칭
-> 리전 선택 : 아시아 태평양(서울)
-> 로드 밸런서 선택 : EDU-ALB
레코드 생성

- 주소창에 입력해서 직접 확인해보기
http://jesuisben.store/
http://www.jesuisben.store/
https://jesuisben.store/
https://www.jesuisben.store/



#### \# 오늘 한 것들 모두 삭제하는 법 (cloud.02.AWS 교안(실습).pdf(P.216~244))
- 순서
탄력적 IP -> EC2 인스턴스 -> 로드 밸런서 -> 대상 그룹
-> (RDS) -> EC2 보안 그룹 -> (RDS 보안 그룹) -> VPC
-> (DHCP 옵션 셋트) -> 인증서 -> 레코드 -> 호스팅 영역



# 4. 12:30 ~ 13:20
이어서 작성



# 5. 14:20 ~ 15:10
#### \# 실습해보기



# 6. 15:20 ~ 16:10
자유시간



# 7. 16:20 ~ 17:10
자유시간



# 8. 17:20 ~ 18:10
자유시간
