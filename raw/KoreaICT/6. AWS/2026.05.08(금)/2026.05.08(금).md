# 1. 09:30 ~ 10:20
#### \# VPC와 서브넷 2개 만들어서 MobaXterm으로 세션 연결까지 하기
(실습 관리 대장(텍스트).txt 이용)

1) Public2A


52.78.59.174
http://52.78.59.174

2) Public2C


43.200.241.82
http://43.200.241.82



# 2. 10:30 ~ 11:20
#### \# ping 테스트 (cloud.02.AWS 교안(실습).pdf(P.82~84))
(MobaXterm)
Public2A에서 Public2C로의 ping 테스트
1. 실습 (하지만 패킷 손실나서 실패)
1)


(Public2A)

```bash
ping -c 5 상대ip주소 (Public2C 주소)
ping -c 5 43.200.241.82
```
Public2A가 Public2C에게 서버가 살아있는지 확인하기 위해 5번 리턴해줘라
->

```text
PING 43.200.241.82 (43.200.241.82) 56(84) bytes of data.
```

```text
--- 43.200.241.82 ping statistics ---
5 packets transmitted, 0 received, 100% packet loss, time 4133ms
```
-> 5개 다 패킷 손실나서 확인 불가

2)


(Public2C)

```bash
ping -c 5 상대ip주소 (Public2A 주소)
ping -c 5 52.78.59.174
```
Public2C가 Public2A에게 서버가 살아있는지 확인하기 위해 5번 리턴해줘라
->

```text
PING 52.78.59.174 (52.78.59.174) 56(84) bytes of data.
```

```text
--- 52.78.59.174 ping statistics ---
5 packets transmitted, 0 received, 100% packet loss, time 4119ms
```
-> 5개 다 패킷 손실나서 확인 불가


2. 패킷 해결하기 (보안그룹 인바운드 규칙 추가)
(AWS - VPC - 보안 그룹 - 인바운드 규칙 편집)
- 인바운드 규칙 추가
모든 ICMP - IPv4	 /	 0.0.0.0/0


3. 다시 핑 테스트하기
1)


(Public2A)

```bash
ping -c 5 43.200.241.82
```
->

```text
PING 43.200.241.82 (43.200.241.82) 56(84) bytes of data.
64 bytes from 43.200.241.82: icmp_seq=1 ttl=63 time=1.07 ms
64 bytes from 43.200.241.82: icmp_seq=2 ttl=63 time=1.02 ms
64 bytes from 43.200.241.82: icmp_seq=3 ttl=63 time=1.03 ms
64 bytes from 43.200.241.82: icmp_seq=4 ttl=63 time=1.02 ms
64 bytes from 43.200.241.82: icmp_seq=5 ttl=63 time=1.01 ms
```

```text
--- 43.200.241.82 ping statistics ---
5 packets transmitted, 5 received, 0% packet loss, time 4005ms
rtt min/avg/max/mdev = 1.006/1.030/1.074/0.023 ms
```


2)


(Public2C)

```bash
ping -c 5 52.78.59.174
```
->

```text
PING 52.78.59.174 (52.78.59.174) 56(84) bytes of data.
64 bytes from 52.78.59.174: icmp_seq=1 ttl=63 time=1.07 ms
64 bytes from 52.78.59.174: icmp_seq=2 ttl=63 time=1.08 ms
64 bytes from 52.78.59.174: icmp_seq=3 ttl=63 time=1.08 ms
64 bytes from 52.78.59.174: icmp_seq=4 ttl=63 time=1.08 ms
64 bytes from 52.78.59.174: icmp_seq=5 ttl=63 time=1.06 ms
```

```text
--- 52.78.59.174 ping statistics ---
5 packets transmitted, 5 received, 0% packet loss, time 4005ms
rtt min/avg/max/mdev = 1.061/1.072/1.082/0.007 ms
```



#### \# nginx 패키지 설치 (cloud.02.AWS 교안(실습).pdf(P.82~86))
1. Public2A 인스턴스에 sudo 명령어를 사용하여 nginx 패키지를 설치

```bash
sudo apt install -y nginx
sudo systemctl start nginx
sudo systemctl status nginx
```

2. Public2C 인스턴스에 sudo 명령어를 사용하여 nginx 패키지를 설치

```bash
sudo apt install -y nginx
sudo systemctl start nginx
sudo systemctl status nginx
```

3. Public2A와 Public2C 인스턴스가 잘 굴러가는지 웹페이지로 확인하기

1) http://52.78.59.174/


2) http://43.200.241.82/



#### \# nginx 홈페이지 변경 (cloud.02.AWS 교안(실습).pdf(P.87~88))
1. Public2A

```bash
git clone https://github.com/seoljinuk/ec2_homepage.git
```

```bash
ls # 목록 확인
```

```bash
cd ec2_homepage/Public-2A/ # `Public-2C` 인스턴스에서는 `Public-2C`라고 입력합니다.
```

```bash
ls
sudo cp -r ./* /var/www/html/
```

```bash
sudo systemctl restart nginx
```

2. Public2C

```bash
git clone https://github.com/seoljinuk/ec2_homepage.git
```

```bash
ls # 목록 확인
```

```bash
cd ec2_homepage/Public-2C/ # `Public-2C` 인스턴스에서는 `Public-2C`라고 입력합니다.
```

```bash
ls
sudo cp -r ./* /var/www/html/
```

```bash
sudo systemctl restart nginx
```



# 3. 11:30 ~ 12:20
#### \# 스프링 부트 연동 (cloud.02.AWS 교안(실습).pdf(P.89~94))
1. (Public2A)

```bash
sudo systemctl stop nginx
```

```bash
cd
```

```bash
sudo apt update # 관련 패키지 업데이트
```

```bash
sudo apt install -y openjdk-17-jdk # 다소 시간이 걸릴 수 있습니다.
sudo apt install -y maven (스프링 부트에서 코딩했을 시점의 JDK 버전과 동일한 버전을 설치)
```

```bash
java -version
```

```bash
mvn -v
```

```bash
git clone https://github.com/seoljinuk/shopping_01_no_database.git
```

```bash
cd shopping_01_no_database/
```

```bash
mvn clean package -DskipTests
```

```bash
cd target/
```

// 포트 포워딩(포트 리디렉션) (이제 9000 포트 넘버 입력하면 홈페이지 뜸)

```bash
sudo iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 9000
```

```bash
java -jar shopping-0.0.1-SNAPSHOT.jar (스프링 서버 구동)
```

웹사이트 확인하기 : http://52.78.59.174


2. (Public2C)

```bash
sudo systemctl stop nginx
```

```bash
cd
```

```bash
sudo apt update # 관련 패키지 업데이트
```

```bash
sudo apt install -y openjdk-17-jdk # 다소 시간이 걸릴 수 있습니다.
sudo apt install -y maven (스프링 부트에서 코딩했을 시점의 JDK 버전과 동일한 버전을 설치)
```

```bash
java -version
```

```bash
mvn -v
```

```bash
git clone https://github.com/seoljinuk/shopping_01_no_database.git
```

```bash
cd shopping_01_no_database/
```

```bash
mvn clean package -DskipTests
```

```bash
cd target/
```

// 포트 포워딩(포트 리디렉션) (이제 9000 포트 넘버 입력하면 홈페이지 뜸)

```bash
sudo iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 9000
```

```bash
java -jar shopping-0.0.1-SNAPSHOT.jar
```

웹사이트 확인하기 : http://43.200.241.82



#### \# 스프링 부트 연동 - Public-2C에서 html 문서 수정 (cloud.02.AWS 교안(실습).pdf(P.95))
- "Public-2A"와 "Public-2C" 페이지를 구분하기 위하여 html 문서를 수정

1. productList.html 문서 내에서 "상품 목록"을 "Product List"로 변경

```bash
sudo vi /home/ubuntu/shopping_01_no_database/src/main/resources/templates/productList.html
```
html 문서 내에서 "상품 목록"을 "Product List"로 변경 후 :wq

2. 다시 패키지 만들고 패키지 실행 (애플리케이션 실행) 하면 됨

```bash
cd shopping_01_no_database/
```

```bash
mvn clean package -DskipTests (clean 때문에 target 디렉토리를 알아서 지우고 다시 생성함)
```

```bash
cd target/
```

(포트 포워딩은 이미 되어 있음)

```bash
java -jar shopping-0.0.1-SNAPSHOT.jar (마치 인텔리제이에서 스프링 애플리케이션 실행하는 것과 같음)
```

3. 웹페이지 확인하기
http://43.200.241.82



# 4. 12:30 ~ 13:20
#### \# RDS와 Spring Boot 연동 (cloud.02.AWS 교안(실습).pdf(P.96~124))
(교안과 바뀐 내용이 많아서 교안은 참고용으로 사용해야 함)
(참고로 EC2에 직접 DB를 설치 할 수도 있음)
- RDS 설치
(application.properties 파일과 관련된 설정)
DB 인스턴스 식별자 정보   dbmysql80
마스터 사용자 이름 정보   myroot
마스터 암호 정보   My#sql$80
데이터 베이스 이름   shopping

- 설정하기
(AWS - Aurora and RDS)
\* Aurora는 AWS에서 만든 DB
1. 데이터베이스 - 데이터베이스 생성 - 전체구성
엔진 옵션 : MySQL
데이터베이스 생성 방식 선택 : 전체 구성
템플릿 : 프리 티어
가용성 및 내구성 : 단일 AZ DB 인스턴스 배포(인스턴스 1개)
DB 인스턴스 식별자 : dbmysql80
마스터 사용자 이름 : myroot
마스터 암호 : My#sql$80

퍼블릭 액세스 : 예 (자바의 접근지정자를 public으로 바꾼 느낌)

기본 VPC 보안 그룹 : 생성한 2개의 보안 그룹 선택하기
가용 영역 : ap-northeast-2a

추가 구성 (맨 아래에 있는) - 초기 데이터베이스 이름 : shopping
-> 생성하는데 시간이 좀 오래 걸리니까 커피 한 잔 하고 오자~
상태가 사용가능으로 바뀌어야 다 생성된거임

- 생성 후
생성한 데이터베이스 클릭 -> 연결 및 보안 -> 다음을 사용하여 연결 : 엔드포인트
추가 구성 -> 연결 및 보안 -> 엔트포인트 및 포트 -> 엔드포인트 (이 코드를 복사해서 따로 가지고 있기)
dbmysql80.czose4m0yblo.ap-northeast-2.rds.amazonaws.com


- 생성한 데이터베이스와 EC2 연결하기

1) 데이터베이스 - 생성한 데이터베이스 클릭 - 작업 - EC2 연결 설정 - EC2 인스턴스 : ap-northeast-2a - 계속 - 설정


2) 데이터베이스 - 생성한 데이터베이스 클릭 - 작업 - EC2 연결 설정 - EC2 인스턴스 : ap-northeast-2c - 계속 - 설정


\* 연결하면 자동으로 보안그룹이 생성됨

- 자동 생성된 보안 그룹 확인하기
(VPC - 보안 그룹)
rds-ce2-1과 ec2-rds-1라는 이름의 보안 그룹 생성 확인



# 5. 14:20 ~ 15:10
#### \# MySQL 클라이언트 설치/접속 (cloud.02.AWS 교안(실습).pdf(P.125~129))
- Public-2A EC2 인스턴스에서 AWS RDS(MySQL)에 CLI를 통해 접속 테스트하려면 mysql 클라이언트를 사용하면 됨
(보안 그룹 인바운드 규칙 설정을 하지 않으면 안되니 반드시 확인하시길 바랍니다)

```bash
sudo apt update
```

```bash
sudo apt install mysql-client -y
```

```bash
mysql -h dbmysql80.czose4m0yblo.ap-northeast-2.rds.amazonaws.com -P 3306 -u myroot -p (중간에 .com은 생성한 데이터베이스의 엔드포인트)
```
Enter password: My#sql$80 # 비밀 번호 항목은 화면에 보이지 않습니다.
… 중략

```text
mysql>
```


- SQL 클라이언트 사용 / 데이터베이스 접속 /  사용할 테이블 생성 / 생플용 데이터 추가
show databases;

use shopping ;
show tables ;

CREATE TABLE products(
id BIGINT PRIMARY KEY,
name VARCHAR(100),
price INT,
description TEXT,
image_url VARCHAR(255)
);

INSERT INTO products(id, name, price, description, image_url) VALUES
(1, '카푸치노', 4500, '맛있는 카푸치노', '/images/cappuccino01.png'),
(2, '크로아상', 5000, '빵은 역시 크로아상^^', '/images/croissant_01.png'),
(3, '우유', 3000, '따듯한 우유 조아요ㅎㅎ', '/images/milk01.jpg');

commit;

select * from products ;

exit ;


- 깃에서 자료 받고, 설정 변경
(Public-2A EC2 인스턴스)

```bash
git clone https://github.com/seoljinuk/shopping_02_yes_database.git
```

```bash
cd shopping_02_yes_database/src/main/resources/
```

// 내용 수정 예정

```bash
vi application.properties
```
-> 수정 내용

1) spring.datasource.url의 값에서 localhost라는 값을 대신해서 데이터베이스의 엔드포인트인


dbmysql80.czose4m0yblo.ap-northeast-2.rds.amazonaws.com 로 수정

2) spring.datasource.username의 값을 myroot로 수정


3) spring.datasource.password의 값을 My#sql$80으로 수정


```bash
cd
cd shopping_02_yes_database/
mvn clean package -DskipTests
```

```bash
cd target/
sudo iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 9000 (이미 포트 포워딩(포트 리디렉션)을 해서 다시 할 필요는 없음)
java -jar shopping-0.0.1-SNAPSHOT.jar
```

\* 이 2개의 세션(서브넷과 연결된)은 한개의 데이터 베이스를 공유함!! (중요!!)



# 6. 15:20 ~ 16:10
#### \# 도메인 등록하기 (cloud.02.AWS 교안(실습).pdf(P.134~))
(가비아 사용 예정)



#### \# 삭제하기 (cloud.02.AWS 교안(실습).pdf(P.248~))
- 순서
탄력적 IP -> EC2 -> 데이터베이스(체크박스 해제 후 나오는 체크박스만 체크 후 삭제)
-> 보안 그룹(ec2-rde-1의 아웃바운드 규칙 삭제)(rde-ec2-1의 인바운드 규칙 삭제)(default빼고 다 삭제) -> VPC 삭제



# 7. 16:20 ~ 17:10



# 8. 17:20 ~ 18:10
