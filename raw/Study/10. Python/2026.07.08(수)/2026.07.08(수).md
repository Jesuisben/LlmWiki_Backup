# 1. 09:30 ~ 10:20
#### \# 크롤링 예외사항
2026.07.06(월)부터 했던 크롤링 중에서
"2\. 한국 영화 진흥원(KMDB) 데이터 가져오기 (P.245~266)"
이 부분은 어떻게 하는지만 간단하게 소개 후 넘어감

새로 받은 "새 폴더01" 압축파일 압축해제 후 해당 경로에 맞는 폴더 덮어쓰기함

https://www.kobis.or.kr/kobisopenapi/homepg/main/main.do
접속해서 키발급받고
.env 파일에 환경변수 넣고 (변수명은 KMDB_API_KEY)
아래 파일들 하나씩 실행

- 관련파일
kmdb11.get_movie_list_to_csv.py
kmdb12.get_movie_detail_list.py
kmdb13.get_movie_people_list.py
kmdb14.get_movie_people_detail.py



# 2. 10:30 ~ 11:20
#### \# 텍스트 마이닝 (Python(실습).pdf (P.268~282))

- speechTextMining01.py 일부 내용
```python
os.environ[javahome] = r'C:\Users\ICT02-006\.jdks\ms-21.0.10\bin\server'
```
위 문자열을 자신의 Java 설치 경로로 수정 후 실행.
KoNLPy가 내부적으로 Java를 사용하므로, Java 실행 환경(JVM)을 찾을 수 있도록 JAVA_HOME 환경 변수를 지정해야 함.
JVM의 핵심 라이브러리인 jvm.dll을 이 경로를 통해 참조하게 됨.


- 설치할 라이브러리
1) nltk
자연어 처리 툴킷(National Language ToolKit)
영어 중심의 자연어 처리(NLP) 라이브러리

2) konlpy
한글 형태소 분석 라이브러리
형태소(Morpheme)는 의미를 가지는 가장 작은 언어 단위

jvm.dll (자바 동적 파일)을 참조하는 라이브러리
-> 내부적으로 Java(JVM)를 사용하므로 Java가 설치되어 있어야 하며 JVM(jvm.dll)을 참조함.

https://konlpy.org/ko/latest/index.html
접속 후 좌측에 API 클릭
tag Package 하위 5개 패키지(Class)가 한글을 처리하는 패키지


- speechTextMining01.py 코드 내용
```python
'''  
관련 파일  
    윤석열 제20대 대통령 취임사.txt  
    user_dic.txt    stopword.txt    alice_color.png  
설치할 라이브러리  
    pip install konlpy <- 한글 형태소 분석 라이브러리  
    pip install nltk <- 자연어 처리 툴킷(National Language ToolKit)  
    pip install wordcloud <- 워드 클라우드  
'''  
import os  
javahome = 'JAVA_HOME'  
# os.environ[javahome] = r'C:\Program Files\Java\jdk-12.0.1\bin\server'  
# os.environ[javahome] = r'C:\Program Files\Java\jdk-12.0.2\bin\server'  
os.environ[javahome] = r'C:\Users\ICT02-006\.jdks\ms-21.0.10\bin\server'  
  
print(javahome in os.environ)  
  
dataIn, dataOut = '../dataIn/', '../dataOut/'  
filename = dataIn + '윤석열 제20대 대통령 취임사.txt'  
  
speech = open(filename, mode='r', encoding='UTF-8').read()  
# print(speech)  
  
# 사용자 정의 단어들을 저장해 놓은 사전 파일을 읽습니다.  
# 사용자가 최소 형태소를 커스트 마이징 하려면, 이 파일에 기록해 두세요.  
user_dict = dataIn + 'user_dic.txt' # 사용자 단어 정의 사전  
  
from konlpy.tag import Komoran  
  
# userdic으로 값을 넣으면 원래 konlpy가 형태소 분석을 할때  
# "챗 / 지피티"로 원래 나눈다고 할때 userdic에 들어간 파일에 "챗지피티 NNP" 라고 적어두면
# "챗지피티"를 하나의 형태소로 인식함 -> 한마디로 커스텀이 가능함  
komo = Komoran(userdic=user_dict) # 형태소 분석 객체 생성  
  
# nouns() 함수는 명사를 추출해 줍니다.  
token_list = komo.nouns(speech)  
  
# print('토큰 목록')  
# print(token_list)  
  
# 불용어(stopword)는 빈도 수는 많지만, 분석시 크게 의미를 부여하지 않아도 되는 단어들을 의미합니다.  
stopword_file = dataIn + 'stopword.txt' # 불용어 파일  
stop_file = open(stopword_file, mode='r', encoding='UTF-8').readlines()  
stop_words = [word.strip() for word in stop_file]  
  
# print('불용어 목록')  
# print(stop_words)  
  
new_token_list = [word for word in token_list if word not in stop_words]  
print('불용어를 제외한 토큰 목록')  
# print(new_token_list)  
  
# 집합을 이용하여 불용어 처리가 잘 되었는 지 확인합니다.  
set_old_token = set(token_list)  
set_new_token = set(new_token_list)  
difference = set_old_token.difference(set_new_token)  
  
# print('불용어로 처리된 단어 목록 확인')  
# print(difference)  
  
# nltk : National Language ToolKit  
import nltk  
  
# 단어의 빈도 수를 내림차순 정렬하여 상위 bindo_size개만 추출합니다.  
nltk_token = nltk.Text(tokens=new_token_list)  
bindo_size = 500 # 빈도 수  
token_data = nltk_token.vocab().most_common(bindo_size)  
  
# print('토큰과 빈도 수 확인')  
# print(token_data)  
  
# 해당 목록을 csv 파일로 저장합니다.  
import pandas as pd  
filename = dataOut + 'word_list.csv'  
dataFrame = pd.DataFrame(token_data, columns=['단어', '빈도수'])  
dataFrame.to_csv(filename, encoding='UTF-8', index=False)  
print(filename + ' 파일 저장 완료')  
  
# 튜플(단어, 빈도수)을 저장하고 있는 리스트  
wordList = list()  
  
# 단어의 길이가 2이상이고, 빈도 수가 2이상인 항목만 추출하기  
for word, bindo in token_data:  
    if len(word) >= 2 and bindo >= 2:  
        wordList.append((word, bindo))  
  
print(wordList)  
  
myFrame = pd.DataFrame(wordList, columns=['단어', '빈도수'])  
print(myFrame.head())  
  
print('상위 top 10개 막대 그래프')  
barCount = 10 # 그리고자 하는 막대 개수  
chartData = myFrame.set_index('단어').iloc[0:barCount]  
  
import matplotlib.pyplot as plt  
plt.rc('font', family='Malgun Gothic')  
  
chartData.plot(kind='bar', rot=10, grid=True, legend=False)  
  
plt.title('빈도 ' + str(barCount) + '개 상위 단어', size=20)  
plt.xlabel('주요 키워드', size=12)  
plt.ylabel('빈도 수', size=12)  
  
barFileName = dataOut + 'bar_chart.png'  
plt.savefig(barFileName)  
print(barFileName + ' 파일 저장됨')  
# plt.show()  
  
# 참조 url : https://amueller.github.io/word_cloud/index.htmlprint('단어 빈도를 이용한 워드 클라우드')  
  
import numpy as np  
  
# opencv 라이브러리도 같이 공부할 것  
from PIL import Image # Python Image Library  
  
# 워드 클라우드가 그려질 이미지  
alice_color_file = dataIn + 'alice_color.png'  
alice_color_array = np.array(Image.open(alice_color_file)) # 이미지 배열  
  
# print(alice_color_array)  
  
wordDict = dict(wordList) # 단어 사전  
  
from wordcloud import WordCloud  
  
fontName = 'malgun.ttf' # 글꼴  
  
# myCloud : 워드 클라우드 객체  
myCloud = WordCloud(font_path=fontName, mask=alice_color_array, background_color='white')  
  
# 사전 정보(wordDict)로부터 빈도 수(frequencies)를 생성(generate)합니다.  
myCloud = myCloud.generate_from_frequencies(wordDict)  
  
# ImageColorGenerator : 컬러 이미지의 색상 톤을 유지하면서 워드 클라우드를 사용하고자 할때 사용합니다.  
from wordcloud import ImageColorGenerator  
  
colorGenerator = ImageColorGenerator(alice_color_array)  
  
myCloud = myCloud.recolor(color_func=colorGenerator)  
  
plt.figure(figsize=(16, 8)) # 큰 새로운 도화지 준비  
plt.axis('off') # 그래프 테두리 없애기  
plt.imshow(myCloud)  
  
cloudFileName = dataOut + 'word_cloud.png'  
plt.savefig(cloudFileName)  
print(cloudFileName + ' 파일 저장됨')
```



- WordCloud 소개
단어 빈도를 이용한 워드 클라우드
https://amueller.github.io/word_cloud/


- speechTextMining02.py 일부 내용
```python
os.environ[javahome] = r'C:\Users\ICT02-006\.jdks\ms-21.0.10\bin\server'
```
위 문자열을 자신의 Java 설치 경로로 수정



# 3. 11:30 ~ 12:20
#### \# 머신 러닝 개요 (머신 러닝 교안(이론).pdf (P.1~9))

- AI, 머신 러닝, 딥러닝 관계
딥러닝 ⊂ 머신 러닝 ⊂ 인공 지능

- 머신 러닝
컴퓨터가 주어진 데이터를 이용하여 스스로 규칙/패턴을 찾는 기술

- 딥러닝
사람의 뇌 구조를 모방한 신경망(Neural Network)을 이용하는 머신 러닝 기술
ex) 얼굴 인식, 자율 주행, 질병 진단, OCR 문자 인식

- 모델 : 머신러닝에서 학습된 최종 결과물

-  용어 비교

|    구분    | 인공 지능(AI)                | 머신 러닝(ML)                       | 딥러닝(DL)                             |
| :------: | ------------------------ | ------------------------------- | ----------------------------------- |
|    정의    | 인간의 지능을 모방하는 컴퓨터 시스템     | 데이터에서 학습하여 패턴을 찾는 알고리즘          | 다층 신경망을 이용하여 학습하는 머신 러닝의 하위 분야      |
|  주요 개념   | 추론, 계획, 문제 해결            | 지도 학습, 비지도 학습                   | 신경망(NN), 합성곱 신경망(CNN), 순환 신경망 (RNN) |
|  예제 기술   | 전문가 시스템, 퍼지 로직, 검색 알고리즘  | 회귀, 의사결정 트리, 랜덤 포레스트, SVM       | CNN, RNN, GAN, 트랜스포머                |
| 데이터 필요성  | 적을 수도 있음                 | 비교적 많은 데이터 필요                   | 매우 큰 데이터셋 필요(빅데이터)                  |
| 특징 엔지니어링 | 논리 규칙에 따라 수동 설계 가능       | 데이터에서 특징을 추출하여 모델을 학습           | 모델이 직접 특징을 학습                       |
|  계산 비용   | 상대적으로 낮음                 | 중간 정도                           | 매우 높음(GPU/TPU 필요)                   |
|  응용 분야   | 챗봇, 로봇, 자율주행, 게임 AI      | 추천 시스템, 자연어 처리(NLP), 금융 예측      | 이미지 인식, 음성 인식, 자율주행, 생성 AI          |
| 대표 프레임워크 | Prolog, LISP, OpenAI Gym | Scikit-learn, XGBoost, LightGBM | TensorFlow, PyTorch, Keras          |



#### \# 머신 러닝의 2가지 분류 (머신 러닝 교안(이론).pdf (P.10~23))
##### 1\. 학습 방법에 따른 분류
- 학습이란
데이터 기반, 특정 작업(분류, 회귀 등등)을 수행할 수 있도록 패턴이나 규칙을 스스로 찾아내는 과정을 의미

###### 1) 지도 학습
- 개념
정답을 알려주면서 공부시키는 방법
독립변수(학습자료)(Input)와 종속변수(정답)(Label)를 동시에 알려주고 학습시킴
ex) 
선형 회귀(Linear Regression)
로지스틱 회귀(Logistic Regression)
서포트 벡터 머신(Support Vector Machines, SVM)
의사 결정 나무(Decision Trees)
랜덤 포레스트(Random Forest)
신경망(Neural Networks)

- 목표
예측 문제
정답이 있는 데이터를 사용하여 학습하고, 새로운 데이터에 대해 예측하는 것


###### 2) 비지도 학습
- 개념
정답 없이 스스로 규칙을 찾게 하는 방법
독립변수(학습자료)(Input)를 알려주고, 종속변수(정답)(Label)는 알려주지 않고 학습시킴
ex) 
K-평균 군집화(K-Means Clustering)
계층적 군집화(Hierarchical Clustering)
주성분 분석(Principal Component Analysis, PCA)
독립 성분 분석(Independent Component Analysis, ICA)
자기 조직화 지도(Self-Organizing Maps, SOM)

- 목표
데이터 탐색과 분석
정답이 없는 데이터를 사용하여 데이터의 패턴이나 구조를 발견하는 것


###### 3) 강화 학습
- 개념
행동의 결과로 얻는 보상(Reward)을 바탕으로 스스로 최적의 행동을 학습하는 방법

- 목표
환경과의 상호 작용을 통한 최대화된 보상 받기


##### 2\. 문제의 유형에 따른 분류
- 데이터 분류

| 구분               | 정성/정량 관계              | 설명                  | 예시              |
| ---------------- | --------------------- | ------------------- | --------------- |
| 범주형(Categorical) | 정성적 데이터(qualitative)  | 수치보다는 구분(label)이 중요 | 성별, 지역, 직업, 혈액형 |
| 연속형(Continuous)  | 정량적 데이터(quantitative) | 수치로 연속적인 값 표현       | 키, 몸무게, 점수, 매출액 |


###### 1) 회귀(Regression)
- 목표
숫자를 예측
ex)
집값 예측
매출 예측
온도 예측
->
결과 : 집값 = 5억 3천만원

- 학습 방법에 따른 분류
지도 학습

###### 2) 분류(Classification)
- 목표
범주를 예측
ex)
스팸 메일 여부
남성/여성
암 여부
->
결과 : 스팸

- 학습 방법에 따른 분류
지도 학습

###### 3) 군집(Clustering)
- 목표
비슷한 것끼리 그룹화
ex)
고객 세분화
상품 추천
이상치 탐지
->
결과 : 그룹 A / 그룹 B / 그룹 C

- 학습 방법에 따른 분류
비지도 학습



##### 3\. 분류 방식
###### 머신 러닝
1) 지도 학습
	1-1) 회귀 분석 -> 숫자 예측
	1-2) 분류 분석 -> 범주 예측

2) 비지도 학습
	2-1) 군집 분석 -> 그룹 찾기
	2-2) 연관 규칙 -> 장바구니 분석
	2-3) 이상치 탐지 -> 금융 사기 방지



#### \# 머신 러닝에 대한 용어들 (머신 러닝 교안(이론).pdf (P.28~))
- 추천(Recommendation)
사용자의 취향에 맞는 상품, 서비스, 콘텐츠 등을 예측하고 제공하는 기술

- 차원 축소
많은 데이터를 핵심 정보만 남겨 단순화 시키는 기법입니다.(고차원→저차원)

- 자연어 처리(Natural Language Processing, NLP)
인간이 사용하고 있는 언어/문자 등을 이용하는 기법
문서 분류, 감정 분석, 기계 번역, 질의 응답 시스템 등에 사용
ex) 스팸 메일 필터링, 챗봇, 문서 요약 등.

- 결측치
데이터셋에서 특정 변수의 값이 비어 있거나 누락된 상태를 의미
결측치가 있으면 분석 전에 반드시 탐지하고 적절히 처리하는 전처리 과정을 거쳐야 함

- 이상치(outlier)
데이터 집합에서 다른 값들의 데이터 패턴과 크게 벗어난 값을 의미
이러한 값들은 데이터의 일반적인 패턴과 동떨어져 있어 극단적인 값으로 간주
ex) 키가 대부분 150~180cm인데 한 명이 250cm라고 기록됨

- 가중치


# 4. 12:30 ~ 13:20
#### \# 파이썬으로 머신러닝 공부하기

- a.basic
기본 개념 파일들이니까 알아서 공부하기



# 5. 14:20 ~ 15:10
#### \# simple_linear_regression.py (머신 러닝 교안(실습).pdf (P. 33))
(b.linear - simple_linear_regression.py)

- linear regression : 선형 회귀 (머신 러닝 교안(이론).pdf (P.153))
1) 단순 선형 회귀 : 하나의 독립 변수와 하나의 종속 변수로 가정
2) 다중 선형 회귀 : 여러 개의 독립 변수와 하나의 종속 변수를 가정

- 모델
주어진 훈련 데이터를 기반으로 회귀 계수(coefficient)를 추정하고, 이를 사용하여 새로운 입력 데이터에 대한 출력 값을 예측할 수 있음

- 회귀 분석 - R^2 결정 계수 (머신 러닝 교안(이론).pdf (P. 186))
	회귀 분석에서 모델의 설명력을 나타내는 통계 지표
	구하는 함수 : r_square = model.score(x_test, y_test)
	
1) R^2 = 1: 완벽한 모델
	독립 변수가 종속 변수의 변동을 완벽하게 설명하는 경우.
	(이상적인 상황) 독립 변수들이 종속 변수의 변동에 상당한 영향을 미치고 있다는 것을 의미.
2) R^2 = 0.5:
	회귀선이 데이터의 50% 변동성을 설명하고, 50%만 다른 요인(오차)로 설명됨.
3) R^2 = 0: 설득(설명)력이 전혀 없는 모델
	독립 변수가 종속 변수의 변동을 전혀 설명하지 못하는 경우.
	회귀선이 데이터 평균과 다를 바 없음.


- 구현할 내용
```text
자동차 연비 데이터 셋을 읽어 들입니다.
열 이름 지정 및 데이터 통계 요약 정보를 확인합니다.
상관 계수 분석 및 히트맵을 그려 봅니다.
데이터 전처리 작업을 수행합니다.
분석에 필요한 속성들을 선택합니다.
산점도를 그려 봅니다.
머신 러닝을 위한 데이터 셋 구분을 구분합니다.
단순 회귀 분석 모델을 구현합니다
R^2 결정 계수 및 회귀식의 기울기 및 y 절편을 구해 봅니다.
분석 모델에 테스트용 데이터를 입력하여 값을 예측해 봅니다.
평균 제곱 오차(mse)를 구해 봅니다.
실제 정답과 예측 데이터를 이용하여 산점도를 그려 봅니다.
```


- 코드 내용
```python
import pandas as pd  
from sklearn.linear_model import LinearRegression  
from sklearn.model_selection import train_test_split  
  
pd.set_option('display.max_columns', None)  
pd.set_option('display.max_rows', None)  
  
dataIn = '../dataIn/'  
  
df = pd.read_csv(dataIn + 'auto-mpg.csv', header=None)  
df.columns = ['mpg', 'cylinders', 'displacement', 'horsepower', 'weight', 'acceelerations', 'model_year', 'origin', 'name']  
print('\n데이터 살펴 보기')  
print(df.head())  
  
print('\n데이터 자료형 확인')  
print(df.info())  
  
print('\n데이터 통계 정보 요약')  
print(df.describe())  
  
print('\n컬럼별 누락 데이터 확인')  
print(df.isnull().sum())  
  
import seaborn as sns  
import matplotlib.pyplot as plt  
plt.rc('font', family='Malgun Gothic')  
plt.rcParams['axes.unicode_minus'] = False  
  
dataOut = '../dataOut/'  
  
sns.pairplot(df)  
filename = 'pairplot.png'  
plt.savefig(dataOut + filename)  
print(filename + ' 파일이 저장되었습니다.')  
  
print('\n상관 계수')  
corr = df.corr(numeric_only=True)  
print(corr)  
  
import numpy as np  
  
plt.figure(figsize=(10, 8))  
mask = np.triu(np.ones_like(corr, dtype=bool))  
  
sns.heatmap(corr, mask=mask, cmap='coolwarm', annot=True, fmt='.2f', cbar=True, square=True)  
  
plt.title('변수들간 상관 계수', size=15)  
filname = 'corr_heatmap.png'  
plt.savefig(dataOut + filname)  
print(f'{filname} 파일 생성됨')  
  
print('\n컬럼 목록')  
print(df.columns)  
  
print('\nhorsepower 컬럼의 고유한 값 확인')  
print(df['horsepower'].unique())  
  
# 마력이 '?'인 항목 개수 파악  
print('\n빈도수 역순으로 조회')  
print(df['horsepower'].value_counts())  
  
df['horsepower'] = df['horsepower'].replace('?', np.nan)  
df['horsepower'] = df['horsepower'].astype('float')  
  
print('\n데이터 통계 정보 다시 확인')  
print(df.describe())  
  
print('\n결측치 제거전 : ' + str(df['horsepower'].isnull().sum()))  
df_nan = df.dropna(subset=['horsepower'], axis=0)  
print('\n결측치 제거후 : ' + str(df_nan['horsepower'].isnull().sum()))  
  
# 결측치들을 비결측치들의 평균 값으로 대체하도록 합니다.  
print('\n결측치 제거전 : ' + str(df['horsepower'].isnull().sum()))  
df['horsepower'] = df['horsepower'].fillna(df['horsepower'].mean())  
print('\n결측치 제거후 : ' + str(df['horsepower'].isnull().sum()))  
  
print('\n분석에 필요한 열(feature_특성)만 추출하기')  
print('연비, 실린더, 마력(출력), 중량')  
ndf = df[['mpg', 'cylinders', 'horsepower', 'weight']]  
  
print('다중 회귀 분석을 위하여 복사본을 생성합니다.')  
multidf = ndf.copy() # 참조 복사가 아닌 값 복사를 수행합니다.  
  
xxx = ndf # 참조 복사  
  
  
print('\n상관 계수')  
corr = ndf.corr(numeric_only=True)  
print(corr)  
  
import numpy as np  
  
plt.figure(figsize=(10, 8))  
mask = np.triu(np.ones_like(corr, dtype=bool))  
  
sns.heatmap(corr, mask=mask, cmap='coolwarm', annot=True, fmt='.2f', cbar=True, square=True)  
  
plt.title('변수들간 상관 계수', size=15)  
filname = 'corr_heatmap_new.png'  
plt.savefig(dataOut + filname)  
print(f'{filname} 파일 생성됨')  
  
message = '''  
차량 무게(weight)는 연비(mpg)에 음의 상관 계수를 보이고 있습니다.  
즉, 차량이 무거울수록 연비가 떨어진다는 것을 파악할 수 있습니다.  
'''  
  
print(message)  
  
print('`연비`와 `무게`의 산점도 그래프')  
plt.figure(figsize=(10, 8))  
sns.scatterplot(data=ndf, x='weight', y='mpg')  
plt.title('`연비`와 `무게`의 산점도 그래프', size=15)  
filname = 'scatterplot.png'  
plt.savefig(dataOut + filname)  
print(f'{filname} 파일 생성됨')  
  
# 독립 변수 : 영향을 주는 변수 / 종속 변수 : 영향을 받는 변수  
# 지도 학습은 : 독립 변수 몇개와 종속 변수를 가지고 학습하는 알고리즘  
# 독립 변수(weight) 1개를 사용하여 종속 변수(mpg)에 대한 단순 회귀 분석 테스트  
# 회귀선 표시  
plt.figure(figsize=(10, 8))  
# kind='reg' : 회귀선(regression line)을 포함해 주세요.  
sns.jointplot(data=ndf, x='weight', y='mpg', kind='reg', line_kws={'color':'red'})  
filname = 'jointplot.png'  
plt.savefig(dataOut + filname)  
print(f'{filname} 파일 생성됨')

x = ndf[['weight']] # 독립 변수
y = ndf['mpg'] # 종속 변수

print('독립 변수 및 종속 변수를 7대3 비율로 학습용 데이터 셋과 테스트용 데이터 셋으로 분리합니다.')
# test_size=0.3 : 비율이 7(학습)대3(테스트)
# 머신 러닝 교안(이론).pdf (P. 72)
# train_test_split(독립변수, 종속변수, 비율, (랜덤시드값)) : 4개의 데이터를 반환해서 4개의 변수를 정의함
# x : 독립 변수 / y : 종속 변수 -> x_train 학습용 독립변수 / y_train 학습용 종속변수
# x_test 테스트용 독립변수 / y_test 테스트용 종속변수
# random_state=10 : 랜덤의 시드값을 임의적으로 넣어줌
# (공부할때 매번 값이 바뀌니까 불편. 진짜 할때는 시드값 없이 해야함)
x_train, x_test, y_train, y_test = \
    train_test_split(x, y, test_size=0.3, random_state=10)

print(f'학습용 데이터 셋 개수 : {len(x_train)}')
print(f'테스트용 데이터 셋 개수 : {len(x_test)}')

# 머신러닝의 실제 동작 순서 : 훈련 데이터 → fit() → 회귀 계수·절편 도출 → 회귀식 생성 → predict()로 예측
# 단순 회귀 분석 모델 객체 생성
# 모델 객체는 훈련 데이터를 학습(fit)하여
# 회귀 계수(기울기)와 절편을 도출하고,
# 이를 이용하여 새로운 입력 데이터의 값을 예측함.
# 현재는 객체만 생성한 상태이므로 아직 학습되지 않았음.
model = LinearRegression()

# fit() : 학습
model.fit(x_train, y_train) # 학습

# coef_ : 모델의 회귀계수 / 회귀식의 기울기 / 가중치
# ex) y = 2x + 3 일때 model.coef_는 [2.]
print('\n회귀 계수 : 회귀 식의 기울기')
print(model.coef_)

# intercept_ : 절편(y절편)
# ex) y = 2x + 3 일때 model.intercept_ 는 3.0
print('\n회귀식의 y 절편')
print(model.intercept_)

print('\n수식 표현')
print(f'y = {model.coef_[0]} * x + {model.intercept_}')
'''
이 모델은 독립 변수와 종속 변수 사이에 음의 상관 관계를 가지고 있음을 보여줍니다.
가중치가 -0.0076554은 독립 변수가 1증가할 때, 종속 변수는 대략 0.0076554씩 감소함을 의미합니다.
y 절편 46.60365052224634는 독립 변수의 값이 0일 때 예상되는 종속 변수의 값입니다.
'''

# score(테스트용 독립변수, 테스트용 종속변수) : R^2 결정 계수
# (모델의 설명력, 신뢰도를 나타내는 통계지표) -> 1에 가까울 수록 완벽한 모델
r_square = model.score(x_test, y_test)
print(f'R^2 결정 계수 : {r_square}')
'''
설명력 : 모델이 종속 변수의 변동 중에서 약 68.9%를 설명하고 있습니다.
즉, 독립 변수들이 종속 변수에 상당한 영향력을 미치고 있음을 의미합니다.

잔여 변동 : 나머지 31.1% 정도는 모델이 잘 설명하지 못하는 부분으로, 데이터의 불확실성,
혹은 모델의 구조적인 한계일 수 있습니다.
'''

# prediction은 머신 러닝이 학습한 모델을 이용하여 예측해준 정보
prediction = model.predict(x_test)

# 오차(잔차) 계산
test_preds = pd.DataFrame()
test_preds['label'] = y_test # 실제 정답 데이터를 의미하는 label
test_preds['prediction'] = prediction # 학습한 모델이 예측해준 정보
# squared_error : (실제정답 - 예측값)의 제곱 -> 0에 가까워져야 이상적임
# 공식
test_preds['squared_error'] = (test_preds['label'] - test_preds['prediction']) ** 2

print('\n평균 제곱 오차 데이터 프레임')
print(test_preds.head())

filename = 'test_preds.csv'
test_preds.to_csv(dataOut + filename, index=False)
print(f'{filename} 파일 생성됨')

# 평균 제곱 오차(mean squared error) -> 실제 데이터와 예측 데이터의 오차의 평균의 제곱
# 선형 회귀의 목적은 평균 제곱 오차 (MSE)를 최소로 하는 것.
mse = test_preds['squared_error'].mean()
print(mse)

print('산점도와 회귀선')
plt.figure(figsize=(10, 10))
# 머신러닝에서 정답을 label / 모델의 예측 : prediction
sns.regplot(x='label', y='prediction', data=test_preds, line_kws={'color':'red'})
plt.title('정답과 예측 값의 산점도 그래프')
plt.xlim([5, 45])
plt.ylim([5, 45])
filename = 'regplot.png'
plt.savefig(dataOut + filename)
print(filename + ' 파일이 저장되었습니다.')

# 여기서 부터 다중 회귀 분석입니다.
print('multidf.columns')
print(multidf.columns)

# 독립 변수와 종속 변수를 분리
independent_variable = ['cylinders', 'horsepower', 'weight']
x = multidf[independent_variable]
y = multidf['mpg']

# 훈련용 데이터와 테스트용 데이터 분리
x_train, x_test, y_train, y_test = \
    train_test_split(x, y, test_size=0.3, random_state=10)

print(f'훈련용 데이터 형상 : {x_train.shape}')
print(f'테스트용 데이터 형상 : {x_test.shape}')

# 다중 회귀 분석을 위한 모델 생성
multi_model = LinearRegression()

multi_model.fit(x_train, y_train)

r_square = multi_model.score(x_test, y_test)
print(f'R^2 결정 계수 : {r_square}')

slope = multi_model.coef_.tolist()
print(f'기울기 : {slope}')
print(f'y절편 : {multi_model.intercept_}')

# 막대 그래프 그리기
plt.bar(independent_variable, slope)
plt.title('회귀식의 기울기')
plt.xlabel('변수')
plt.ylabel('기울기')
plt.savefig(dataOut + 'independent_variable_slope.png')

prediction = multi_model.predict(x_test)

fig, axes = plt.subplots(1, 3, figsize=(12, 5))

for i, col in enumerate(x_test.columns):
    axes[i].plot(x_train[col], y_train, 'o', label='훈련 데이터')
    axes[i].plot(x_test[col], prediction, 'r+', label='예측값')
    axes[i].set_xlabel(col)
    axes[i].set_ylabel('mpg')
    # 범례 위치는 데이터의 분포를 보시고, 적정 위치에 두세요.
    axes[i].legend(loc='best')

plt.suptitle('독립 변수에 따른 종속 변수 산점도', size=15)
plt.savefig(dataOut + 'multivariance_regression.png')
```



# 6. 15:20 ~ 16:10
#### \# simple_linear_regression.py (머신 러닝 교안(실습).pdf (P. 33)) 이어서 작성


# 7. 16:20 ~ 17:10
자습


# 8. 17:20 ~ 18:10
자습