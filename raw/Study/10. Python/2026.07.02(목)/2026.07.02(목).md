# 1. 09:30 ~ 10:20
#### \# Chap07.Data Reshape.ipynb이 템플릿인 새 파일 생성
(ch13_pandas 폴더에 새 파일 하나 생성 후 작성)
이 파일에 직접 타이핑 하는 것이 아니고
Chap07.Data Reshape.ipynb 이 파일은 이미 완성된 템플릿 파일

새로운 파일을 하나 만들어서 저 파일 기반으로 내가 직접 타이핑해서 만들어보는 작업
(쥬피터 노트북으로 진행)

- 코드 내용 (데이터 결합(concat))
```python
import pandas as pd

dataIn = "../dataIn/"


# 1분기 판매량
filename = dataIn + "가전제품판매량01.csv"
# 선생님께서 엑셀에서 보시려고 해당 파일의 인코딩을 CP949로 만들어 놓으심
homeware01 = pd.read_csv(filename, encoding="CP949", index_col="지역")
homeware01

# 2분기 판매량
filename = dataIn + "가전제품판매량02.csv"
homeware02 = pd.read_csv(filename, encoding="CP949", index_col="지역")
homeware02

# 각 분기 판매량의 행 색인들 변수로 지정
columnInfo01 = homeware01.columns
columnInfo02 = homeware02.columns

# 2개의 Index 객체 (겉으로는 리스트 타입처럼 보임) 비교
# intersection() : 교집합 표현하는 함수
columnInfo01.intersection(columnInfo02)

# difference() : 차집합 표현하는 함수
# A - B를 표현 (A에만 존재하는 것 표현)
columnInfo01.difference(columnInfo02)

columnInfo02.difference(columnInfo01)

# concat() : 문자열을 합치는 함수
# join="기본값은 outer"
# join="inner" : 교집합 가져오기 / 데이터베이스에서 inner join 같은 것
# 서로 짝이 맞는(공통으로 존재하는) 행이나 열만 남기고 나머지는 다 버리겠다는 뜻
# join="outer" : 합집합 가져오기
# result = pd.concat([homeware01, homeware02], join="inner")
# result

# axis=0 : 행을 기준으로 (행이 늘어난다는 뜻)
# 이때 열에 대한 내용이 기본적으로 합집합으로 나타냄
result = pd.concat([homeware01, homeware02], axis=0)
result

# axis=1 : 열을 기준으로 (행이 늘어난다는 뜻)
result = pd.concat([homeware01, homeware02], axis=1)
result

result.columns

# 서로 원래 공통된 열이 있어서 2개의 데이터 프레임을 그냥 합치면 하나로 합쳐지는데
# 서로의 열을 따로 표시하면서 하나의 데이터 프레임을 만들기 위해서 열의 이름을 서로 다르게 만들어서 합침
homeware01.columns = ["미니오븐01", "전기밥솥01", "후라이팬01", "분기01"]
# 이런식으로 바꿔도 됨
homeware02.columns = [item + "02" for item in homeware02.columns]

result = pd.concat([homeware01, homeware02], axis=1)
result
```



# 2. 10:30 ~ 11:20
#### \# Chap07.Data Reshape.ipynb이 템플릿인 새 파일 생성 (이어서)
- 코드 내용 (데이터 연결하기(merge))
```python
file01 = dataIn + "학생정보01.csv"
student = pd.read_csv(file01, encoding="CP949")
student

file02 = dataIn + "시험점수01.csv"
jumsu = pd.read_csv(file02, encoding="CP949")
jumsu

# 교집합으로 공통된 컬럼 정보 확인하기
# 양쪽에 동일한 이름의 컬럼이 존재하는 지 확인합니다.
col_student=student.columns
col_jumsu=jumsu.columns
col_student.intersection(col_jumsu)

# 2개의 데이터 프레임의 열 중에서 교집합인 "id" 열에 대하여 merge하기
# pd.merge() : 특정 기준 열의 값이 같은 것끼리 찾아서 가로로 '조립(병합)'하는 함수
# SQL에서 JOIN 연산과 같은 개념
# on="공통된열, 행"
result = pd.merge(student, jumsu, on='id')
result

# copy() : 원본 데이터는 놔두고 복사 데이터를 만들기
# chartdata03 = result 이렇게 하면 result가 변하면 chartdata03도 같이 변하는건 당연하고
# chartdata03를 바꾸면 result도 같이 변해버림 -> 데이터의 '메모리 주소'를 공유하고 있어서
chartdata03 = result.copy()
# chartdata03["id"]=="kang" : 열 색인 기준으로 값이 "kang"인 데이터를 가져오기
# -> "id"라는 이름의 열 색인에 "kang"이라는 데이터가 있는 걸 찾고 그 데이터가 있는 행들을 가져옴
# chartdata03.index == "행 색인 이름" : 행 색인 기준으로 데이터 가져올때
# -> 열 색인 기준으로 가져올때는 열 색인 이름과 그 값에 대해서 찾고 그에 해당하는 행들을 가져오지만
# -> 행 색인 기준으로 가져올때는 행 색인의 이름(레이블) 자체를 찾고 그에 해당하는 행들을 가져옴
chartdata03 = chartdata03.loc[chartdata03["id"]=="kang", ["과목", "점수"]]
# set_index("기존에 있던 열 색인") : 열 색인을 행 색인으로 바꾸는 함수
chartdata03 = chartdata03.set_index("과목")
chartdata03

# 데이터 프레임을 기반으로 그래프 그리기
import matplotlib.pyplot as plt
plt.rc('font', family='Malgun Gothic')

mylabel = chartdata03.index
chartdata03.plot(y='점수', kind='pie', labels=mylabel, shadow=False, \
         autopct='%1.2f%%', startangle=90, counterclock=False)
plt.grid(True)
plt.legend(loc=4)
plt.xlabel(chartdata03.index.name)
plt.title('과목별 점수 비율')

# merge() 메소드의 다양한 옵션
# how 옵션 사용해보기
result = pd.merge(student, jumsu, on='id', how="outer")
result

result = pd.merge(student, jumsu, on='id', how="left")
result

result = pd.merge(student, jumsu, on='id', how="right")
result

# 데이터 프레임의 컬럼 이름이 다른 경우
file01 = dataIn + "학생정보02.csv"
student = pd.read_csv(file01, encoding="CP949")
student

file02 = dataIn + "시험점수02.csv"
jumsu = pd.read_csv(file02, encoding="CP949")
jumsu

# left_on, right_on 옵션 사용 (Python(이론).pdf (P.285))
result = pd.merge(student, jumsu, left_on='leftid', right_on='rightid')
result
```



# 3. 11:30 ~ 12:20
#### \# Chap07.Data Reshape.ipynb이 템플릿인 새 파일 생성 (이어서)
- 코드 내용 (데이터 연결하기(merge))
```python
# 여러 개의 컬럼 병합하기
file01 = dataIn + "시험점수03.csv"
data01 = pd.read_csv(file01, encoding='CP949')
data01

file02 = dataIn + "시험점수04.csv"
data02 = pd.read_csv(file02, encoding='CP949')
data02

# on 옵션 사용해보기
# 양쪽 프레임에 동일 개념의 컬럼이 2개 이상인 경우
on_condition = ["이름", "과목"]
result = pd.merge(data01, data02, on=on_condition, how="outer")
result

# on, how 옵션 사용해보기
file01 = dataIn + "시험점수05.csv"
data01 = pd.read_csv(file01, encoding='CP949')
data01

file02 = dataIn + "시험점수06.csv"
data02 = pd.read_csv(file02, encoding='CP949')
data02

# 동일 컬럼 이름("시험")은 접미사 '_x', '_y'를 붙여 줍니다.
# 따로 설정하지 않아도 기본값으로 점미사를 붙여줌
result = pd.merge(data01, data02, on=on_condition, how='outer')
result

# 동일 이름의 컬럼("시험")에 접미사 사용
# suffixes=튜플형식 : 동일 컬럼의 접미사 옵션을 내 마음대로 설정할때
result = pd.merge(data01, data02, on=on_condition, how='outer', \
                  suffixes=('_중간', '_기말'))
result


# 색인 정보를 사용한 merge 기능

# 파일을 불러들임과 동시에 행 색인을 바꾸는 법
# 학생의 시험 정보 읽기(index_col 옵션)
file01 = dataIn + "학생정보01.csv"
student = pd.read_csv(file01, encoding="CP949", index_col='id')
student

file02 = dataIn + "시험점수01.csv"
jumsu = pd.read_csv(file02, encoding="CP949", index_col='id')
jumsu

# 컬럼(열) 가지고 할때는 left_on / right_on
# 인덱스(행) 가지고 할때는 left_index / right_index
# 다양한 옵션 사용하기
result = pd.merge(student, jumsu, left_index=True, right_index=True, \
                  how='outer', indicator = True)
result

# 한쪽에 있는 데이터만 찾기(query 메소드)
# 왼쪽에 있는 데이터만 추출하기
# 시험 안본 학생 추출하는 것 (right(시험점수 데이터 프레임)에는 choi가 없음)
m_result = result.query('_merge == "left_only"')
m_result
```



# 4. 12:30 ~ 13:20
#### \# Chap07.Data Reshape.ipynb이 템플릿인 새 파일 생성 (이어서)
- 코드 내용 (형상 변경하기)
```python
# 과일 구매 내역 정보 읽기(pivot)

filename = dataIn + "pivotFile.csv"
data = pd.read_csv(filename, encoding="UTF-8")
data

# 긴 형식 데이터를 넓은 형식으로 변환

# 긴 형식(long format)의 데이터를 넓은 형식(wide format)으로 변환합니다.
pivot_data = data.pivot(index="name", columns="item", values="value")
pivot_data

# 2개 이상의 컬럼도 가능

pivotData = data.pivot(index='name', columns='item', values=['value', 'qty'])
pivotData


# 직원별 월별 실적(melt)
# 데이터 읽기
filename = dataIn + "meltsheet.csv"
siljuk = pd.read_csv(filename, encoding='CP949')
siljuk

# 직원별 월별 실적 그래프
import pandas as pd
import matplotlib.pyplot as plt
plt.rc('font', family='Malgun Gothic')

chartdata=siljuk.copy()
chartdata=chartdata.set_index('이름').T
chartdata

chartdata.plot(kind='line', figsize=(10, 5), ylim=[50, 100], grid=False, \
                rot=10, use_index=True)
plt.xlabel('월')
plt.ylabel('실적')
plt.title('직원별 월별 꺽은 선')

# 넓은 형식 데이터를 긴 형식으로 변환
siljuk_long = pd.melt(siljuk, id_vars =['이름'],
                var_name='월', value_name = '실적')
siljuk_long.head(8)

# 특정인만 조회해 보기
result = siljuk_long.loc[siljuk_long['이름'] == '이순신']
result

# 특정월만 조회해 보기
result = siljuk_long.loc[siljuk_long['월'] == '4월']
result

# isin() 함수는 특정 조건에 만족하는 데이터에 대하여 True를 반환해주는 함수입니다.
# 데이터 베이스의 in 키워드와 유사한 개념입니다.
filter_dict = {'월':['1월', '3월']}
condition = siljuk_long.isin(filter_dict)
condition

result = siljuk_long.loc[condition['월'] ]
result
```



# 5. 14:20 ~ 15:10
#### \# Chap07.Data Reshape.ipynb이 템플릿인 새 파일 생성 (이어서)
- 코드 내용 (형상 변경하기)
```python
# 영화 매출 데이터(pivot_table)

# 영화 데이터 읽기
filename = dataIn + "movie_show.csv"
movie_show = pd.read_csv(filename, encoding='CP949')
movie_show

# 장르별 매출 분류하기
movie_show_melt = pd.melt(movie_show, id_vars=['gu', 'year', 'month'], \
                       var_name='genre', value_name='qty')
movie_show_melt.head()

# 구별, 년도별 월 매출 평균, 총합
# pivot_table() : 데이터를 재구성하여 요약된 형태로 표현(그룹화, 집계, 정리 등)하는 데 사용됩니다.
# 특정 지역의 특정 월에 대하여 상영 회수의 평균 값을 보여 줍니다.
# aggfunc는 집계 함수(aggregate function)를 의미합니다.
# mean : 평균
pivoting = movie_show_melt.pivot_table(index=['gu', 'year'], columns='month', values='qty', aggfunc='mean')
pivoting

pivoting = movie_show_melt.pivot_table(index=['gu', 'year'], columns='month', values='qty', aggfunc='sum')
pivoting # 총합


# 2개 이상의 컬럼에 대한 집계
dict_aggfunc = {'genre':'count', 'qty':'mean'}

pivoting = movie_show_melt.pivot_table(
    index=['gu', 'year'],
    columns='month',
    values=['genre', 'qty'],
    aggfunc=dict_aggfunc)
pivoting

# 특정 컬럼 1개에 대하여 여러 개의 함수를 적용합니다.
dict_aggfunc = {'qty':['mean', 'sum']}

pivoting = movie_show_melt.pivot_table(
    index=['gu', 'year'],
    columns='genre',
    values='qty',
    aggfunc=dict_aggfunc)
pivoting


# 중첩 열 색인의 단순화(simplification)

# 내부와 외부 색인 사이에 언더바를 붙여서 renaming하기
outer = pivoting.columns.get_level_values(0) # 외부 색인
inner = pivoting.columns.get_level_values(1) # 내부 색인
print(outer, inner)
newColumns = inner + '_' + outer
pivoting.columns = newColumns
pivoting
```



# 6. 15:20 ~ 16:10
#### \# Chap04.DataFrame Operator.ipynb
DataFrame 연산하기

- 코드 내용 (다중 열(column) 선택)
```python
# DataFrame 요약 정보 확인

# DataFrame 기초 정보 확인
import pandas as pd

dataIn = '../dataIn/'

filename = dataIn + 'payment02.csv'
payment = pd.read_csv(filename, encoding='UTF-8')
print('payment 정보 학인하기')
print(payment.info())

print(payment.index)

print(payment.columns)

print(payment.dtypes)

# 다중 열 선택하기
search_column = ['사원명', '부서', '성별']
payment_data = payment[search_column]
result = payment_data.head()
result

result = payment[['사원명']]
result
```


- 코드 내용 (메소드를 사용한 열 선택)
```python
# 실습 파일 읽기

# DataFrame 데이터 타입 확인하기
filename = dataIn + 'payment02.csv'
payment = pd.read_csv(filename, index_col='사원명', encoding='UTF-8')
payment.dtypes.to_frame().T

payment


# 개발자 지정 데이터 타입의 열만 선택하기
result = payment.select_dtypes(include=['int64'])
result

result = payment.select_dtypes(include=['number'])
result

# 컬럼 필터링하기
# 컬럼 이름에 '비'라는 글자가 들어 있으면 추출합니다.
result = payment.filter(like='비').head()
result

# regex : 정규 표현식(regular expression)
# 꺽쇠(^) 기호는 '~으로 시작하는', 달러($) 기호는 '~으로 끝나는'의 의미
result = payment.filter(regex='비$').head()
result

# 정규 표현식 : '부'또는 '박'자를 포함하고 있는~~
payment.filter(regex='(부|박)').head()

result = payment.filter(items=['식비', '교통비']).head()
result
```


- 코드 내용 (컬럼 이름 재배치하기)
```python
# 실습 파일 읽기

# DataFrame 내용 확인
filename = dataIn + 'payment02.csv'
payment = pd.read_csv(filename, encoding='UTF-8')
payment

print(payment.columns)


# 컬럼 재배치 하기

# 재배치를 위한 list 만들기
column01 = ['사원명', '출장지역', '부서', '성별']
column02 = ['교통비', '숙박비', '식비', '부대비용', '출장기간']
myordering = column01 + column02
print('컬럼 재배치 현황 : \n' + str(myordering))

result = set(payment.columns) == set(myordering)
print('최초 컬럼 정보와 동일한가 ? ' +str(result))

paymentnew = payment[myordering]
paymentnew
```


- 코드 내용 (기초 통계 요약 정보 확인)
```python
# 통계 요약 정보

# 속성을 이용한 행과 열의 기본 정보

print('payment.size : ' + str(payment.size))# 전체 요소의 갯수
print('payment.shape : ' + str(payment.shape)) # 형상 확인
print('행수 : ' + str(payment.shape[0]))
print('열수 : ' + str(payment.shape[1]))
print('payment.ndim : ' + str(payment.ndim)) # 차원 수
print('len(payment) : ' + str(len(payment)))# 행 개수

print('payment.count()')
payment.count().to_frame().T


# 통계 정보 확인하기

# 기본 값으로 숫자형 데이터의 간략한 통계치 정보를 보여 줍니다.
payment.describe().T

payment.describe(include='number').T

payment.describe(include='all')

payment.describe(include='object').T

mylist = [0.3 * idx for idx in range(1, 4)]

# percentiles 매개 변수를 이용하여 특정 지점의 백분위수를 확인할 수 있습니다.
paymentPect = payment.describe(include='number', percentiles=mylist).T
paymentPect

# 숫자 타입은 '산술 연산', 문자열 타입은 '문자열 결합'이 이루어 집니다.
payment.sum().to_frame().T

payment.sum(numeric_only=True).to_frame().T

payment.mean(numeric_only=True).to_frame().T

payment.min().to_frame().T
```


- 코드 내용 (메소드 Chaining 연산 및 축 지정)
```python
# 누락 데이터, 산술 연산, 축지정

# 누락 데이터 개수 파악

payment.isnull()

payment.isnull().sum().to_frame().T

print('전체 결측치 개수 : ' + str(payment.isnull().sum().sum()))


# 산술 연산
# 다음 문장은 오류가 발생합니다.
#print(payment + 5)

payment.dtypes.to_frame().T

likes = payment.filter(items=['출장기간', '교통비', '식비', '숙박비', '부대비용'])
likes

result = likes + 10
result


# 축(axis) 지정하기
filename = dataIn + 'likelyhood.csv'
likelyhood = pd.read_csv(filename)
likelyhood.head()

print(likelyhood.info())

likelyhood.count().to_frame().T

likelyhood.count(axis=0).to_frame().T

likelyhood.count(axis='rows').to_frame().T

print('열 개수 : ' + str(len(likelyhood.columns)))

likelyhood.count(axis='columns').head().to_frame().T

likelyhood.sum(axis='columns', numeric_only=True).head().to_frame().T
```


- 코드 내용 (기타 유용한 기능)
```python
# 메모리 사용량

# 데이터 유형 확인

filename = dataIn + 'payment02.csv'
payment = pd.read_csv(filename)
payment


# 상위 x명 추출
top=4
print('\'부대 비용\' 상위 '+ str(top) + '명 추출하기')
payment.nlargest(top, '부대비용')

# 상위 x명 중에서 하위 y명 추출
bottom=2
print('\'부대 비용\' 상위 '+ str(top) + '명 중에서 \'숙박비\' 하위 ' + \
      str(bottom) + '명 추출하기')
payment.nlargest(top, '부대비용').nsmallest(bottom, '숙박비')


# query() 메소드 사용

# 관계 연산자 사용

payment.query('숙박비 > 식비')

payment[payment['숙박비'] > payment['식비']]

payment.query('교통비 == 식비')

payment[payment['교통비'] == payment['식비']]

# 조건식 필터링
# 조회하고자 하는 부서 목록
dept_col = ['영업부', '전산부', '구매부']
# 조회하고자 하는 컬럼 목록
column_list = ['사원명', '출장지역', '부서', '성별', '교통비']

# 부서가 dept_col에 포함되어 있고, 남자이며, 교통비가 4000이상 7000이하인 데이터
# @ 기호는 치환 변수의 의미로 사용됩니다.
sql = "부서 in @dept_col and 성별 == '남자' and 4000 <= 교통비 <= 7000" # 검색 조건
resultSet = payment.query(sql)
resultSet[column_list]


# 빈도수가 높은 상위 부서 2개 고르기
dept_frequency=payment.부서.value_counts()
print('부서별 빈도수 확인')
dept_frequency.to_frame().T

rowsu=2 # 추출할 상위 개수
concern_data = dept_frequency.index[:rowsu].tolist()
concern_data

# 빈도수 상위 2개 부서 멤버가 아닌 사원 중 남자인 사원은?
print('\n빈도 수가 많은 상위 ' + str(rowsu) + '개 : '+ str(concern_data))
filtering = payment.query("부서 not in @concern_data and 성별 == '남자'")
filtering
```



# 7. 16:20 ~ 17:10



# 8. 17:20 ~ 18:10
