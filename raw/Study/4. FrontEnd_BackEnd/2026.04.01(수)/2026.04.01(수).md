# 1. 09:30 ~ 10:20
#### \# SpringBoot 교안 PDF (P.55)
현재 Controller까지 함



#### \# react의 웹페이지 만드는 과정 (MenuItems에 내용 추가하기)
1) App.tsx 확인


2) 추가할 컴포넌트? 생성하기


3) MenuItems.tsx에 추가할 컴포넌트 내용추가


ex) 이동할 주소 추가, 웹페이지 상 이름 추가

```typescript
<NavDropdown.Item onClick={() => navigate(`/product/list`)}>상품 목록</NavDropdown.Item>
```

4) AppRoutes.tsx에 라우팅하기


ex01) 파일 import하고 파일의 경로 추가

```typescript
import ProductList from './../pages/ProductList';
```

ex02) 이동할 주소 추가, 이동하고 나올 웹페이지 파일 추가

```typescript
<Route path="/product/list" element={<ProductList />} />
```



#### \# App.tsx의 코드 수정하기

```typescript
import './App.css';

// 외부 컴포넌트 import하기
// import 컴포넌트이름 from '경로와 파일명';
import MenuItems from './ui/MenuItems';
import AppRoutes from './routes/AppRoutes';

function App(){
  const appName = "IT Academy Coffee Shop" ;

  return (
    <>
      <MenuItems />
      <AppRoutes />

      <footer className="bg-dark text-light text-center py-3 mt-5">
        <p>&copy; 2025 {appName}. All rights reserved.</p>
      </footer>
    </>
  );
}

export default App;
```



#### \# MenuItems.tsx의 코드 교체하기 (03.기본기 다지기.txt 247번째 줄 이용)

```typescript
import { NavDropdown, Navbar, Container, Nav } from "react-bootstrap";

import { useNavigate } from "react-router-dom";

function App() {
   const navigate = useNavigate();

   return (
      <Navbar bg="dark" variant="dark" expand="lg">
         <Container>
            <Nav className="me-auto">
               <NavDropdown title={`기본 연습`}>
                  <NavDropdown.Item onClick={() => navigate(`/fruit`)}>과일 1개</NavDropdown.Item>
                  <NavDropdown.Item onClick={() => navigate(`/fruit/list`)}>과일 목록</NavDropdown.Item>
               </NavDropdown>
            </Nav>
         </Container>
      </Navbar >
   );
}

export default App;
```



#### \# VSC에 src폴더 안에 types폴더 생성 후 Fruit.ts파일 생성
ts는 typescript 파일을 의미함
???에 보낼 데이터의 타입을 명시하는 것?



#### \# Fruit.ts 타입 스크립트 인터페이스 파일에 코드 작성
- 코드 작성
```typescript
// 리액트에서 사용할 과일의 유형은 다음과 같습니다.
export interface Fruit {
    id: string
    name: string
    price: number
}
```

-> Fruit라는 데이터가 3개의 변수를 가지고 있다는 것? 타입?을 명시함
-> Fruit라는 객체가 어떤 형태(Shape)여야 하는지 "설계도"를 그린 것

- 이유
1) 자동 완성: 개발 도구(VS Code 등)에서 fruit.까지만 쳐도 id, name, price가 목록에 자동으로 떠서 오타를 줄여줌



#### \# VSC 추가 설정하기 (좌측 파일 탐색기 영역 조금 더 편하게 보기)
1) VSC에서 설정창 여는 단축키 : Ctrl + , 하면 됨


2) 설정창 검색창에


```java
workbench.tree.indent
```

입력

3) Workbench › Tree: Indent


Controls tree indentation in pixels.
이곳에 24 입력



# 2. 10:30 ~ 11:20
#### \# 03.기본기 다지기.txt 파일 교체하기
선생님이 나중에 추가하신 내용 있다고 교체하는게 좋다고 하심



#### \# 03.기본기 다지기.txt 384번째 줄 설명 (중요!!!!)

```typescript
// 백엔드한테 데이터를 요청하고 웹페이지에 보여주기 (간단 설명)
```

1)

```typescript
const url = `${API_BASE_URL}/fruit`; // 요청할 url 주소
```

-> config.tsx 파일에서 정의한

```java
export const API_BASE_URL = `http://${API_HOST}:${API_PORT}` ;
```

을 이용함

2)

```typescript
const response = await axios.get<Fruit>(url, {
```

	withCredentials: true,

```java
});
```

axios라는 녀석이 get방식으로 Fruit을 요청하고 있다.
-> spring에 가보면 이것을 FruitController.java 파일에 있는 @GetMapping("/fruit")가 응대를 함
(const url = `${API_BASE_URL}/fruit`; // 요청할 url 주소)의 /fruit에 의해서

3)
return되면 JSON파일로 return이 된 상태로 웹페이지에 보여짐

```typescript
// W3Schools에 useState검색해서 React useState Hook 클릭
```

1. Update State의 2번째 Example을 Run Example 클릭
Blue클릭 시 문장에 red가 blue로 변하는 것 확인

- const [color, setColor] = useState("red");

State는 이 프로그램 안에서 내가 관리해야하는 바뀔 소지가 있는 어떠한 놈들

```typescript
useState는 hook이라고도 하는데(State들을 낚아채서 관리해서) State들을 관리할때 사용함
useState()에서 괄호 안에 내용은 기본값임
```

setColor는 setter인데 이름은 마음대로 해도 됨
-> 나는 지금부터 color라는 상자를 만들 건데, 처음에는 red를 넣어둘게.
그리고 이 안에 내용물을 바꾸고 싶을 때는 꼭 setColor라는 전용 도구를 사용할 거야.

(텍스트 안에 있는 내용으로 설명)
1)

```typescript
const response = await axios.get<Fruit>(url, {
```

	withCredentials: true,

```java
});
```

응답값을 가지고옴
response(응답값)는 ${API_BASE_URL}/fruit에 있는 JSON 파일명

2) setFruit(response.data);


setter로 값을 바꿈

```typescript
// W3Schools에 useEffect검색해서 React useEffect Hooks 클릭
```

1. 첫번째 Example을 Run Example 클릭

useEffect는 BackEnd 서버에서 데이터 가져올때? 사용
(텍스트 안에 있는 내용으로 설명)

```typescript
useEffect(() => { // BackEnd 서버에서 데이터 읽어 오기
	const fetchFruit = async () => {
		try {
			const url = `${API_BASE_URL}/fruit`; // 요청할 url 주소
			const response = await axios.get<Fruit>(url, {
```

				withCredentials: true,

```typescript
			});
			setFruit(response.data);
		} catch (error) {
			console.error(error);
		}
	};

	fetchFruit();
}, []);
```

-> 스프링으로 가서 주소 던지고 데이터 달라고 요청함

2. 다른 예시
1) useEffect(() => {


```typescript
  //Runs on every render
});
```

매번 호출
-> 타이머 만들때 사용

2) useEffect(() => {


```typescript
  //Runs only on the first render
}, []);
```

오직 한 번만 호출
-> 우리가 사용하는 코드에서 이렇게 사용함

3) useEffect(() => {


```typescript
  //Runs on the first render
  //And any time any dependency value changes
}, [prop, state]);
```

바뀔 수 있는 state가 들어 있음
일단 한번 호출하고 state가 바뀔때마다 호출



# 3. 11:30 ~ 12:20
#### \# CORS (교차 출처 리소스 공유) (IT 관련 용어 PDF (P.51~53))
port number가 다른다 = origin이 다르다. ?

원래 origin이 다르면 서로 접근이 불가능함
SOP(Same-origin policy)때문에



#### \# 파일 경로에 적힌 ..의 의미?
(리액트에서만 그런게 아니고 IT전반엔 사용되는 개념)
절대 경로와 상대 경로
. 기호는 현위치
. . 기호는 상위 디렉토리
/ 기호는 리눅스에서의 폴더 또는 디렉토리 구분자
\ 기호는 윈도우에서의 폴더 또는 디렉토리 구분자
(\ 기호는 특수기호여서 2개를 써줘야 한개로 인식함)
ex) productImageLocation=C:\\shop\\images

1) 상대 경로


- 특정한 누군가를 가지고 경로를 정하는 것
서로 다른 디렉토리를 연결하려면
. . 기호로 상위 디렉토리로 올라가고 /로 구분해놓고
연결하려는 파일의 디렉토리 적고 다시 /로 구분해놓으면 됨
ex)

```java
FruitOne.tsx : src/pages/FruitOne.tsx
AppRoutes : src/routes/AppRoutes
```

AppRoutes상에서

문법 :

```typescript
import {파일명} from "../pages/FruitOne"
```

이렇게 하면 FruitOne과 연결? 참조?됨

2) 절대 경로


- 어떠한 기준점을 가지고 경로를 정하는 것



#### \# FruitOne.tsx 파일에 코드 작성

```typescript
import { useState } from "react";
```

- 상대경로로 useState할때 필요한 Fruit.ts를 호출
```typescript
import type { Fruit } from "../types/Fruit";
```

- useState 추가
문법 :

```typescript
const [state변수, state변경함수] = useState<타입>(초기값);
```

(코드 작성)

```typescript
const [fruit, setFruit] = useState<Fruit | null>(null); // 넘겨 받은 과일 1개
```

-> 타입인 Fruit를 작성하면 ../types/Fruit 경로에 있는 Fruit.ts파일을 가져오고 그 속성을 fruit가 가짐

- useEffect 추가
문법 :

```typescript
useEffect(() => {}, []); // 한 번만 호출해라
```

(코드 작성)

```typescript
import { useEffect, useState } from "react";
import type { Fruit } from "../types/Fruit";
import { API_BASE_URL } from "../config/config";
import axios from "axios";


function App(){
  const [fruit, setFruit] = useState<Fruit | null>(null); // 넘겨 받은 과일 1개

  useEffect(() => { // Backend 서버에서 데이터 받아 오기
    const fetchResult = async () => {
      try{ // 백엔드에 요청하는 내용 작성
          const url = `${API_BASE_URL}/fruit`; // 요청할 url 주소 // config.tsx에서 변수 가져와야해서 import도 함
          const config = {withCredentials: true}; // 쿠키 관련 내용인데 나중에 설명해줌
          const response = await axios.get<Fruit>(url, config); // spring에서 요청한걸 주면 내가 이렇게 응답해서 줄게 (JSON파일로)
      }catch(error){ // 예외처리할때 사용하는 구문 (오류처리)
        console.log(error);
      }
    };

    fetchResult(); // 직접 호출
  }, []);

  return (
    <>
      상품 1개
    </>
  );
}

export default App;
```

- 통신 방식 종류
1) 동의 통신 방식


택배 기사님이 배달왔는데 내가 집에 없어서 내가 올때까지 기다려달라

2) 비동의 통신 방식


택배 기사님이 배달왔는데 내가 집에 없어서 택배를 문 앞에 놓고감
-> 리액트에서 사용함 -> 데이터를 백엔드에 툭 던져놓고 react는 내 할 일 하는중

\* async ()와 await * 무조건 써야하는 건 아니지만 많이 사용됨
1) async () : 비동의 통신 방식


2) await


- react에서 useEffect할때 response에 get방식을 쓸지 post방식을 쓸지 구분하는 기준
spring에 가서 보면 GetMapping으로 되어있어서
만약 PostMapping이였으면 post를 써야함



# 4. 12:30 ~ 13:20
#### \# axios 응답 스키마 (React 교안 PDF (P.104))
살펴보기



#### \# 이어서 작성
1) State 변수는 자기가 직접 수정은 안되고


반드시 setter로 수정해야함
ex)

```typescript
setFruit(response.data); // 응답 데이터를 fruit인 state변수에 넣음

```

2) <td>{fruit?.id}</td>에서 ?의 의미


{/* optional chaining은 객체가 null 또는 undefined일 때 오류 없이 접근하도록 하는 자바 스크립트 문법입니다.*/}
{/* optional chaining : fruit가 null → 아무것도 안 나옴(undefined 반환), fruit가 존재 → id 출력 */}
-> 이게 없으면 fruit가 null일때 빨간글씨 뜨고 난리남

3) (코드 추가 작성)


```java
return (
        <>
            <Table hover style={{ margin: '20px' }}>
                <tbody>
                    <tr>
                        <td>아이디</td>

                        {/* optional chaining은 객체가 null 또는 undefined일 때 오류 없이 접근하도록 하는 자바 스크립트 문법입니다.*/}
                        {/* optional chaining : fruit가 null → 아무것도 안 나옴(undefined 반환), fruit가 존재 → id 출력 */}
                        <td>{fruit?.id}</td>
                    </tr>
                    <tr>
                        <td>상품명</td>
                        <td>{fruit?.name}</td>
                    </tr>
                    <tr>
                        <td>단가</td>
                        <td>{fruit?.price.toLocaleString()}원</td>
                    </tr>
                </tbody>
            </Table >
        </>
    );
```

4) 이렇게 작성하고 리액트 웹페이지에서 FruitOne에 맞는 페이지 들어가면


fruit?.id 같은 데이터가 안뜸 -> spring과 react의 port number가 달라서 SOP(Same-origin policy)때문에 오류가 남
Access to XMLHttpRequest at 'http://localhost:9000/fruit'

```sql
from origin 'http://localhost:5173' has been blocked by CORS policy: No 'Access-Control-Allow-Origin'
```

header is present on the requested resource.

\* 03.기본기 다지기.txt 내용으로 이해하기 *
브라우저 확인
실행 결과를 보면 CORS 오류가 발생했음을 알려 주면서 데이터를 제대로 읽어 오지 못합니다.
"과일 목록"이라는 메뉴 역시 CORS 오류가 발생했음을 알려 주면서 데이터를 제대로 읽어 오지 못합니다.
이제 CORS가 무엇인지 좀 살펴 보고 해결해 보도록 하겠습니다.
\**********************************************************************************
CORS
CORS(Cross-Origin Resource Sharing, 교차 출처 리소스 공유) 라고 부르며,
웹 브라우저에서 다른 도메인(출처)의 리소스(데이터, 이미지, API 등)에 접근할 수 있도록 허용/제어하는 보안 메커니즘입니다.
웹 브라우저는 보안상 기본적으로 동일 출처 정책(Same-Origin Policy, SOP)을 따릅니다.
즉 포트 번호가 동일하지 않으면 접속을 거부합니다.

사용 예시
	http://localhost:3000 → http://localhost:3000/api/data (같은 출처 → 허용)
	http://localhost:3000 → http://localhost:9000/api/data (포트 다름 → 차단)
	http://myapp.com → http://api.myapp.com (서브 도메인 달라서 차단)



#### \# CORS 설정하기 (port 다른걸 서로 연결하기) (spring - java에서 하기)
WebConfig 클래스는 @Configuration 어노테이션에 의하여 Spring Boot의 설정 클래스로 등록되어 있고,
WebMvcConfigurer를 상속 받아 addCorsMappings를 오버라이딩하여 React(3000번 포트)에서 들어 오는
모든 요청을 허용하도록 세부 규칙을 정하고 있습니다.

(java/com.coffee에 config폴더 생성 후 WebConfig 클래스 생성)
(WebConfig 클래스에 코드 복붙)

```java
package com.coffee.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration // 이 파일은 스프링에서 설정용 파일로 사용하겠습니다.
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        // 3000번 포트에서 GET부터 PATCH까지의 열거한 요청들을 모두 수락하겠습니다.
        registry.addMapping("/**") /* 모든 경로 허용  */
                .allowedOrigins("http://localhost:5173", "http://localhost:3000") /* react 포트 */
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH")  /* 허용할 메소드 */
                .allowCredentials(true) ; // 쿠키 전송 허용
    }
}
```



# 5. 14:20 ~ 15:10
#### \# 나중에 바로바로 이용하게 이런 작업하는 코드들 탬플릿으로 만들어서 저장하기



#### \# 추가로 공부하기



#### \# VisualStudioCode PDF (P.12)
- 저장하면 자동 줄맞춤
세팅에 fommat on save 검색
체크박스 체크하기



#### \# W3Schools에 javascript array map검색 (JavaScript Array map())
(자바스크립트의 배열 반복문)
문법 :

```java
array.map(function(currentValue, index, arr), thisValue)
```

function(), currentValue는 필수



# 6. 15:20 ~ 16:10
\* return위는 JS치는 곳
\* return안에는 JSX치는 곳 (마치 HTML처럼)
\* return안에 중괄호 안에 JS치는 곳
\* 화살표 함수 : () => {}
왼쪽에는 매개변수가 들어옴



#### \# FruitList.tsx 파일에 코드 추가

```typescript
import { useEffect, useState } from "react";
import type { Fruit } from "../types/Fruit";
import axios from "axios";
import { API_BASE_URL } from "../config/config";
import { Table } from "react-bootstrap";


function App() {
  const [fruitList, setFruitList] = useState<Fruit[]>([]); // 과일 여러개

  useEffect(() => {
    const fetchData = async () => {
      try {
        const url = `${API_BASE_URL}/fruit/list`;
        const response = await axios.get<Fruit[]>(url);
        setFruitList(response.data);

      } catch (error) {
        console.log(error);
      }
    }
    fetchData();
  }, []);

  return (
    <>
      <Table hover style={{ margin: '20px' }}>
        <thead>
          <tr>
            <th>아이디</th>
            <th>상품명</th>
            <th>단가</th>
          </tr>
        </thead>
        <tbody>
          {fruitList.map((fruit) =>
            <tr key={fruit.id}> {/* 데이터 베이스의 primary key같은 느낌 */}
              <td>{fruit.id}</td> {/* 그냥 적으면 문자열로 인식하니까 { }중괄호 사용해서 붙여넣기 */}
              <td>{fruit.name}</td>
              <td>{fruit.price.toLocaleString()}</td>
            </tr>
          )}
        </tbody>
      </Table >
    </>
  );
}

export default App;
```



# 7. 16:20 ~ 17:10
#### \# 03.기본기 다지기.txt (-> 홈 페이지 구현 절차)
폴더 생성 및 이미지 파일 첨부
- 이미지 위치 (윈도우)
```properties
productImageLocation=C:\\shop\\images
```

- 업로드 위치 (리눅스)
```properties
uploadPath=file:///C:/shop/images/
```

-> 이건 \가 3개인데 표기법이 원래 이러니까 그러려니 하기



#### \# 03.기본기 다지기.txt (-> WebConfig 클래스)
WebConfig 클래스
오버라이딩되어 있는 addResourceHandlers() 메소드는 외부에서 "/images/"으로 시작하는 요청이 들어 오면,
요청을 실제 서버의 로컬 경로와 연결시켜 주는 역할을 합니다.
예를 들어, 사용자가 http://localhost:9000/images/coffee1.jpg 와 같은 주소로 이미지를 요청하면,
Spring Boot는 이 요청을 실제 서버 내부의 폴더(예: C:/shop/images/coffee1.jpg) 에서 찾아 반환합니다.

(spring의 WebConfig.java 파일에 추가)

```java
import org.springframework.beans.factory.annotation.Value;

@Value("${uploadPath}")
private String uploadPath ; // file:///C:/shop/images/

import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;

@Override
public void addResourceHandlers(ResourceHandlerRegistry registry) {
	registry
			.addResourceHandler("/images/**") //**은 이 주소안의 모든 것을 의미
			.addResourceLocations(uploadPath);
}
```



#### \# 03.기본기 다지기.txt (-> MenuItems.tsx)

App.tsx 파일에서 넘겨 준 appName 프롭스를 함수의 매개 변수란에 작성합니다.
그리고, 네비게이션 바(Navbar) 컴포넌트 안에서 사이트의 로고나 제목 부분을 표시하기 위하여

<Navbar.Brand /> 코드를 추가합니다.

그리고, 홈 페이지로 이동하기 위하여 href='/'를 지정하도록 합니다.

App.tsx의 변수?하나를 MenuItems에게 넘겨주는것
일종의 부모자식 관계라고 생각하고

App.tsx가 부모 / MenuItems가 자식이라고 생각하면되는데
부모가 자식에게 넘겨주는 값을 property라고 함 (properties -> props)
(key=value 방식)
ex) spring.datasource.password=mysql (spring의 application.properties 파일안에)

```java
spring.datasource.password가 키(key)(식별자) / 값은 mysql
```

-> props는 mysql

ex)
(react - App.tsx)

```typescript
<MenuItems xxx={appName} /> // 수정
```

(react - MenuItems.tsx)

```typescript
function App({xxx}) { // 수정
console.log('xxx 프롭스 : ' + xxx); // 추가
```



# 8. 17:20 ~ 18:10
#### \# 이어서 작성 -> 03.기본기 다지기.txt (-> MenuItems.tsx)
(MenuItems.tsx에)

```typescript
<Navbar.Brand href='/'>{appName}</Navbar.Brand> // 추가

type MenuItemsProps = { // 추가
    appName: string;
};

function App({ appName }: MenuItemsProps) { // 수정
```

- (전체 코드 - App.tsx - 부모)
```typescript
import './App.css';

// 외부 컴포넌트 import하기
// import 컴포넌트이름 from '경로와 파일명';
import MenuItems from './ui/MenuItems';
import AppRoutes from './routes/AppRoutes';

function App(){
  const appName = "IT Academy Coffee Shop" ;

  return (
    <>
      <MenuItems appName={appName} />
      <AppRoutes />

      <footer className="bg-dark text-light text-center py-3 mt-5">
        <p>&copy; 2025 {appName}. All rights reserved.</p>
      </footer>
    </>
  );
}

export default App;
```

- (전체 코드 - MenuItems.tsx - 자식)
```typescript
import { NavDropdown, Navbar, Container, Nav } from "react-bootstrap";

import { useNavigate } from "react-router-dom";

type MenuItemsProps = {
    appName: string;
};

function App({ appName } : MenuItemsProps) {
   console.log('xxx 프롭스 : ' + appName);
   const navigate = useNavigate();

   return (
      <Navbar bg="dark" variant="dark" expand="lg">
         <Container>
            <Navbar.Brand href='/'>{appName}</Navbar.Brand>
            <Nav className="me-auto">
               <NavDropdown title={`기본 연습`}>
                  <NavDropdown.Item onClick={() => navigate(`/fruit`)}>과일 1개</NavDropdown.Item>
                  <NavDropdown.Item onClick={() => navigate(`/fruit/list`)}>과일 목록</NavDropdown.Item>
               </NavDropdown>
            </Nav>
         </Container>
      </Navbar >
   );
}

export default App;
```

* 부모에서 1개의 props를 자식에게 주면
자식은 2개의 행동을 해야 함 *
-> 매개변수 작성 / type 지정하기



#### \# props vs model
props 핸들링할때는

```typescript
type MenuItemsProps = {
    appName: string;
};
```

을 주로 쓰고

model 핸들링할때는 (나만의 타입)

```typescript
export interface Fruit {
    id: string
    name: string
    price: number
}
```

을 주로 씀



#### \# 내일 오면 할 일
03.기본기 다지기.txt (-> 홈 페이지 작성)
