# 1. 09:30 ~ 10:20
#### \# 프로젝트 정보
5월 14일 프로젝트 시작
6월 18일 프로젝트 발표
이번주 or 다음주 프로젝트 팀원 선정 및 자리 옮김

#### \# 새로운 파일 다운 받기
오픈 채팅에 zip파일 다운
- react_cafe / spring_cafe
여태까지 작성한 파일들

- react_coffee(04.회원) / spring_coffee(04.회원)
전에 이미 받았던 완성본 파일들
(추가 사항 있어서 새로 다운)

- 04.회원.txt / IT 관련 용어.pdf
전에 이미 받았던 파일
(추가 사항 있어서 새로 다운)

- JWT(이론).pdf
새로운 파일

#### \# 로그인 관련 개념
이전에는 쿠키와 세션이라는 개념을 가지고 이 사람이 로그인이 되었는지 아닌지 이야기를 했었는데
보안등과 같은 여러 이야기때문에
JWT라는 토큰을 주로 사용하게 됨

#### \# 쿠키(Cookie) (IT 관련 용어.pdf (P.48))
쿠키 : 클라이언트의 브라우저가 관리하는 저장소에 들어 있는 작은 데이터를 의미
Chrome, Edge, Safari 같은 브라우저 내부
사용자 PC 안에 존재하지만 실제로는 브라우저가 접근/관리
보안에 취약함
ex)
팝업창에서 일주일동안 보지 않기라는 체크 박스 / 오늘 하루 보지 않기 체크 박스같은거
체크하고 끄면 다음에 시작할때 팝업창이 안뜸
그 이유는 체크 박스에 체크를 하면 쿠키라는 정보로 저장이 됨

#### \# 세션(Session) (IT 관련 용어.pdf (P.49))
(클라이언트가 아니고) 웹 서버에 저장되는 사용자 상태 정보를 의미

- 서버 동작
```properties
JSESSIONID=hello 확인
```
→ 세션 저장소 조회
→ 사용자 정보 확인

ex)
네이버에 A가 로그인해서 접속했고 B도 로그인해서 접속한 상태
서버에 A가 요청했는지 B가 요청했는지 서버가 어떻게 구분할까

노트북에서 카톡 로그인하고 데스크탑으로 카톡 로그인하면 한명은 연결이 끊김
이렇게 한곳만 가능한 것도 있지만
여러 곳에서 동시에 로그인 가능한 것도 있음
이럴때 같은 아이디이지만 어떤기기로? 로그인했는지 구분할때 JSESSIONID로 구분함

- 세션 : 로그인하고 로그아웃하기까지의 그 상태의 정보?
세션이 맺어졌다.
세션의 유효기간 (ex-공공기관 로그인하고 몇분간 이용하지 않으면 자동으로 세션이 종료되는 경우가 있음)
세션의 유효기간이 없으면 보안에 취약함

- stateless : 로그인한 사람의 세션정도의 데이터가 들어가있을지언정 로그인후 뭘 했는지는 모름.
-> 로그아웃한지는 계속 제이세션아이디(JSESSIONID)를 서버에 보내줘서 로그인한 상태인걸 알게됨

#### \# 쿠키 / 세션 정리
쿠키 : 클라이언트쪽에 저장되는 개념
다음에 팝업 떠올리지 말기같은 개념

세션 : 웹서버에 저장되는 개념
이 사람이 접속해있는지 아는 용도로 쓰는 개념



# 2. 10:30 ~ 11:20
#### \# JWT - SPA구조 - MPA와 SPA 비교 (JWT(이론)(P.2))
- MPA(Multi Page Application)
페이지 이동마다 전체 HTML 다시 받음 / 화면이 계속 깜빡임 / 서버가 View까지 담당
예시) JSP + Spring MVC 구조
브라우저 → 서버 요청 → 서버가 HTML 생성 → 브라우저 전체 새로 고침

- SPA(Single Page Application)
페이지 전체 새로 고침 없음 / 속도 빠름 / 서버는 API만 제공
예시) React
브라우저에 index.html 한 번 로딩 -> 이후에는 JS가 화면만 동적으로 변경 -> 서버와는 JSON 데이터만 주고 받음

- 선생님 설명
페이지에 어떤 부분을 갱신할때
MPA방식은 페이지의 한 부분을 갱신하기 위해서 전체 웹페이지를 로딩함
-> 옛날 데스크탑 환경때문에 사용
SPA방식은 페이지의 한 부분을 갱신하기 위해서 그 한 부분을 로딩함
-> 요즘 모바일 환경때문에 사용
ajax (AJAX는 Asynchronous JavaScript and XML)(비동기식 자바스크립트와 XML)가 대표적인 SPA방식...?

#### \# JWT - SPA구조 (JWT(이론)(P.2))
단 하나의 HTML 페이지로 동작하는 웹 애플리케이션 구조

- SPA 프레임워크
React / Vue.js / Angular

- 주요 특징
페이지 - 1개
화면 전환 - JS가 담당
서버 역할 - 데이터(API) 제공
인증 방식 - JWT 사용 많음
장점 - 빠름, 앱 같은 UX
단점 - 초기 로딩 느릴 수 있음

#### \# JWT - Session 인증 방식 (JWT(이론)(P.4))
Session 방식은 서버가 로그인한 사용자의 정보를 직접 저장하는 인증 방식
똑같은 사용자가 서로 다른 PC에서 같은 계정으로 로그인하면
두 PC의 JSESSIONID가 달라서 이 JSESSIONID로 구분함

#### \# JWT - JWT 인증 방식 (JWT(이론)(P.5))
JWT 방식은 사용자가 로그인하면 서버에서 JSON Web Token을 발급해주는 인증 방식
JWT라는 토큰을 가지고 구분하는 방식
사용자가 요청을 보낼때 마다 토큰가지고 구분함

#### \# Stateful vs Stateless (JWT(이론)(P.8))
따로 적기

#### \# JWT - JWT의 구조 (JWT(이론)(P.11))
Token을 직접 만드는 것이 아니고 Java의 라이브러리가 있어서
Java로 코딩만 하면 됨

(아래 부분은 나중에 또 이야기 해주신다고 하심 - 간단히 보기)
Header - 토큰 타입, 서명 알고리즘 정보(어떤 알고리즘으로 서명했나?)
Payload - 실제 데이터(사용자 정보, 권한 등), 여기서 Claims가 들어갑니다
Signature - 위변조 방지용 서명으로 SECRET_KEY(문자열 변수)로 암호화해서 생성해 줍니다.

ex)

```java
eyJhbGciOiJIUzI1NiJ9.eyJyb2xlIjoiVVNFUiJ9.9TtnHdAWS6XX9iLYOHaFKVJmw3_JAJqg3yHT98VrEpM
```
점을 기준으로 [ Header ] . [ Payload ] . [ Signature ]으로 구성
[eyJhbGciOiJIUzI1NiJ9] / [eyJyb2xlIjoiVVNFUiJ9] / [9TtnHdAWS6XX9iLYOHaFKVJmw3_JAJqg3yHT98VrEpM]

#### \# 로그인 - axiosInstance.tsx (리액트 시작) (04.회원.txt)
(Visual Studio Code(react) - src - api폴더 생성 후 - axiosInstance.tsx 파일 생성)

```java
axiosInstance.tsx
```
로그인 인증(JWT)을 자동으로 처리해주는 커스텀 axios 설정 파일을 작성합니다.
이 파일은, API 요청할 때마다 토큰을 붙이고 인증 실패(401)하면 자동 로그 아웃까지 처리해주는 구조입니다.

- 코드 작성 : 해당 파일 참조 (오늘 받은 react_coffee(04.회원) - src - api - axiosInstance.tsx 참고하기)


(아래코드 복붙하기)


(실습)
```typescript
// 로그인 인증(JWT)을 자동으로 처리해주는 커스텀 axios 설정 파일
// get방식 post방식 등을 계속해서 요청하지 않고 자동으로 하게 만드는 파일

// 즉, API 요청할 때마다 토큰 붙이고
// 인증 실패(401)하면 자동 로그 아웃까지 처리해주는 구조입니다.
// 전체 과정 : 토큰확인 - 토큰이 없거나 올바르지 않은 토큰일 경우 삭제 후 로그인 페이지로 보내서 새로운 토큰 생성 유도

import axios from "axios";
import { API_BASE_URL } from "../config/config";

// withCredentials: true 항목은 세션 방식 설정이므로 jwt를 사용하면 삭제하도록 합니다.
const axiosInstance = axios.create({
    baseURL: API_BASE_URL
});

// 인터셉터(interceptor) : 요청(Request)이나 응답(Response)을 가로 채서 공통 로직을 처리하는 기능입니다.
// 요청(Request) : ~하기 전에 가로채기 (사전)
// 응답(Response) : ~한 후에 가로채기 (사후)
// 요청을 보내기 전에 인터셉터가 자동으로 JWT 붙이기 (사전에 가로채기)
// 토큰을 확인하는 과정
axiosInstance.interceptors.request.use(
    (config) => {
        const token = localStorage.getItem("accessToken"); // 로컬 저장소에 accessToken이라는 이름으로 만들어 놓음
        console.log("interceptors.request 토큰 확인 : ", token);

        if (token) { // token가 undefined일 수 있으므로...
            config.headers = config.headers || {};
            // Bearer 단어 대소문자 주의 바람
            config.headers.Authorization = `Bearer ${token}`;
        }

        return config;
    },
    (error) => Promise.reject(error)
);

// 응답 처리 : 401 에러 발생 시 자동 로그 아웃 처리 (사후에 가로채기)
// 401에러 : 인증이 필요하거나 실패했다는 의미
// 올바르지 않아서 401에러가 생긴? 토큰 삭제 후 로그인 페이지로 유도하기
axiosInstance.interceptors.response.use(
    (response) => response,
    (error) => {

        const isLoginRequest = error.config?.url?.includes("/member/login");

        if (error.response?.status === 401 && !isLoginRequest) {
            localStorage.removeItem("accessToken");

            // window.location.href 사용시 React Router 사용 중이면
            // 페이지 전체 리로드 발생할 수 있으므로 replace() 메소드 사용 바람
            window.location.replace("/member/login");
        }

        return Promise.reject(error);
    }
);

export default axiosInstance;
```

#### \# 로컬 스토리지(localStorage)확인하는 법
크롬 - F12 - Application - Storage - Local storage

#### \# 로그인 - User.ts (리액트) (04.회원.txt)
(Visual Studio Code(react) - src - types폴더에 - User.ts 파일 생성)
파일 : User.ts
- 리액트 앱 내부에서 사용하는 사용자 모델'을 처리해주는 타입 스크립트 인터페이스를 작성합니다.

\* 여기서 export는 Java의 public같은 것 - 모든 곳에서 사용해야하니까

코드 작성

```typescript
export interface User {
    id: number;
    name: string;
    email: string;
    role: "USER" | "ADMIN";
}
```
- LoginResponse 내용을 User.ts 파일 내에 작성하도록 합니다.
'서버가 로그인 시 내려 주는 응답'을 처리해주는 타입 스크립트 인터페이스를 작성합니다.

코드 작성

```typescript
export interface LoginResponse extends User {
    accessToken: string;
}
```



# 3. 11:30 ~ 12:20
#### \# MIME type (IT 관련 용어 PDF (P.50))
MIME type (Content type이라고도 함)
파일이나 데이터의 "종류(형식)"를 나타내는 표준 문자열로써,
이 데이터가 어떤 형식인지 알려주는 역할
json으로 줄때는 application/json 이런 양식을 적어서 줘야한다는 것을 약속함

- 기본 구조
type/subtype

ex)
text/html (메모장으로 볼 수 있는 html형식이구나) → 브라우저가 웹페이지로 렌더링
application/json → 데이터로 처리 (JS에서 사용)
image/jpeg (그림판으로 볼 수 있는 jpef형식이구나) → 이미지로 표시
application/xml

#### \# http로 MIME type을 이용해서 보낼때
header부분과 body부분이 있음

```java
const config = {
	headers: {
		"Content-Type": "application/json"
	}
};
```
header 부분에 어떤 것을 넘길때 이렇게 작성함



# 4. 12:30 ~ 13:20
#### \# 로그인 페이지 작성 - LoginPage.tsx (리액트) (04.회원.txt)
(Visual Studio Code(react) - src - pages폴더에 - LoginPage.tsx 파일 생성)
코드 작성 : LoginPage01.txt 파일 참조

- 기타 코드들
```properties
sm={}는 grid에서 구역나누기 {}안에 숫자 입력하면 됨
```
컬럼에서 스타일 입히려면 className="" 하면 됨
Control안에 required는 필수사항 표시임

```properties
onChange={(e)=> setEmail(e.target.value)}
```
-> change할때 e라는 change가 일어나는 객체에 target.value라는 값으로 Email값을 set한다.

```typescript
<Form.Group>은 양식 1개를 의미
```
- 전개 연산자 (React 교안 (P.41))
```typescript
// 서버의 응답을 전개 연산자로 처리합니다.
const { accessToken, ...userData } = response.data;
```
1) 개념
나열형 자료(배열, 객체 등)를 개별로 추출하거나 연결하고자 할 때 사용하는 연산자

2) 예시

```java
const numbers = [1, 2, 3, 4, 5, 6];

const [one, two, ...rest] = numbers;
// one에 숫자 1이, two에 숫자 2의 값이 저장됩니다.
// 변수 rest에 나머지 항목 [3, 4, 5, 6]이 저장됩니다.
```
3) 장점
코드 간결성 : props와 state 관리가 깔끔해짐.
유연성 : 객체와 배열을 쉽게 병합하거나 수정 가능.
가독성 : 데이터 구조를 직관적으로 다룰 수 있음.

4) 사용 예시

```typescript
// 서버의 응답을 전개 연산자로 처리합니다.
// accessToken는 JWT, userData는 User.ts으로 구성된 객체
const { accessToken, ...userData } = response.data;
```
원래 response.data에는 id, name, email, role, accessToken이 들어있는데

```typescript
const accessToken에는 response.data에서 온 accessToken값이 들어가고 (accessToken은 JWT임)
const userData에는 response.data에서 온 나머지 값들인 id, name, email, role값들이 들어감
```
- 코드 작성 실습
```typescript
import { useState } from "react";
import { Alert, Button, Card, Col, Container, Form, Row } from "react-bootstrap";
import { Link, useNavigate } from "react-router-dom";

import axios from "../api/axiosInstance.tsx";
import type { LoginResponse, User } from "../types/User";

interface Props {
    // onLogin 프롭스는 User 형식으로 매개 변수를 받고, 반환 타입이 없습니다.
    onLogin: (user: User) => void;
}

function App({ onLogin }: Props) {
    // 이 문서내에서 바뀔 소지가 있는 것들은 state로 만들어 관리 할 수 있음
    // props는 부모에게서! 받은거고 / state는 자신의 문서 내에서! 있는 것들이고
    // 로그인과 관련된 state
    const [email, setEmail] = useState('');
    const [password, setPassword] = useState('');

    // 에러 관련 메시지
    const [errors, setErrors] = useState('');

    const navigate = useNavigate();

    const handleLogin = async (event: React.SubmitEvent) => {
        event.preventDefault();
        console.log('로그인 시도중입니다.');

        try {
            const url = '/member/login';
            const params = { email, password }; // 파라미터
            const config = {
                headers: {
                    "Content-Type": "application/json"
                }
            };

            const response = await axios.post<LoginResponse>(url, params, config);

            console.log('응답 데이터 : \n' + response.data);

            // 서버의 응답을 전개 연산자로 처리합니다.
            // accessToken는 JWT, userData는 User.ts으로 구성된 객체
            const { accessToken, ...userData } = response.data;

            localStorage.setItem("accessToken", accessToken);

            console.log('로그인 성공 사용자 : ' + userData);


            if (onLogin) {
                onLogin(userData);

                // userData는 자바스크립트 객체여서 문자열로 바꿔줘야 함
                // JSON.stringify 함수는 JavaScript 객체를 JSON 문자열로 변환해 줍니다.
                localStorage.setItem("user", JSON.stringify(userData));
            }

            navigate("/");

        } catch (error: any) {
            if (error.response) {
                setErrors(error.response.data.message || "로그인 실패");
            } else {
                setErrors("Server Error");
            }
        }
    };

    console.log('자바스크립트 코딩 영역');

    return (
        <Container fluid className="d-flex justify-content-center align-items-center" style={{ height: "70vh" }}>
            <Row className="w-100 justify-content-center">
                <Col md={6} sm={10}>
                    <Card>
                        <Card.Body>
                            <h2 className="text-center mb-4">로그인</h2>

                            {errors && <Alert variant="danger">{errors}</Alert>}

                            <Form onSubmit={handleLogin}>
                                <Form.Group as={Row} className="mb-3 align-items-center">
                                    <Form.Label column sm={3} className="text-end fw-bold text-primary">
                                        이메일
                                    </Form.Label>
                                    <Col sm={9}>
                                        <Form.Control
                                            type="email"
                                            placeholder="이메일을 입력해 주세요."
                                            value={email}
                                            onChange={(e) => setEmail(e.target.value)}
                                            required
                                        />
                                    </Col>
                                </Form.Group>

                                <Form.Group as={Row} className="mb-3 align-items-center">
                                    <Form.Label column sm={3} className="text-end fw-bold text-primary">
                                        비밀 번호
                                    </Form.Label>
                                    <Col sm={9}>
                                        <Form.Control
                                            type="password"
                                            placeholder="비밀 번호을 입력해 주세요."
                                            value={password}
                                            onChange={(e) => setPassword(e.target.value)}
                                            required
                                        />
                                    </Col>
                                </Form.Group>

                                <Row className="g-2">
                                    <Col xs={8}>
                                        <Button variant="primary" type="submit" className="w-100">
                                            로그인
                                        </Button>
                                    </Col>
                                    <Col xs={4}>
                                        <Link to="/member/signup" className="btn btn-outline-secondary w-100">
                                            회원 가입
                                        </Link>
                                    </Col>
                                </Row>
                            </Form>

                        </Card.Body>
                    </Card>
                </Col>
            </Row>
        </Container>
    );
};

export default App;
```



# 5. 14:20 ~ 15:10
#### \# 라우팅 설정 : AppRouters.tsx (리액트) (04.회원.txt)
(AppRoutes.tsx에 코드 추가)

```typescript
import LoginPage from './../pages/LoginPage';
import type { User } from "../types/User";

interface AppProps {
  user: User | null ;
  handleLoginSuccess : (userData: User) => void ;
}

function App({ user, handleLoginSuccess }: AppProps) {

<Route path='/member/login' element={<LoginPage onLogin={handleLoginSuccess} />} />
```

#### \# 메뉴 목록 구성 : MenuItems.tsx (리액트) (04.회원.txt)
풀다운 메뉴를 구성하는 MenuItems.tsx 파일에 다음과 같이 코딩합니다.
로그인 여부에 따른 분기 처리를 해야 하므로 "App 함수형 컴포넌트 정의부"에 관련 프롭스를 추가하도록 합니다.

주요 변경 사항
	user, handleLogout 프롭스 추가됨
	renderMenu 함수 추가

- 코드 작성 : MenuItems01.txt 파일 참조
```typescript
import { NavDropdown, Navbar, Container, Nav } from "react-bootstrap";

import { useNavigate } from "react-router-dom";
import type { User } from "../types/User";

type MenuItemsProps = {
   appName: string;
   user: User | null; // 이 데이터는 null일 수도 있습니다.
   handleLogout: (event: React.MouseEvent<HTMLElement>) => void;
};

function App({ appName, user, handleLogout }: MenuItemsProps) {
   console.log('xxx 프롭스 : ' + appName);
   const navigate = useNavigate();

   // user 프롭스를 사용하여 상단에 보이는 풀다운 메뉴를 적절히 분기 처리합니다.
   const renderMenu = () => {
      switch (user?.role) {
         case 'ADMIN':
            return (
               <>
                  <Nav.Link onClick={() => navigate(`/product/insert`)}>상품 등록</Nav.Link>
                  {/* 관리자는 모든 사람의 주문 내역 확인 */}
                  <Nav.Link onClick={() => navigate(`/order/list`)}>주문 내역</Nav.Link>
                  <Nav.Link onClick={handleLogout}>로그 아웃</Nav.Link>
               </>
            );
         case 'USER':
            return (
               <>
                  <Nav.Link onClick={() => navigate(`/cart/list`)}>장바구니</Nav.Link>
                  <Nav.Link onClick={() => navigate(`/order/list`)}>주문 내역</Nav.Link>
                  <Nav.Link onClick={handleLogout}>로그 아웃</Nav.Link>
               </>
            );
         default:
            return (
               <>
                  <Nav.Link onClick={() => navigate(`/member/login`)}>로그인</Nav.Link>
                  <Nav.Link onClick={() => navigate(`/member/signup`)}>회원 가입</Nav.Link>
               </>
            );
      }
   };

   return (
      <Navbar bg="dark" variant="dark" expand="lg">
         <Container>
			{/* 매개변수로 받은 프롭 사용하기 */}
            <Navbar.Brand href='/'>{appName}</Navbar.Brand>
            <Nav className="me-auto">
               {/* 하이퍼링크 : Nav.Link는 다른 페이지로 이동할 때 사용됩니다.  */}
               <Nav.Link onClick={() => navigate(`/product/list`)}>상품 보기</Nav.Link>

               {renderMenu()}

               <NavDropdown title={`기본 연습`}>
                  <NavDropdown.Item onClick={() => navigate(`/fruit`)}>과일 1개</NavDropdown.Item>
                  <NavDropdown.Item onClick={() => navigate(`/fruit/list`)}>과일 목록</NavDropdown.Item>
               </NavDropdown>
            </Nav>
         </Container>
      </Navbar >
   );
}

export default App;
```



# 6. 15:20 ~ 16:10
#### \# 앱 메인 파일 : src/App.tsx (리액트 종료) (04.회원.txt)
- 코드 작성 : App02.txt 파일 참조
(리액트 부분 완성)

#### \# index.css : 이 파일은 확인 요망 (리액트) (04.회원.txt)
이 항목은 무시하기

#### \# 직렬화 문제 해결 (스프링 시작) (04.회원.txt)
1) pom.xml
객체를 네트워크를 통하여 다른 컴퓨터로 보내고자 하는 경우, 전송 가능하거나 저장 가능한 형태(바이트)로 바꾸는 작업을 직렬화라고 합니다.
Spring Security 사용시 Entity에 LocalDate 타입의 변수가 들어 있는 경우, JSON 타입으로 변환시 직렬화 문제가 생깁니다.

이 문제를 미연에 방지하는 의존성 추가

```html
<dependency>
   <groupId>com.fasterxml.jackson.datatype</groupId>
   <artifactId>jackson-datatype-jsr310</artifactId>
</dependency>
```
2) 엔터티(회원)(entity - Member에 추가)

```java
private LocalDate regdate ; // 등록 일자
```
이 구문 위에

```java
@JsonFormat(pattern = "yyyy-MM-dd")
```
이 구문 추가

#### \# pom.xml / jwt와 관련된 의존성을 3개 추가합니다.  (스프링 시작) (04.회원.txt)
- 코드 작성
```html
<!-- JWT API -->
<dependency>
	<groupId>io.jsonwebtoken</groupId>
	<artifactId>jjwt-api</artifactId>
	<version>0.11.5</version>
</dependency>

<!-- JWT 구현체 -->
<dependency>
	<groupId>io.jsonwebtoken</groupId>
	<artifactId>jjwt-impl</artifactId>
	<version>0.11.5</version>
	<scope>runtime</scope>
</dependency>

<!-- JSON 처리 (Jackson 연동) -->
<dependency>
	<groupId>io.jsonwebtoken</groupId>
	<artifactId>jjwt-jackson</artifactId>
	<version>0.11.5</version>
	<scope>runtime</scope>
</dependency>
```



# 7. 16:20 ~ 17:10
#### \# Bean 객체
- 같은 의미의 다른 표현
dto (Data Transfer Object) / vo (Value Object)

#### \# LoginDto 클래스 신규 작성  (스프링 시작) (04.회원.txt)
클라이언트에서 온 로그인 데이터를 담는 전용 클래스입니다.
(com.coffee - dto폴더 생성 후 - LoginDto 클래스 생성 후 작성)

(코드 작성)

```java
package com.coffee.dto;

import lombok.Getter;
import lombok.Setter;

// dto(Data Transfer Object)
// 클라이언트에서 넘겨진 로그인 정보를 저장하기 위한 자바 클래스
@Getter @Setter
public class LoginDto {
    private String email ;
    private String password ;
}
```

#### \# Claims 인터페이스 (JWT(이론)(P.15))
JWT 안에 담기는 사용자 정보(누가 주인, 어떤 권한)의 데이터 조각을 의미
JWT 안에 들어 있는 '사용자 정보나 토큰 정보'를 담는 데이터
이 사용자가 누구냐 이 사용자가 어디까지 할 수 있는지

- Registered Claims
iss 토큰 발급자(Issuer)
sub 토큰 주제(Subject, 보통 사용자 ID)
aud 토큰 대상자(Audience)
exp 만료 시간(Expiration)
iat 발급 시간(Issued At)
nbf 유효 시작 시간(Not Before)

#### \# 토큰 관리 클래스 신규 작성 : JwtTokenProvider(config 패키지)  (스프링) (04.회원.txt)
(config 폴더에 JwtTokenProvider 클래스 생성)
JWT를 생성하고, 해석하고, 검증하는 기능을 합니다.
JWT 생성, 검증 기능 담당자 클래스
- getSigningKey() 메소드 : 위조 방지를 위한 서명
- createToken(Member member) 메소드 : JWT 생성
- getClaims(String token) 메소드 : 토큰을 사용하여 Claims 정보 추출
- getEmail(String token) 메소드 : 토큰에서 email 추출
- public boolean validateToken(String token) : 토큰 유효성 검사

(코드)

```java
package com.coffee.config;

import com.coffee.entity.Member;
import io.jsonwebtoken.*;
import io.jsonwebtoken.security.Keys;
import org.springframework.stereotype.Component;

import java.security.Key;
import java.util.Date;
import java.util.Map;

@Component
public class JwtTokenProvider { // JWT 생성, 검증 기능 담당자 클래스
    // 이 값도 사실 노출 방지를 위하여 어딘가에 숨겨야 함
    private final String SECRET_KEY =
            "my-secret-key-my-secret-key-my-secret-key";

    private Key getSigningKey(){ // 위조 방지를 위한 서명
        return Keys.hmacShaKeyFor(SECRET_KEY.getBytes());
    }

    private final long EXPIRATION = 1000 * 60 * 60 ; // 만료 1 시간

    // MemberController 클래스에서 인증 성공한 사용자를 위하여 로그인 증명서(토큰)를 발급하는 데 사용될 예정입니다.
    public String createToken(Member member){ // 매개 변수 : 토큰 안에 사용자 식별값 저장
        return Jwts.builder()
                .setSubject(member.getEmail()) // 토큰 주인
                .setIssuedAt(new Date()) // 토큰 발급 시간
                .setExpiration(new Date(System.currentTimeMillis() + EXPIRATION)) // 토큰 만료 시간


                .signWith(getSigningKey(), SignatureAlgorithm.HS256)
                .setClaims(Map.of("role", member.getRole().name())) // 권한 정보
                .compact(); // 최종 문자열 생성하기
    }

    public String getEmail(String token){ // JWT 토큰에서 사용자 정보 가져 오기
        return this.getClaims(token).getSubject() ;
    }

    public Claims getClaims(String token){
        return Jwts.parserBuilder()
                .setSigningKey(getSigningKey())
                .build()
                .parseClaimsJws(token)
                .getBody();
    }

    public boolean validateToken(String token){ // JWT 토큰 유효성 검사
        try {
            Jwts.parserBuilder()
                    .setSigningKey(getSigningKey())
                    .build()
                    .parseClaimsJws(token);
            return true;

        } catch (ExpiredJwtException e) {
            System.out.println("토큰 만료됨");

        } catch (io.jsonwebtoken.security.SecurityException | MalformedJwtException e) {
            System.out.println("토큰 서명/형식 오류");

        } catch (Exception e) {
            System.out.println("기타 토큰 오류");
        }
        return false ;
    }

}
```



# 8. 17:20 ~ 18:10
