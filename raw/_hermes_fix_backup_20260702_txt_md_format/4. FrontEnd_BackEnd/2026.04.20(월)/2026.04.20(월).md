# 1. 09:30 ~ 10:20
#### \# 주문 목록(OrderLIst.tsx) (리액트) (07.주문.txt)
(pages - OrderLIst 파일에 코드 추가)
	orders 자바 스크립트 배열과 map() 함수를 사용하여 목록을 보여 줍니다.
주의 : 컬렉션 map과는 다른 단순 반복 나열하는 함수임 (key가 필요함)

- 코드 작성 : OrderList02.txt 파일 참조
(코드 복붙)

```typescript
<Container className="my-4">
	<h1 className="my-4">주문 내역</h1>
	{orders.length === 0 ? (
		<Alert variant="secondary">주문 내역이 없습니다.</Alert>
	) : (
		<Row>
			{orders.map((bean) => (
				<Col key={bean.orderId} md={6} className="mb-4">
					<Card
						className="h-100 shadow-sm"
						style={{
							border: '2px solid #495057'
						}}
					>
						<Card.Body>
							<div className="d-flex justify-content-between">
								<Card.Title>주문 번호 : {bean.orderId}</Card.Title>
								<small className="text-muted">{bean.orderDate}</small>
							</div>

							<Card.Text className="text-start">
								상태 : <strong>{bean.status}</strong>
							</Card.Text>
							<Card.Text className="text-start">
								<ul>
									{bean.orderItems.map((item, index) => (
										<li key={index}>
											- {item.productName} ({item.quantity}개)
										</li>
									))}
								</ul>
							</Card.Text>
						</Card.Body>

					</Card>
				</Col>
			))}
		</Row>
	)}
</Container>
```

#### \# 테스트 시나리오 (리액트) (07.주문.txt)
					공통 사항
										최신 주문 목록이 먼저 조회되어야 합니다. (송장 번호 역순)
					관리자 로그인
										모든 사용자들의 주문 정보가 조회되어야 합니다.
					일반인 로그인
										사용자 본인의 주문 정보만 조회되어야 합니다.

\* 이런 시나리오를 잘 짜야 함

#### \# 상태 변경 버튼 생성 (리액트) (07.주문.txt)
(pages - OrderLIst 파일에 코드 추가)
(시나리오를 짜고 그에 맞춰 코드를 작성하기)
(코드 작성 : OrderList03.txt 파일 참조)

우선 주문의 상태 및 버튼 동작에 대하여 우선 살펴 보겠습니다.

※ 주문 상태
	주문을 생성하면 기본 상태는 주문 대기(PENDING) 입니다.
	배송이 아직 이루어지지 않았음을 의미합니다.
	관리자가 주문 '완료' 버튼을 클릭하면 상태가 COMPLETED로 변경됩니다.
	사용자가 주문 '취소' 버튼을 클릭하면 주문 내역이 삭제되며 상태는 CANCELED로 변경됩니다.
(스프링의 constant 폴더에 enum인 OrderStatus을 바꿈?))

공통 사항
	화면에는 주문 대기(PENDING) 상태인 항목만 표시됩니다.
	주문 '취소' 버튼 클릭 시 상태 변경과 동시에 화면이 갱신됩니다.
(화면 갱신을 하는 것은 다른 말로 useEffect 함수가 동작한 것이고 이것을 랜더링한다라고 표현)

관리자 모드
	주문 '완료', 주문 '취소' 버튼을 모두 제공합니다.
	주문 '완료' 버튼 클릭 시 PENDING → COMPLETED로 상태가 변경되며 화면이 즉시 갱신됩니다.

일반 사용자 모드
	주문 '취소' 버튼만 제공합니다.
	클릭 시 주문 내역이 삭제되고 화면이 갱신됩니다.

1) makeStatusButton 함수 생성
	주문 '완료' & 주문 '취소' 버튼을 생성해주는 함수입니다.
- 코드 추가
```java
const makeStatusButton = (bean: Order) => {
	if (user?.role !== "ADMIN" && user?.role !== "USER") return null;

	return (
		<div className="d-flex align-items-center">
			{/* 관리자일 때 사용자 이름 표시 */}
			{user?.role === 'ADMIN' && (
				<span
					className="me-3 px-3 py-1 border rounded fw-bold text-primary"
					style={{
						borderColor: '#0d6efd',   // 테두리 색상 (Bootstrap primary)
						backgroundColor: 'transparent', // 내부 채우기 제거
						fontSize: '0.9rem',
					}}
				>
					👤 {bean.name}
				</span>
			)}
			{/* `완료` 버튼은 관리자만 볼수 있습니다. */}
			{user?.role === 'ADMIN' && (
				<Button
					variant="success"
					size="sm"
					className="me-2">
					완료
				</Button>
			)}

			<Button
				variant="danger"
				size="sm"
				className="me-2">
				취소
			</Button>
		</div>
	);
};
```
2) <Card.Body> 태그의 끝단
	주문 상태 변경 버튼 생성을 호출하는 {makeStatusButton(bean)}라는 코드를 작성합니다.
- 코드 추가
{/* 주문 상태 변경 버튼 생성 */}
{makeStatusButton(bean)}

#### \# 테스트 시나리오 (리액트) (07.주문.txt)
관리자 모드로 로그인
	주문 내역에 모든 사용자의 주문 내역 정보가 각각 사용자의 이름과 함께 표시됩니다.
	'완료' 버튼과 '취소' 버튼 2가지가 모두 보여야 합니다.

일반인 모드로 로그인
	주문 내역에 나 자신의 주문 내역 정보가 표시됩니다.
	'취소' 버튼이 보여야 합니다.



# 2. 10:30 ~ 11:20
#### \# 제07장-03. 주문 상품 완료 처리

#### \# 주문 목록(완료 처리) (리액트) (07.주문.txt)
(pages - OrderLIst 파일에 코드 추가)
	1) `완료` 버튼 클릭에 대한 처리를 위하여 changeCompleted 함수를 구현합니다.
	이 함수는 `PENDING` 모드를 `COMPLETED` 모드로 변경하는 역할을 합니다.
\* 백엔드에 요청하고 그 요청에 따라 DB에서 상태를 update해줘야 함
(makeStatusButton 함수 안에)

```java
const changeCompleted = async (newStatus: string) => {
	try {
		const url = `${API_BASE_URL}/order/update/${bean.orderId}?status=${newStatus}`;
		await customAxios.put(url);

		alert(`송장 번호 ${bean.orderId}의 주문 상태가 ${newStatus}으로 변경이 되었습니다.`);

		setOrders((previous) =>
			// order들을 하나하나 체크하면서
			// order.orderId !== bean.orderId 인것들만 챙기고 나머지는 버림
			previous.filter((order) => order.orderId !== bean.orderId)
		);

	} catch (error) {
		console.log(error);
		alert('상태 변경(주문 완료)에 실패하였습니다.');
	};

};

	2) `완료` 버튼에 onClick 이벤트를 구현합니다.
	<Button ... onClick={() => changeCompleted('COMPLETED')}>
		완료
	</Button>
onClick={() => changeCompleted('COMPLETED')}>
```
완료

- 코드 작성 : OrderList04.txt 파일 참조

#### \# 리포지터리(OrderRepository) (스프링) (07.주문.txt)
(repository - OrderRepository 파일에 코드 추가)

```java
	int updateOrderStatus(@Param("orderId") Long orderId, @Param("status") OrderStatus status);
	특정 주문에 대하여 주문의 상태를 '주문 완료(COMPLETED)'로 변경합니다.
	쿼리 메소드대신 @Query 어노테이션을 사용한 예시입니다.(sql문법 대신 JPQL문법을 사용합니다.)
	(JPQL문법은 JPA에서 지원하는 SQL문법하고 비슷한 문법) (따로 시간내서 공부는 X, 그때그때 AI한테 물어보기)
```

* repository에 들어있는 저런 메소드들을 쿼리 메소드라고 함
조건식이 길어지면 길이가 엄청 길어지는 단점이 있음
따라서 조건식이 간단할때 쓰는게 쿼리 메소드임
조건식이 길면 쿼리 어노테이션을 사용함

- 코드 추가
```typescript
// 특정 주문에 대하여 주문의 상태를 '주문 완료(COMPLETED)'로 변경합니다.
// 쿼리 메소드대신 @Query 어노테이션 사용 예시 : sql 대신 JPQL
// 주의 사항
// 1. 테이블 이름 대신 Entity 이름을 명시 (중요!!!)
// 2. 대소문자 구분합니다. (중요!!!)
@Modifying // 이 쿼리는 select 구문이 아니고, 데이터 변경을 위한 쿼리입니다.
@Transactional // import jakarta.transaction.Transactional; // 롤백할 수도 있으니까
@Query("update Order o set o.orderStatus = :status where o.id = :orderId")
int updateOrderStatus(@Param("orderId") Long orderId, @Param("status") OrderStatus status);
```



# 3. 11:30 ~ 12:20
#### \# 서비스(OrderService) (스프링) (07.주문.txt)
(service - OrderService 파일에 코드 추가)

```java
	public String updateOrderStatus(Long orderId, OrderStatus newStatus)
```

	관리자가 수행하는 주문된 상품에 대한 `완료` 처리 기능
	특정 주문 ID를 받아서, 해당 주문의 상태를 확인하고, 조건에 따라 상태를 변경하고, 그 결과를 메시지로 반환하는 비즈니스 로직 담당입니다.
- 코드 추가
```java
@Transactional
public String updateOrderStatus(Long orderId, OrderStatus newStatus){
	final String message = "해당 주문이 존재하지 않습니다. 주문 Id : " + orderId ;
	Order order = orderRepository.findById(orderId)
			.orElseThrow(() -> new IllegalArgumentException(message));

	if (order.getOrderStatus() == OrderStatus.CANCELED){
		throw new IllegalStateException("취소된 주문은 상태를 변경할 수 없습니다.");
	}

	order.setOrderStatus(newStatus);

	return "송장 번호 " + orderId + "의 주문 상태가 " + newStatus + "(으)로 변경되었습니다." ;
}
```

#### \# 컨트롤러(OrderController) (스프링) (07.주문.txt)
(controller - OrderController 파일에 코드 추가)

```java
	public ResponseEntity<String> statusChange(@PathVariable Long orderId, @RequestParam OrderStatus status)
```

	관리자가 수행하는 주문된 상품에 대하여 `완료` 버튼을 클릭했을 때 처리해주는 컨트롤러 메소드입니다.
- 코드 추가
```java
@PutMapping("/update/{orderId}")
public ResponseEntity<String> statusChange(@PathVariable Long orderId,
										   @RequestParam OrderStatus status){
	System.out.println("수정할 항목의 아이디 : " + orderId);
	System.out.println("변경하고자 하는 주문 상태 : " + status);

	String message = orderService.updateOrderStatus(orderId, status);

	return ResponseEntity.ok(message) ;
}
```

#### \# 테스트 시나리오 (MySQL) (07.주문.txt)
(웹페이지)
주문 조회 sql 구문을 사용하여 현재 주문 정보를 확인합니다.
관리자 모드로 로그인 합니다.
특정 주문에 대하여 '완료' 버튼을 클릭해 봅니다.

(MySQL Workbench)
1)주문 조회 sql 구문을 사용하여 현재 주문 정보를 확인합니다.
	이후 실습을 수행하기 위하여 다음 sql 구문을 실습합니다.

- 코드 작성
-- 테스트 시나리오(주문 조회 sql 구문) (07.주문.txt)

```sql
select m.member_id, m.name, o.order_id, op.product_id, op.quantity, p.name, p.price, o.order_status
from ((members m join orders o
```
on m.member_id = o.member_id) join order_products op
on o.order_id = op.order_id) join products p
on op.product_id = p.product_id

```java
order by order_id desc, product_id asc ;
```
2) 일반 사용자로 로그인하여 '취소' 테스트하기

-- 완료된 상품들을 다시 주문 내역에 넣는걸로 되돌리기

```java
update orders set order_status = 'PENDING' ;

commit;
```

#### \# 제07장-04. 주문 상품 취소하기

#### \# 주문 목록(상품 취소) (리액트) (07.주문.txt)
(pages - OrderLIst 파일에 코드 추가)
	1) `취소` 버튼 클릭에 대한 처리를 위하여orderCancel() 함수를 다음과 같이 정의합니다.
	이 함수는 `대기 상태(PENDING)`인 주문 내역을 주문 취소(CANCELED) 상태로 변경해 줍니다.
(makeStatusButton 함수 안에)

```java
const orderCancel = async () => {
	try {
		const url = `${API_BASE_URL}/order/delete/${bean.orderId}`;
		await customAxios.delete(url);

		alert(`송장 번호 ${bean.orderId}의 주문이 취소되었습니다.`);

		setOrders((previous) =>
			// order들을 하나하나 체크하면서
			// order.orderId !== bean.orderId 인것들만 챙기고 나머지는 버림
			previous.filter((order) => order.orderId !== bean.orderId)
		);


	} catch (error) {
		console.log(error);
		alert('주문 취소를 실패하였습니다.');
	};
};

	2) `취소` 버튼에 onClick 이벤트를 구현합니다.
	<Button ... onClick={() => orderCancel()}>
		취소
	</Button>
onClick={() => orderCancel()}
>
	취소
```
- 코드 작성 : OrderList04.txt 파일 참조
\* Card.text 태그를 div 태그로 바꾸기

```html
<div className="text-start">
	<ul>
		{bean.orderItems.map((item, index) => (
			<li key={index}>
				- {item.productName} ({item.quantity}개)
			</li>
		))}
	</ul>
</div>
```



# 4. 12:30 ~ 13:20
#### \# 서비스(ProductService) (스프링) (07.주문.txt)
(service - ProductService 파일에 코드 추가)
(이미 코드 작성되어 있음)
	이미 구현되어 있으므로 코딩할 내역이 없습니다.
	save() 메소드는 Product 객체(상품 정보)를 데이터베이스에 저장하거나 수정하는 기능을 수행합니다.
	즉, 새로운 상품을 추가하거나 기존 상품 정보를 업데이트할 때 사용됩니다.

```java
    public void save(Product product) {
        productRepository.save(product);
    }
```

#### \# 서비스(OrderService) (스프링) (07.주문.txt)
(service - OrderService 파일에 코드 추가)

```java
	public String cancelOrder(Long orderId)
```

	주문된 상품에 대한 `취소` 기능을 수행하는 메소드를 구현합니다.
	이 메소드는 주문된 상품에 대해 취소 요청이 들어왔을 때 처리하는 서비스 로직입니다.

```java
	@Transactional 어노테이션이 적용되어 있어, 메소드 내에서 발생하는 모든 데이터 변경 사항은
```

	트랜잭션 단위로 처리되며, 도중에 예외가 발생하면 전체 작업이 롤백됩니다.
- 코드 추가
```java
@Transactional
public String cancelOrder(Long orderId){
	Optional<Order> orderOptional = orderRepository.findById(orderId) ;

	// 주문이 있는지 확인
	if (orderOptional.isEmpty()){
		throw new IllegalArgumentException("해당 주문이 존재하지 않습니다. Id : " + orderId);
	}

	// 주문한 상품을 for문으로 돌림 (취소했으니까 주문 신청한 상품만큼 재고에 다시 넣기)
	Order order = orderOptional.get() ;

	for (OrderProduct op : order.getOrderProducts()){
		Product product = op.getProduct() ;
		int quantity = op.getQuantity() ;

		// 현재 자신이 가진 재고에 quantity를 더 함
		product.setStock(product.getStock() + quantity);

		productService.save(product);
	}

	orderRepository.deleteById(orderId);

	return "주문이 취소 되었습니다." ;
}
```

#### \# 컨트롤러(Order) (스프링) (07.주문.txt)
(controller - OrderController 파일에 코드 추가)

```java
	public ResponseEntity<String> cancelOrder(@PathVariable Long orderId)
```

	주문된 상품에 대한 `취소` 기능을 요청하였습니다.
	`관리자` 또는 `당사자`가 주문에 대한 삭제 요청을 하였으므로, 주문된 상품에 대한 `취소` 기능을 처리합니다.

```java
	요청 경로의 {orderId}는 취소하고자 하는 주문의 고유 번호를 의미합니다.
```
- 코드 추가
```java
@DeleteMapping("/delete/{orderId}")
    public ResponseEntity<String> cancelOrder(@PathVariable Long orderId){
        try {
            String message = orderService.cancelOrder(orderId);
            return ResponseEntity.ok(message);

        }catch(IllegalArgumentException err){
            return ResponseEntity.notFound().build() ;
        }
    }
```

#### \# REST 매핑 관련 (IT 관련 용어 PDF (P.62))
매핑을 어떤 매핑을 쓸지 대략적으로 사용예시가 있는데
미리 어떤 매핑을 쓸지 고민해보는 것도 좋음

#### \# 테스트 시나리오 (MySQL) (07.주문.txt)

```sql
select m.member_id, o.order_id, op.product_id, op.quantity, p.price, o.order_status, p.stock
from ((members m join orders o
```
on m.member_id = o.member_id) join order_products op
on o.order_id = op.order_id) join products p
on op.product_id = p.product_id

```java
order by order_id desc, product_id asc ;
```
※ 관리자 모드
일반인 고객이 주문 번호 xxx번에 대한 `취소` 요청을 하였습니다.
`취소` 버튼을 클릭하게 되면 주문 내역 목록 화면에서 사라져야 합니다.

※ `주문 취소` 테스트
	주문 취소할 상품들의 재고 수량을 먼저 파악합니다.
	주문 목록 화면에서 사라져야 합니다.
	데이터 베이스에서 주문(orders 테이블 행 삭제)이 삭제되어야 합니다.
	데이터 베이스에서 주문 상품(order_products 테이블 행 삭제)이 삭제되어야 합니다.
	주문 상품들에 대한 재고 수량이 더해 져야 합니다.



# 5. 14:20 ~ 15:10
#### \# 제08장-03. 홈 페이지 대표 상품 캐러셀 구성 (08.페이징과 필드 검색.txt)

#### \# 홈 페이지(HomePage.tsx) (리액트) (08.페이징과 필드 검색.txt)
(pages 폴더에 HomePage.tsx 파일 코드 추가)
	현재 메인 홈 페이지는 정적인 방식으로 작성되어 있습니다.
	예를 들면 아메리카노 관련 이미지를 불러 들이기 위하여 다음과 같이 직접 파일 이름을 작성하고 있습니다.
	이 부분은 백앤드 서버 데이터 베이스에서 전달 받은 상품 정보를 이용해 홈페이지가 자동으로 구성되도록 수정하도록 합니다.
	소스 코드에 대한 별도의 백업 파일인 ""를 만들어 두었습니다.

```html
	<img
		className="d-block w-100"
		src={`${API_BASE_URL}/images/americano01_bigsize.png`}
		alt="아메리카노"
	/>
```
구현 내용

```typescript
	const [products, setProducts] = useState([]);
const [products, setProducts] = useState<Product[]>([]);

	const navigate = useNavigate();
const navigate = useNavigate();

	useEffect() 훅 구현
useEffect(() => { // 필터링할건데 다른 것들과는 구분되는 무언가가 필요함 (파라미터)
        const url = `${API_BASE_URL}/product?filter=bigs`;

        // try / catch 와는 다른 방식 (많은 양말고 한 줄 정도 양일때 사용)
        customAxios.get(url)
            // 문제가 없으면 then()
            .then((response) => setProducts(response.data))
            // 문제가 있으면 catch()
            .catch((error) => console.log(error));

    }, []);
	--------------------------------------------------------------- 일단 위의 3개 메소드만 만들었음


	detailView() 함수 구현
	<Carousel> 영역을 JavaScript의 map 함수를 사용하여 반복합니다.
	긴 글자(15자 이상)는 짧게 보여 주고, 후위에 ...을 보여줍니다.
```

	이미지를 클릭하면 상세 보기 페이지로 이동합니다.
- 코드 작성 : HomePage02.txt 파일

#### \# 레포지터리(ProductRepository) 작성 (스프링) (08.페이징과 필드 검색.txt)
(repository 폴더에 ProductRepository 파일 코드 추가)

```java
	List<Product> findByImageContaining(String keyword);
```

	상품 정보를 읽어 오기 위하여 다음과 같이 작성합니다.
	image 컬럼에 특정 문자열이 포함된 데이터를 조회하기 위한 코드입니다.
	데이터 베이스의 like 키워드와 유사합니다.

```java
		select * from products where image like '%bigs%';
```

* SpringBoot 교안 PDF (P.92) 참고 (Like 키워드)
- 코드 추가
```java
List<Product> findByImageContaining(String keyword);
```

#### \# 서비스(ProductService) 작성 (스프링) (08.페이징과 필드 검색.txt)
(service 폴더에 ProductService 파일 코드 추가)

```java
	public List<Product> getProductsByFilter(String filter)
```

	서비스 영역에서 이미지 이름에 특정 문자열이 포함된 상품만 가져오거나, 필터가 없으면 전체 상품을 반환하는 메소드입니다.
- 코드 추가
```java
public List<Product> getProductsByFilter(String filter){
	// 어떤 값이 있다는 것
	if (filter != null && !filter.isEmpty()){
		return productRepository.findByImageContaining(filter);
	}
	return productRepository.findAll() ;
}
```

#### \# 컨트롤러(ProductController) 작성 (스프링) (08.페이징과 필드 검색.txt)
(controller 폴더에 ProductController 파일 코드 추가)

```java
	public List<Product> getBigsizeProducts(@RequestParam(required = false) String filter)
```

	홈 페이지에서 큰 이미지 상품을 가져오기 위해, 선택적으로 필터를 적용하여 상품 리스트를 반환하는 API 엔드포인트 역할을 합니다.
	큰 이미지 상품 정보를 요청할 때 호출되는 컨트롤러로, 쿼리 파라미터 filter는 옵션 사항이며 필터 문자열이
	전달되지 않으면 null이 되므로, 전체 상품을 반환하게 됩니다.
- 코드 추가
```java
@GetMapping("")
public List<Product> getBigsizeProducts(@RequestParam(required = false) String filter){
	return productService.getProductsByFilter(filter) ;
}
```

#### \# 일단 결과 값을 보기 위해 HomePage.tsx에 코드 추가
return의 첫번째 <Carousel> 밑에
- 코드 추가
조회 갯수 : {products.length}개
- 확인하기
리액트의 홈페이지 화면을 보면 조회 갯수가 표시됨
(조회 갯수 : 5개)



# 6. 15:20 ~ 16:10
#### \# 홈 페이지(HomePage.tsx) (리액트) (08.페이징과 필드 검색.txt)
(pages 폴더에 HomePage.tsx 파일 코드 추가)
	현재 메인 홈 페이지는 정적인 방식으로 작성되어 있습니다.
	예를 들면 아메리카노 관련 이미지를 불러 들이기 위하여 다음과 같이 직접 파일 이름을 작성하고 있습니다.
	이 부분은 백앤드 서버 데이터 베이스에서 전달 받은 상품 정보를 이용해 홈페이지가 자동으로 구성되도록 수정하도록 합니다.
	소스 코드에 대한 별도의 백업 파일인 ""를 만들어 두었습니다.

```html
	<img
		className="d-block w-100"
		src={`${API_BASE_URL}/images/americano01_bigsize.png`}
		alt="아메리카노"
	/>
```
구현 내용

```typescript
	const [products, setProducts] = useState([]);
	const navigate = useNavigate();
	useEffect() 훅 구현
	--------------------------------------------------------------- 일단 위의 3개 메소드만 만들었음
	밑에 꺼하기

	detailView() 함수 구현
const detailView = (id: number) => {
	navigate(`/product/detail/${id}`);
};

	<Carousel> 영역을 JavaScript의 map 함수를 사용하여 반복합니다.
	긴 글자(15자 이상)는 짧게 보여 주고, 후위에 ...을 보여줍니다.
```

	이미지를 클릭하면 상세 보기 페이지로 이동합니다.

```typescript
<Carousel>
	{products.map((bean) => (
		<Carousel.Item key={bean.id}>
			<img
				className="d-block w-100"
				src={`${API_BASE_URL}/images/${bean.image}`}
				alt={bean.name}
				style={{ cursor: 'pointer' }} // 마우스 오버시 손가락 모양
				onClick={() => detailView(bean.id)} // 클릭시 상세 보기 페이지
			/>
			<Carousel.Caption>
				<h3>{bean.name}</h3>
				<p>
					{/* 긴 글자는 짧게 보여 주고, 후위에 ...을 보여줍니다. */}
					{bean.description.length > 15
						? bean.description.substring(0, 15) + '...'
						: bean.description
					}
				</p>
			</Carousel.Caption>
		</Carousel.Item>
	))}
</Carousel>
```
+ 뒤에 하드코딩한 <Carousel.Item>들을 삭제하기

#### \# 제08장-01. 페이징 (08.페이징과 필드 검색.txt)

#### \# 필드 검색 기능.pdf (08.페이징과 필드 검색.txt)
PDF로 페이징과 필드검색 확인하기

#### \# 페이징(Paging.ts) : Paging TypeScript 파일 (리액트) (08.페이징과 필드 검색.txt)
(types 폴더에 Paging.ts 파일 생성)
	페이징 처리를 위한 파일을 다음과 같이 정의합니다.

```typescript
	type PagingInfo 타입 정의
	const initialPagingInfo 초기 값 정의
```
- 코드 작성 : Paging.ts 파일
```typescript
// Step01. 페이징과 검색 조건 타입
// 페이징 정보 (백엔드 Page 객체 대응)
export type PagingInfo = {
    totalElements: number; // 전체 데이터 개수
    pageSize: number;      // 페이지당 데이터 수
    totalPages: number;    // 전체 페이지 수
    pageNumber: number;    // 현재 페이지 번호 (0-based)
    pageCount: number;     // 하단 페이지 버튼 개수
    beginPage: number;     // 시작 페이지 번호
    endPage: number;       // 끝 페이지 번호
    pagingStatus: string;  // "1 / 10 페이지"
};


// Step02. 초기 값 정의
export const initialPagingInfo: PagingInfo = {
    totalElements: 0,
    pageSize: 6,
    totalPages: 0,
    pageNumber: 0,
    pageCount: 10,
    beginPage: 0,
    endPage: 0,
    pagingStatus: ''
};
```

#### \# React Bootstrap 들어가기
https://react-bootstrap.github.io/docs/components/pagination/
페이지네이션 (pagination)

#### \# 컴포넌트 종류
- 함수형 컴포넌트
```typescript
function App() {}
```
- 클래스형 컴포넌트



# 7. 16:20 ~ 17:10
#### \# 페이징 컴포넌트(Paging.tsx) (리액트) (08.페이징과 필드 검색.txt)
(pages 폴더에 Paging.tsx 파일 생성)

```typescript
	<Pagination> 사용하기
```

	※ '맨처음', '이전', '다음', '맨끝' 버튼
	※ 숫자 링크가 들어가는 영역
- 코드 작성
```typescript
import { Pagination } from "react-bootstrap";
import type { PagingInfo } from "../types/Paging";

// 프롭스 받기
type Props = {
    paging: PagingInfo,
    setPaging: React.Dispatch<React.SetStateAction<PagingInfo>>
}

// 사용할 프롭스 꺼내고 재정의하기
function App({ paging, setPaging }: Props) {
    console.log('자바스크립트 코딩 영역');

    return (
        // 페이징을 가운데로 오게 하기
        <Pagination className="justify-content-center mt-4">
            <Pagination.First // 이전 값 중에서 나머지 값은 전개시키고 pageNumber만 교체하기
                onClick={() => setPaging((prev) => ({ ...prev, pageNumber: 0 }))}
                // 선택 못하게 만들기 (버튼 비활성화)
                // 맨처음은 처음 하단 페이지 버튼의 갯수보다 현재 페이지숫자가 크면 눌러도 됨
                // disabled={true} 이면 비활성화하게 됨
                disabled={paging.pageNumber < paging.pageCount}
            >
                맨처음
            </Pagination.First>

            <Pagination.Prev
                onClick={() => {
                    const gotoPage = paging.beginPage - 1;
                    setPaging(prev => ({ ...prev, pageNumber: gotoPage }));
                }}
                disabled={paging.pageNumber < paging.pageCount}
            >
                이전
            </Pagination.Prev>

            {/* ...은 전개 연산자 */}
            {/* 밑에 배열은 원소 10개짜리 배열임 */}
            {/* 무슨말인지 몰라서 다시 공부해야함 */}
            {/* 꼭 알아야 하는 부분 / 중요!!! */}
            {[...Array(paging.endPage - paging.beginPage + 1)].map((_, idx) => {
                const pageIndex = paging.beginPage + idx + 1;

                return (
                    <Pagination.Item
                        key={pageIndex}
                        active={paging.pageNumber === (pageIndex - 1)}
                        onClick={() =>
                            setPaging(prev => ({
                                ...prev,
                                pageNumber: pageIndex - 1
                            }))
                        }
                    >
                        {pageIndex}
                    </Pagination.Item>
                );
            })}

            <Pagination.Next
                onClick={() => {
                    const gotoPage = paging.endPage + 1;
                    setPaging(prev => ({ ...prev, pageNumber: gotoPage }));
                }} // 수학 공식을 모아놓은 기본 객체인 Math 객체를 이용함
                disabled={paging.pageNumber >= Math.floor(paging.totalPages / paging.pageCount)
```

                    * paging.pageCount}

```typescript
            >
                다음
            </Pagination.Next>

            <Pagination.Last
                onClick={() => {
                    const gotoPage = paging.totalPages - 1;
                    setPaging(prev => ({ ...prev, pageNumber: gotoPage }));
                }} // 수학 공식을 모아놓은 기본 객체인 Math 객체를 이용함
                disabled={paging.pageNumber >= Math.floor(paging.totalPages / paging.pageCount)
```

                    * paging.pageCount}

```typescript
            >
                맨끝
            </Pagination.Last>

        </Pagination>
    );
};

export default App;
```



# 8. 17:20 ~ 18:10
#### \# 상품 목록(ProductList.tsx) (리액트) (08.페이징과 필드 검색.txt)
(pages 폴더에 ProductList 파일에 코드 추가)
	페이징을 위한 관련 항목들을 작성합니다.

```typescript
	import Paging from "./Paging";
```

	관련 스테이트 정의

```typescript
		import { initialPagingInfo, type PagingInfo } from "../types/Paging";
		const [paging, setPaging] = useState<PagingInfo>(initialPagingInfo);
```
- 코드 작성 : ProductList.txt 파일 참조
```typescript
import { Button, Card, Col, Container, Row } from "react-bootstrap";
import { useEffect, useState } from "react";
import type { Product } from "../types/Product";
import type { User } from "../types/User";

import customAxios from './../api/axiosInstance';
import axios from "axios";

import { Link, useNavigate } from "react-router-dom";

import { API_BASE_URL } from "../config/config";

// import Paging from "./Paging"; (.tsx 가져오기)
import Paging from "./Paging";

// 관련 스테이트 정의 (ts.가져오기)
import { initialPagingInfo, type PagingInfo } from "../types/Paging";

// 넘어온 user를 위한 ProductProps type를 정의합니다.
type ProductProps = { // user: User는 자바의 객체: 클래스 정도로 이해하면 됨
    user: User | null; // 로그인하면 의미 있는 객체, 아니면 null
};


function App({ user }: ProductProps) {
    const [products, setProducts] = useState<Product[]>([]);

    // 관련 스테이트 정의 (ts.가져오기)
    const [paging, setPaging] = useState<PagingInfo>(initialPagingInfo);

    useEffect(() => {
        const url = `${API_BASE_URL}/product/list`;
        customAxios.get(url)
            .then((response) => {
                console.log('응답 받은 데이터');
                console.log(response);
                setProducts(response.data);
            })
            .catch((error) => {
                console.log(error);
            });
    }, []);

    const navigate = useNavigate();

    const makeAdminButtons = (item: Product, user: User | null, navigate: any) => {
        if (user?.role !== 'ADMIN') return null;

        return (
            <div className="d-flex justify-content-center">
                <Button // 수정을 위한 <Button>을 추가합니다.
                    variant="warning"
                    className="mb-2"
                    size="sm"
                    onClick={(event) => {
                        event.stopPropagation(); // 이벤트 버블링 방지
                        navigate(`/product/update/${item.id}`); // 유일한 id가 있음
                    }}>
                    수정
                </Button>

                &nbsp;

                <Button // 삭제를 위한 <Button>을 추가합니다. (confirm 함수 이용)(alert과는 다름)
                    variant="danger"
                    className="mb-2"
                    size="sm"
                    onClick={async (event) => {
                        event.stopPropagation(); // 이벤트 버블링 방지

                        const isDelete = window.confirm(`${item.name} 상품을 삭제하시겠습니까?`);

                        if (isDelete === false) {
                            /* sweet alert2 사이트에 이쁜거 많음 */
                            alert(`${item.name} 상품 삭제를 취소하였습니다.`)
                            return;
                        }

                        try { // 전체 배열에서 일부 데이터만 필터할 수 있음
                            const url = `${API_BASE_URL}/product/delete/${item.id}`;
                            await axios.delete(url);
                            alert(`'${item.name}' 상품이 삭제되었습니다.`)

                            // 상품을 갱신해주는 setter
                            // 이전(prev) 상품 정보를 가져와서 필터링하는데
                            // 이전 상품의 id와 해당 상품 id가 같지 않으면 - 상품 데이터에 해당 상품이 없으면
                            // 상품(목록)을 갱신해라
                            setProducts(prev => prev.filter(p => p.id !== item.id));

                            navigate('/product/list');

                        } catch (error) {
                            console.log(error);
                            if (axios.isAxiosError(error)) {
                                alert(`상품 삭제 실패 : ${error.response?.data || error.message}`);
                            } else {
                                console.log('알수 없는 에러 : ' + error);
                            }
                        };
                    }}>
                    삭제
                </Button>
            </div>
        );
    };

    return (
        <Container className="my-4">
            <h1 className="my-4">상품 목록 페이지</h1>

            {/* 상품 등록을 위한 <Link>를 추가합니다. */}
            <Link to={`/product/insert`}>
                {/* 수정, 삭제를 위한 <Button>을 추가합니다. */}
                {user?.role === 'ADMIN' && (
                    <Button variant="primary" className="mb-3">
                        상품 등록
                    </Button>
                )}
            </Link>


            {/* 필드 검색 영역 */}

            {/* 자료 보여 주는 영역 */}
            <Row>
                {/* products는 상품 배열, item는 상품 1개를 의미 */}
                {products.map((item) => (
                    <Col key={item.id} md={4} className="mb-4">
                        <Card className="h-100"
                            onClick={() => navigate(`/product/detail/${item.id}`)}
                            style={{ cursor: 'pointer' }}>

                            <Card.Img
                                variant="top"
                                src={`${API_BASE_URL}/images/${item.image}`}
                                alt={item.name}
                                style={{ width: '100%', height: '200px' }}
                            />
                            <Card.Body>
                                <table style={{ width: '100%', borderCollapse: 'collapse', border: 'none' }}>
                                    <tbody>
                                        <tr>
                                            <td style={{ width: '70%', padding: '4px', border: 'none' }}>
                                                <Card.Title>{item.name}({item.id})</Card.Title>
                                            </td>
                                            <td rowSpan={2} style={{ padding: '4px', border: 'none', textAlign: 'center', verticalAlign: 'middle' }}>
                                                {makeAdminButtons(item, user, navigate)}
                                            </td>
                                        </tr>
                                        <tr>
                                            <td style={{ width: '70%', padding: '4px', border: 'none' }}>
                                                <Card.Text>가격 : {item.price.toLocaleString()} 원</Card.Text>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>

                            </Card.Body>
                        </Card>
                    </Col>
                ))}
            </Row>

            {/* 페이징 처리 영역 */}
            <Paging
                paging={paging}
                setPaging={setPaging}
            />

        </Container>
    );
};

export default App;
```
