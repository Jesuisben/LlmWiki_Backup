\============================================================
\# 리눅스 : Gomdori(id : gomdori, pwd : ubuntu)
\============================================================
Gomdori login : gomdori 입력
Password : ubuntu 입력

sudo poweroff로 피시 끄기
\============================================================
Virtual Box
	Gomdori-설정-네트워크-네트워크 어댑터 : "어댑터에 브리지"으로 설정

Gomdori 재시작 후 로그인
	ip addr 엔터
		enp0s3: 항목의 inet 확인
		192.168.0.xx

	sudo apt install -y openssh-server
	sudo ufw enable
	sudo ufw allow ssh
	sudo ufw status
	sudo reboot
============================================================
MobaXTerm으로 접속하기
\============================================================
\# 도커 셋업 : docker_setup.sh.txt 사용

\# docker_setup.sh.txt 파일을 리눅스에 복사합니다.

sudo cp docker_setup.sh.txt /root/docker_setup.sh

sudo su -

apt update

apt install -y dos2unix

dos2unix docker_setup.sh

chmod +x docker_setup.sh

./docker_setup.sh

docker container -- help

systemctl status docker

exit 

sudo usermod -aG docker $USER

docker version 
\# Permission denied가 보이면 로그 아웃 후 다시 접속합니다.

logout 

docker version 
\============================================================
\# 실습 교안 : 113~118
\============================================================
옵션 사항
	wordpress 대신 개별적으로 이미지 만들어서 실습하기
============================================================